/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class BadSignalElement extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "BadSignalElement";
   }

   protected SV_ID badSVID;
   protected GNSS_SignalIDs badSignalID;  // optional
   protected Asn1OpenExt extElem1;

   public BadSignalElement () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public BadSignalElement (
      SV_ID badSVID_,
      GNSS_SignalIDs badSignalID_
   ) {
      super();
      setBadSVID (badSVID_);
      setBadSignalID (badSignalID_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public BadSignalElement (
      SV_ID badSVID_
   ) {
      super();
      setBadSVID (badSVID_);
   }

   public void init () {
      badSVID = null;
      badSignalID = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof BadSignalElement) ) return false;

      BadSignalElement rhs = (BadSignalElement) obj;

      if (badSVID == null) {
         if (rhs.badSVID != null) return false;
      }
      else {
         if (!badSVID.equals(rhs.badSVID)) {
            return false;
         }
      }

      if (badSignalID == null) {
         if (rhs.badSignalID != null) return false;
      }
      else {
         if (!badSignalID.equals(rhs.badSignalID)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (badSVID != null) __code = 31*__code + badSVID.hashCode();
      if (badSignalID != null) __code = 31*__code + badSignalID.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for BadSVID
    */
   public SV_ID getBadSVID () {
      return badSVID;
   }

   public void setBadSVID (SV_ID value) {
      this.badSVID = value;
   }

   /**
    * Accessor/mutator methods for BadSignalID
    */
   public GNSS_SignalIDs getBadSignalID () {
      return badSignalID;
   }

   public void setBadSignalID (GNSS_SignalIDs value) {
      this.badSignalID = value;
   }

   public boolean hasBadSignalID () {
      return (badSignalID != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return badSVID;
         case 1: return badSignalID;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "badSVID";
         case 1: return "badSignalID";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean badSignalIDPresent = buffer.decodeBit ("badSignalIDPresent");

      // decode badSVID

      buffer.getContext().eventDispatcher.startElement("badSVID", -1);

      badSVID = new SV_ID();
      badSVID.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("badSVID", -1);

      // decode badSignalID

      if (badSignalIDPresent) {
         buffer.getContext().eventDispatcher.startElement("badSignalID", -1);

         badSignalID = new GNSS_SignalIDs();
         badSignalID.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("badSignalID", -1);
      }
      else {
         badSignalID = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((badSignalID != null), null);

      // encode badSVID

      if (badSVID != null) {
         buffer.getContext().eventDispatcher.startElement("badSVID", -1);

         badSVID.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("badSVID", -1);
      }
      else throw new Asn1MissingRequiredException ("badSVID");

      // encode badSignalID

      if (badSignalID != null) {
         buffer.getContext().eventDispatcher.startElement("badSignalID", -1);

         badSignalID.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("badSignalID", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
