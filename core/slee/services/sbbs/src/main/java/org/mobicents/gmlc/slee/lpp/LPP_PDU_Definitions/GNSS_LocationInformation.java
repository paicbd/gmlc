/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class GNSS_LocationInformation extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "GNSS-LocationInformation";
   }

   protected MeasurementReferenceTime measurementReferenceTime;
   protected GNSS_ID_Bitmap agnss_List;
   protected Asn1OpenExt extElem1;

   public GNSS_LocationInformation () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public GNSS_LocationInformation (
      MeasurementReferenceTime measurementReferenceTime_,
      GNSS_ID_Bitmap agnss_List_
   ) {
      super();
      setMeasurementReferenceTime (measurementReferenceTime_);
      setAgnss_List (agnss_List_);
   }

   public void init () {
      measurementReferenceTime = null;
      agnss_List = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof GNSS_LocationInformation) ) return false;

      GNSS_LocationInformation rhs = (GNSS_LocationInformation) obj;

      if (measurementReferenceTime == null) {
         if (rhs.measurementReferenceTime != null) return false;
      }
      else {
         if (!measurementReferenceTime.equals(rhs.measurementReferenceTime)) {
            return false;
         }
      }

      if (agnss_List == null) {
         if (rhs.agnss_List != null) return false;
      }
      else {
         if (!agnss_List.equals(rhs.agnss_List)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (measurementReferenceTime != null) __code = 31*__code + measurementReferenceTime.hashCode();
      if (agnss_List != null) __code = 31*__code + agnss_List.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for MeasurementReferenceTime
    */
   public MeasurementReferenceTime getMeasurementReferenceTime () {
      return measurementReferenceTime;
   }

   public void setMeasurementReferenceTime (MeasurementReferenceTime value) {
      this.measurementReferenceTime = value;
   }

   /**
    * Accessor/mutator methods for Agnss_List
    */
   public GNSS_ID_Bitmap getAgnss_List () {
      return agnss_List;
   }

   public void setAgnss_List (GNSS_ID_Bitmap value) {
      this.agnss_List = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return measurementReferenceTime;
         case 1: return agnss_List;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "measurementReferenceTime";
         case 1: return "agnss-List";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode measurementReferenceTime

      buffer.getContext().eventDispatcher.startElement("measurementReferenceTime", -1);

      measurementReferenceTime = new MeasurementReferenceTime();
      measurementReferenceTime.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("measurementReferenceTime", -1);

      // decode agnss_List

      buffer.getContext().eventDispatcher.startElement("agnss_List", -1);

      agnss_List = new GNSS_ID_Bitmap();
      agnss_List.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("agnss_List", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode measurementReferenceTime

      if (measurementReferenceTime != null) {
         buffer.getContext().eventDispatcher.startElement("measurementReferenceTime", -1);

         measurementReferenceTime.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("measurementReferenceTime", -1);
      }
      else throw new Asn1MissingRequiredException ("measurementReferenceTime");

      // encode agnss_List

      if (agnss_List != null) {
         buffer.getContext().eventDispatcher.startElement("agnss_List", -1);

         agnss_List.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("agnss_List", -1);
      }
      else throw new Asn1MissingRequiredException ("agnss_List");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
