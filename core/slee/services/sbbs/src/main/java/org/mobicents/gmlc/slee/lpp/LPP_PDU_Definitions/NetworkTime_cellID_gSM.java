/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class NetworkTime_cellID_gSM extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "SEQUENCE";
   }

   protected Asn1Integer bcchCarrier;
   protected Asn1Integer bsic;
   protected CellGlobalIdGERAN cellGlobalIdGERAN;  // optional
   protected Asn1OpenExt extElem1;

   public NetworkTime_cellID_gSM () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public NetworkTime_cellID_gSM (
      Asn1Integer bcchCarrier_,
      Asn1Integer bsic_,
      CellGlobalIdGERAN cellGlobalIdGERAN_
   ) throws Asn1Exception {
      super();
      setBcchCarrier (bcchCarrier_);
      setBsic (bsic_);
      setCellGlobalIdGERAN (cellGlobalIdGERAN_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public NetworkTime_cellID_gSM (
      Asn1Integer bcchCarrier_,
      Asn1Integer bsic_
   ) throws Asn1Exception {
      super();
      setBcchCarrier (bcchCarrier_);
      setBsic (bsic_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public NetworkTime_cellID_gSM (long bcchCarrier_,
      long bsic_,
      CellGlobalIdGERAN cellGlobalIdGERAN_
   ) throws Asn1Exception {
      super();
      setBcchCarrier (bcchCarrier_);
      setBsic (bsic_);
      setCellGlobalIdGERAN (cellGlobalIdGERAN_);
   }

   /**
    * This constructor is for required elements only.  It allows 
    * primitive data to be passed for all primitive elements.  
    * It will create new object wrappers for the primitive data 
    * and set other elements to references to the given objects. 
    */
   public NetworkTime_cellID_gSM (
      long bcchCarrier_,
      long bsic_
   ) throws Asn1Exception {
      super();
      setBcchCarrier (bcchCarrier_);
      setBsic (bsic_);
   }

   public void init () {
      bcchCarrier = null;
      bsic = null;
      cellGlobalIdGERAN = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof NetworkTime_cellID_gSM) ) return false;

      NetworkTime_cellID_gSM rhs = (NetworkTime_cellID_gSM) obj;

      if (bcchCarrier == null) {
         if (rhs.bcchCarrier != null) return false;
      }
      else {
         if (!bcchCarrier.equals(rhs.bcchCarrier)) {
            return false;
         }
      }

      if (bsic == null) {
         if (rhs.bsic != null) return false;
      }
      else {
         if (!bsic.equals(rhs.bsic)) {
            return false;
         }
      }

      if (cellGlobalIdGERAN == null) {
         if (rhs.cellGlobalIdGERAN != null) return false;
      }
      else {
         if (!cellGlobalIdGERAN.equals(rhs.cellGlobalIdGERAN)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (bcchCarrier != null) __code = 31*__code + bcchCarrier.hashCode();
      if (bsic != null) __code = 31*__code + bsic.hashCode();
      if (cellGlobalIdGERAN != null) __code = 31*__code + cellGlobalIdGERAN.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for BcchCarrier
    */
   public Asn1Integer getBcchCarrier () {
      return bcchCarrier;
   }

   public void setBcchCarrier (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 1023))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.bcchCarrier = value;
   }

   public void setBcchCarrier (long value) 
      throws Asn1Exception
   {
      setBcchCarrier (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for Bsic
    */
   public Asn1Integer getBsic () {
      return bsic;
   }

   public void setBsic (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 63))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.bsic = value;
   }

   public void setBsic (long value) 
      throws Asn1Exception
   {
      setBsic (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for CellGlobalIdGERAN
    */
   public CellGlobalIdGERAN getCellGlobalIdGERAN () {
      return cellGlobalIdGERAN;
   }

   public void setCellGlobalIdGERAN (CellGlobalIdGERAN value) {
      this.cellGlobalIdGERAN = value;
   }

   public boolean hasCellGlobalIdGERAN () {
      return (cellGlobalIdGERAN != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 4; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return bcchCarrier;
         case 1: return bsic;
         case 2: return cellGlobalIdGERAN;
         case 3: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "bcchCarrier";
         case 1: return "bsic";
         case 2: return "cellGlobalIdGERAN";
         case 3: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean cellGlobalIdGERANPresent = buffer.decodeBit ("cellGlobalIdGERANPresent");

      // decode bcchCarrier

      buffer.getContext().eventDispatcher.startElement("bcchCarrier", -1);

      bcchCarrier = new Asn1Integer();
      bcchCarrier.decode (buffer, 0, 1023);
      buffer.getContext().eventDispatcher.endElement("bcchCarrier", -1);

      // decode bsic

      buffer.getContext().eventDispatcher.startElement("bsic", -1);

      bsic = new Asn1Integer();
      bsic.decode (buffer, 0, 63);
      buffer.getContext().eventDispatcher.endElement("bsic", -1);

      // decode cellGlobalIdGERAN

      if (cellGlobalIdGERANPresent) {
         buffer.getContext().eventDispatcher.startElement("cellGlobalIdGERAN", -1);

         cellGlobalIdGERAN = new CellGlobalIdGERAN();
         cellGlobalIdGERAN.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("cellGlobalIdGERAN", -1);
      }
      else {
         cellGlobalIdGERAN = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((cellGlobalIdGERAN != null), null);

      // encode bcchCarrier

      if (bcchCarrier != null) {
         buffer.getContext().eventDispatcher.startElement("bcchCarrier", -1);

         bcchCarrier.encode (buffer, 0, 1023);

         buffer.getContext().eventDispatcher.endElement("bcchCarrier", -1);
      }
      else throw new Asn1MissingRequiredException ("bcchCarrier");

      // encode bsic

      if (bsic != null) {
         buffer.getContext().eventDispatcher.startElement("bsic", -1);

         bsic.encode (buffer, 0, 63);

         buffer.getContext().eventDispatcher.endElement("bsic", -1);
      }
      else throw new Asn1MissingRequiredException ("bsic");

      // encode cellGlobalIdGERAN

      if (cellGlobalIdGERAN != null) {
         buffer.getContext().eventDispatcher.startElement("cellGlobalIdGERAN", -1);

         cellGlobalIdGERAN.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("cellGlobalIdGERAN", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
