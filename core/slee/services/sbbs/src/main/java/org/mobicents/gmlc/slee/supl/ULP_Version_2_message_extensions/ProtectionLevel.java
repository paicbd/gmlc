/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.ULP_Version_2_message_extensions;

import com.objsys.asn1j.runtime.*;

public class ProtectionLevel extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_ULP_Version_2_message_extensionsRtkey._rtkey);
      Asn1Type._setLicLocation(_ULP_Version_2_message_extensionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "ProtectionLevel";
   }

   protected ProtLevel protlevel = null;
   protected BasicProtectionParams basicProtectionParams;  // optional
   protected Asn1OpenExt extElem1;

   public ProtectionLevel () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public ProtectionLevel (
      ProtLevel protlevel_,
      BasicProtectionParams basicProtectionParams_
   ) {
      super();
      setProtlevel (protlevel_);
      setBasicProtectionParams (basicProtectionParams_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public ProtectionLevel (
      ProtLevel protlevel_
   ) {
      super();
      setProtlevel (protlevel_);
   }

   public void init () {
      protlevel = null;
      basicProtectionParams = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof ProtectionLevel) ) return false;

      ProtectionLevel rhs = (ProtectionLevel) obj;

      if (protlevel == null) {
         if (rhs.protlevel != null) return false;
      }
      else {
         if (!protlevel.equals(rhs.protlevel)) {
            return false;
         }
      }

      if (basicProtectionParams == null) {
         if (rhs.basicProtectionParams != null) return false;
      }
      else {
         if (!basicProtectionParams.equals(rhs.basicProtectionParams)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (protlevel != null) __code = 31*__code + protlevel.hashCode();
      if (basicProtectionParams != null) __code = 31*__code + basicProtectionParams.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Protlevel
    */
   public ProtLevel getProtlevel () {
      return protlevel;
   }

   public void setProtlevel (ProtLevel value) {
      this.protlevel = value;
   }

   /**
    * Accessor/mutator methods for BasicProtectionParams
    */
   public BasicProtectionParams getBasicProtectionParams () {
      return basicProtectionParams;
   }

   public void setBasicProtectionParams (BasicProtectionParams value) {
      this.basicProtectionParams = value;
   }

   public boolean hasBasicProtectionParams () {
      return (basicProtectionParams != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return protlevel;
         case 1: return basicProtectionParams;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "protlevel";
         case 1: return "basicProtectionParams";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean basicProtectionParamsPresent = buffer.decodeBit ("basicProtectionParamsPresent");

      // decode protlevel

      buffer.getContext().eventDispatcher.startElement("protlevel", -1);

      {
         int tval = ProtLevel.decodeEnumValue (buffer);
         protlevel = ProtLevel.valueOf (tval);
      }
      buffer.getContext().eventDispatcher.endElement("protlevel", -1);

      // decode basicProtectionParams

      if (basicProtectionParamsPresent) {
         buffer.getContext().eventDispatcher.startElement("basicProtectionParams", -1);

         basicProtectionParams = new BasicProtectionParams();
         basicProtectionParams.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("basicProtectionParams", -1);
      }
      else {
         basicProtectionParams = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((basicProtectionParams != null), null);

      // encode protlevel

      if (protlevel != null) {
         buffer.getContext().eventDispatcher.startElement("protlevel", -1);

         protlevel.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("protlevel", -1);
      }
      else throw new Asn1MissingRequiredException ("protlevel");

      // encode basicProtectionParams

      if (basicProtectionParams != null) {
         buffer.getContext().eventDispatcher.startElement("basicProtectionParams", -1);

         basicProtectionParams.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("basicProtectionParams", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
