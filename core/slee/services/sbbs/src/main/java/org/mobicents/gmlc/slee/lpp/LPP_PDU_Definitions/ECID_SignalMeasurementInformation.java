/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class ECID_SignalMeasurementInformation extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "ECID-SignalMeasurementInformation";
   }

   protected MeasuredResultsElement primaryCellMeasuredResults;  // optional
   protected MeasuredResultsList measuredResultsList;
   protected Asn1OpenExt extElem1;

   public ECID_SignalMeasurementInformation () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public ECID_SignalMeasurementInformation (
      MeasuredResultsElement primaryCellMeasuredResults_,
      MeasuredResultsList measuredResultsList_
   ) throws Asn1Exception {
      super();
      setPrimaryCellMeasuredResults (primaryCellMeasuredResults_);
      setMeasuredResultsList (measuredResultsList_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public ECID_SignalMeasurementInformation (
      MeasuredResultsList measuredResultsList_
   ) throws Asn1Exception {
      super();
      setMeasuredResultsList (measuredResultsList_);
   }

   public void init () {
      primaryCellMeasuredResults = null;
      measuredResultsList = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof ECID_SignalMeasurementInformation) ) return false;

      ECID_SignalMeasurementInformation rhs = (ECID_SignalMeasurementInformation) obj;

      if (primaryCellMeasuredResults == null) {
         if (rhs.primaryCellMeasuredResults != null) return false;
      }
      else {
         if (!primaryCellMeasuredResults.equals(rhs.primaryCellMeasuredResults)) {
            return false;
         }
      }

      if (measuredResultsList == null) {
         if (rhs.measuredResultsList != null) return false;
      }
      else {
         if (!measuredResultsList.equals(rhs.measuredResultsList)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (primaryCellMeasuredResults != null) __code = 31*__code + primaryCellMeasuredResults.hashCode();
      if (measuredResultsList != null) __code = 31*__code + measuredResultsList.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for PrimaryCellMeasuredResults
    */
   public MeasuredResultsElement getPrimaryCellMeasuredResults () {
      return primaryCellMeasuredResults;
   }

   public void setPrimaryCellMeasuredResults (MeasuredResultsElement value) {
      this.primaryCellMeasuredResults = value;
   }

   public boolean hasPrimaryCellMeasuredResults () {
      return (primaryCellMeasuredResults != null);
   }

   /**
    * Accessor/mutator methods for MeasuredResultsList
    */
   public MeasuredResultsList getMeasuredResultsList () {
      return measuredResultsList;
   }

   public void setMeasuredResultsList (MeasuredResultsList value) 
      throws Asn1Exception
   {
      if (!((value.getElements().length >= 1 && value.getElements().length <= 32))) {
         throw new Asn1ConsVioException ("value.getElements().length", value.getElements().length);
      }

      this.measuredResultsList = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return primaryCellMeasuredResults;
         case 1: return measuredResultsList;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "primaryCellMeasuredResults";
         case 1: return "measuredResultsList";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean primaryCellMeasuredResultsPresent = buffer.decodeBit ("primaryCellMeasuredResultsPresent");

      // decode primaryCellMeasuredResults

      if (primaryCellMeasuredResultsPresent) {
         buffer.getContext().eventDispatcher.startElement("primaryCellMeasuredResults", -1);

         primaryCellMeasuredResults = new MeasuredResultsElement();
         primaryCellMeasuredResults.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("primaryCellMeasuredResults", -1);
      }
      else {
         primaryCellMeasuredResults = null;
      }

      // decode measuredResultsList

      buffer.getContext().eventDispatcher.startElement("measuredResultsList", -1);

      measuredResultsList = new MeasuredResultsList();
      measuredResultsList.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("measuredResultsList", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((primaryCellMeasuredResults != null), null);

      // encode primaryCellMeasuredResults

      if (primaryCellMeasuredResults != null) {
         buffer.getContext().eventDispatcher.startElement("primaryCellMeasuredResults", -1);

         primaryCellMeasuredResults.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("primaryCellMeasuredResults", -1);
      }

      // encode measuredResultsList

      if (measuredResultsList != null) {
         buffer.getContext().eventDispatcher.startElement("measuredResultsList", -1);

         measuredResultsList.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("measuredResultsList", -1);
      }
      else throw new Asn1MissingRequiredException ("measuredResultsList");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
