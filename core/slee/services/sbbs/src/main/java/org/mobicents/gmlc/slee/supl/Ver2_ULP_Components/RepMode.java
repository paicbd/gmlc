/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.Ver2_ULP_Components;

import com.objsys.asn1j.runtime.*;

public class RepMode extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_Ver2_ULP_ComponentsRtkey._rtkey);
      Asn1Type._setLicLocation(_Ver2_ULP_ComponentsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "RepMode";
   }

   protected Asn1Boolean realtime;
   protected Asn1Boolean quasirealtime;
   protected Asn1Boolean batch;
   protected Asn1OpenExt extElem1;

   public RepMode () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public RepMode (
      Asn1Boolean realtime_,
      Asn1Boolean quasirealtime_,
      Asn1Boolean batch_
   ) {
      super();
      setRealtime (realtime_);
      setQuasirealtime (quasirealtime_);
      setBatch (batch_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public RepMode (boolean realtime_,
      boolean quasirealtime_,
      boolean batch_
   ) {
      super();
      setRealtime (realtime_);
      setQuasirealtime (quasirealtime_);
      setBatch (batch_);
   }

   public void init () {
      realtime = null;
      quasirealtime = null;
      batch = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof RepMode) ) return false;

      RepMode rhs = (RepMode) obj;

      if (realtime == null) {
         if (rhs.realtime != null) return false;
      }
      else {
         if (!realtime.equals(rhs.realtime)) {
            return false;
         }
      }

      if (quasirealtime == null) {
         if (rhs.quasirealtime != null) return false;
      }
      else {
         if (!quasirealtime.equals(rhs.quasirealtime)) {
            return false;
         }
      }

      if (batch == null) {
         if (rhs.batch != null) return false;
      }
      else {
         if (!batch.equals(rhs.batch)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (realtime != null) __code = 31*__code + realtime.hashCode();
      if (quasirealtime != null) __code = 31*__code + quasirealtime.hashCode();
      if (batch != null) __code = 31*__code + batch.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Realtime
    */
   public Asn1Boolean getRealtime () {
      return realtime;
   }

   public void setRealtime (Asn1Boolean value) {
      this.realtime = value;
   }

   public void setRealtime (boolean value) {
      setRealtime (new Asn1Boolean(value));
   }

   /**
    * Accessor/mutator methods for Quasirealtime
    */
   public Asn1Boolean getQuasirealtime () {
      return quasirealtime;
   }

   public void setQuasirealtime (Asn1Boolean value) {
      this.quasirealtime = value;
   }

   public void setQuasirealtime (boolean value) {
      setQuasirealtime (new Asn1Boolean(value));
   }

   /**
    * Accessor/mutator methods for Batch
    */
   public Asn1Boolean getBatch () {
      return batch;
   }

   public void setBatch (Asn1Boolean value) {
      this.batch = value;
   }

   public void setBatch (boolean value) {
      setBatch (new Asn1Boolean(value));
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 4; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return realtime;
         case 1: return quasirealtime;
         case 2: return batch;
         case 3: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "realtime";
         case 1: return "quasirealtime";
         case 2: return "batch";
         case 3: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode realtime

      buffer.getContext().eventDispatcher.startElement("realtime", -1);

      realtime = new Asn1Boolean();
      realtime.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("realtime", -1);

      // decode quasirealtime

      buffer.getContext().eventDispatcher.startElement("quasirealtime", -1);

      quasirealtime = new Asn1Boolean();
      quasirealtime.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("quasirealtime", -1);

      // decode batch

      buffer.getContext().eventDispatcher.startElement("batch", -1);

      batch = new Asn1Boolean();
      batch.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("batch", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode realtime

      if (realtime != null) {
         buffer.getContext().eventDispatcher.startElement("realtime", -1);

         realtime.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("realtime", -1);
      }
      else throw new Asn1MissingRequiredException ("realtime");

      // encode quasirealtime

      if (quasirealtime != null) {
         buffer.getContext().eventDispatcher.startElement("quasirealtime", -1);

         quasirealtime.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("quasirealtime", -1);
      }
      else throw new Asn1MissingRequiredException ("quasirealtime");

      // encode batch

      if (batch != null) {
         buffer.getContext().eventDispatcher.startElement("batch", -1);

         batch.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("batch", -1);
      }
      else throw new Asn1MissingRequiredException ("batch");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
