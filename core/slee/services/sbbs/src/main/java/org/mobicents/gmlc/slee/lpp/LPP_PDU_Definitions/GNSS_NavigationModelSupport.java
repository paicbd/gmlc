/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class GNSS_NavigationModelSupport extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "GNSS-NavigationModelSupport";
   }

   protected GNSS_NavigationModelSupport_clockModel clockModel;  // optional
   protected GNSS_NavigationModelSupport_orbitModel orbitModel;  // optional
   protected Asn1OpenExt extElem1;

   public GNSS_NavigationModelSupport () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public GNSS_NavigationModelSupport (
      GNSS_NavigationModelSupport_clockModel clockModel_,
      GNSS_NavigationModelSupport_orbitModel orbitModel_
   ) throws Asn1Exception {
      super();
      setClockModel (clockModel_);
      setOrbitModel (orbitModel_);
   }

   public void init () {
      clockModel = null;
      orbitModel = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof GNSS_NavigationModelSupport) ) return false;

      GNSS_NavigationModelSupport rhs = (GNSS_NavigationModelSupport) obj;

      if (clockModel == null) {
         if (rhs.clockModel != null) return false;
      }
      else {
         if (!clockModel.equals(rhs.clockModel)) {
            return false;
         }
      }

      if (orbitModel == null) {
         if (rhs.orbitModel != null) return false;
      }
      else {
         if (!orbitModel.equals(rhs.orbitModel)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (clockModel != null) __code = 31*__code + clockModel.hashCode();
      if (orbitModel != null) __code = 31*__code + orbitModel.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for ClockModel
    */
   public GNSS_NavigationModelSupport_clockModel getClockModel () {
      return clockModel;
   }

   public void setClockModel (GNSS_NavigationModelSupport_clockModel value) 
      throws Asn1Exception
   {
      if (!((value.getLength() >= 1 && value.getLength() <= 8))) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.clockModel = value;
   }

   public boolean hasClockModel () {
      return (clockModel != null);
   }

   /**
    * Accessor/mutator methods for OrbitModel
    */
   public GNSS_NavigationModelSupport_orbitModel getOrbitModel () {
      return orbitModel;
   }

   public void setOrbitModel (GNSS_NavigationModelSupport_orbitModel value) 
      throws Asn1Exception
   {
      if (!((value.getLength() >= 1 && value.getLength() <= 8))) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.orbitModel = value;
   }

   public boolean hasOrbitModel () {
      return (orbitModel != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return clockModel;
         case 1: return orbitModel;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "clockModel";
         case 1: return "orbitModel";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean clockModelPresent = buffer.decodeBit ("clockModelPresent");
      boolean orbitModelPresent = buffer.decodeBit ("orbitModelPresent");

      // decode clockModel

      if (clockModelPresent) {
         buffer.getContext().eventDispatcher.startElement("clockModel", -1);

         clockModel = new GNSS_NavigationModelSupport_clockModel();
         clockModel.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("clockModel", -1);
      }
      else {
         clockModel = null;
      }

      // decode orbitModel

      if (orbitModelPresent) {
         buffer.getContext().eventDispatcher.startElement("orbitModel", -1);

         orbitModel = new GNSS_NavigationModelSupport_orbitModel();
         orbitModel.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("orbitModel", -1);
      }
      else {
         orbitModel = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((clockModel != null), null);
      buffer.encodeBit ((orbitModel != null), null);

      // encode clockModel

      if (clockModel != null) {
         buffer.getContext().eventDispatcher.startElement("clockModel", -1);

         clockModel.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("clockModel", -1);
      }

      // encode orbitModel

      if (orbitModel != null) {
         buffer.getContext().eventDispatcher.startElement("orbitModel", -1);

         orbitModel.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("orbitModel", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
