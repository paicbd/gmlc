/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class A_GNSS_RequestLocationInformation extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "A-GNSS-RequestLocationInformation";
   }

   protected GNSS_PositioningInstructions gnss_PositioningInstructions;
   protected Asn1OpenExt extElem1;

   public A_GNSS_RequestLocationInformation () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public A_GNSS_RequestLocationInformation (
      GNSS_PositioningInstructions gnss_PositioningInstructions_
   ) {
      super();
      setGnss_PositioningInstructions (gnss_PositioningInstructions_);
   }

   public void init () {
      gnss_PositioningInstructions = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof A_GNSS_RequestLocationInformation) ) return false;

      A_GNSS_RequestLocationInformation rhs = (A_GNSS_RequestLocationInformation) obj;

      if (gnss_PositioningInstructions == null) {
         if (rhs.gnss_PositioningInstructions != null) return false;
      }
      else {
         if (!gnss_PositioningInstructions.equals(rhs.gnss_PositioningInstructions)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (gnss_PositioningInstructions != null) __code = 31*__code + gnss_PositioningInstructions.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Gnss_PositioningInstructions
    */
   public GNSS_PositioningInstructions getGnss_PositioningInstructions () {
      return gnss_PositioningInstructions;
   }

   public void setGnss_PositioningInstructions (GNSS_PositioningInstructions value) {
      this.gnss_PositioningInstructions = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 2; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return gnss_PositioningInstructions;
         case 1: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "gnss-PositioningInstructions";
         case 1: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode gnss_PositioningInstructions

      buffer.getContext().eventDispatcher.startElement("gnss_PositioningInstructions", -1);

      gnss_PositioningInstructions = new GNSS_PositioningInstructions();
      gnss_PositioningInstructions.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("gnss_PositioningInstructions", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode gnss_PositioningInstructions

      if (gnss_PositioningInstructions != null) {
         buffer.getContext().eventDispatcher.startElement("gnss_PositioningInstructions", -1);

         gnss_PositioningInstructions.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("gnss_PositioningInstructions", -1);
      }
      else throw new Asn1MissingRequiredException ("gnss_PositioningInstructions");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
