/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class VerticalAccuracy extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "VerticalAccuracy";
   }

   protected Asn1Integer accuracy;
   protected Asn1Integer confidence;
   protected Asn1OpenExt extElem1;

   public VerticalAccuracy () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public VerticalAccuracy (
      Asn1Integer accuracy_,
      Asn1Integer confidence_
   ) throws Asn1Exception {
      super();
      setAccuracy (accuracy_);
      setConfidence (confidence_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public VerticalAccuracy (long accuracy_,
      long confidence_
   ) throws Asn1Exception {
      super();
      setAccuracy (accuracy_);
      setConfidence (confidence_);
   }

   public void init () {
      accuracy = null;
      confidence = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof VerticalAccuracy) ) return false;

      VerticalAccuracy rhs = (VerticalAccuracy) obj;

      if (accuracy == null) {
         if (rhs.accuracy != null) return false;
      }
      else {
         if (!accuracy.equals(rhs.accuracy)) {
            return false;
         }
      }

      if (confidence == null) {
         if (rhs.confidence != null) return false;
      }
      else {
         if (!confidence.equals(rhs.confidence)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (accuracy != null) __code = 31*__code + accuracy.hashCode();
      if (confidence != null) __code = 31*__code + confidence.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Accuracy
    */
   public Asn1Integer getAccuracy () {
      return accuracy;
   }

   public void setAccuracy (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 127))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.accuracy = value;
   }

   public void setAccuracy (long value) 
      throws Asn1Exception
   {
      setAccuracy (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for Confidence
    */
   public Asn1Integer getConfidence () {
      return confidence;
   }

   public void setConfidence (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 100))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.confidence = value;
   }

   public void setConfidence (long value) 
      throws Asn1Exception
   {
      setConfidence (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return accuracy;
         case 1: return confidence;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "accuracy";
         case 1: return "confidence";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode accuracy

      buffer.getContext().eventDispatcher.startElement("accuracy", -1);

      accuracy = new Asn1Integer();
      accuracy.decode (buffer, 0, 127);
      buffer.getContext().eventDispatcher.endElement("accuracy", -1);

      // decode confidence

      buffer.getContext().eventDispatcher.startElement("confidence", -1);

      confidence = new Asn1Integer();
      confidence.decode (buffer, 0, 100);
      buffer.getContext().eventDispatcher.endElement("confidence", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode accuracy

      if (accuracy != null) {
         buffer.getContext().eventDispatcher.startElement("accuracy", -1);

         accuracy.encode (buffer, 0, 127);

         buffer.getContext().eventDispatcher.endElement("accuracy", -1);
      }
      else throw new Asn1MissingRequiredException ("accuracy");

      // encode confidence

      if (confidence != null) {
         buffer.getContext().eventDispatcher.startElement("confidence", -1);

         confidence.encode (buffer, 0, 100);

         buffer.getContext().eventDispatcher.endElement("confidence", -1);
      }
      else throw new Asn1MissingRequiredException ("confidence");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
