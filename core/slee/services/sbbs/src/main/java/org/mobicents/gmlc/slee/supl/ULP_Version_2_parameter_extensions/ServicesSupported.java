/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.ULP_Version_2_parameter_extensions;

import com.objsys.asn1j.runtime.*;

public class ServicesSupported extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_ULP_Version_2_parameter_extensionsRtkey._rtkey);
      Asn1Type._setLicLocation(_ULP_Version_2_parameter_extensionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "ServicesSupported";
   }

   protected Asn1Boolean periodicTrigger;
   protected Asn1Boolean areaEventTrigger;
   protected Asn1OpenExt extElem1;

   public ServicesSupported () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public ServicesSupported (
      Asn1Boolean periodicTrigger_,
      Asn1Boolean areaEventTrigger_
   ) {
      super();
      setPeriodicTrigger (periodicTrigger_);
      setAreaEventTrigger (areaEventTrigger_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public ServicesSupported (boolean periodicTrigger_,
      boolean areaEventTrigger_
   ) {
      super();
      setPeriodicTrigger (periodicTrigger_);
      setAreaEventTrigger (areaEventTrigger_);
   }

   public void init () {
      periodicTrigger = null;
      areaEventTrigger = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof ServicesSupported) ) return false;

      ServicesSupported rhs = (ServicesSupported) obj;

      if (periodicTrigger == null) {
         if (rhs.periodicTrigger != null) return false;
      }
      else {
         if (!periodicTrigger.equals(rhs.periodicTrigger)) {
            return false;
         }
      }

      if (areaEventTrigger == null) {
         if (rhs.areaEventTrigger != null) return false;
      }
      else {
         if (!areaEventTrigger.equals(rhs.areaEventTrigger)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (periodicTrigger != null) __code = 31*__code + periodicTrigger.hashCode();
      if (areaEventTrigger != null) __code = 31*__code + areaEventTrigger.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for PeriodicTrigger
    */
   public Asn1Boolean getPeriodicTrigger () {
      return periodicTrigger;
   }

   public void setPeriodicTrigger (Asn1Boolean value) {
      this.periodicTrigger = value;
   }

   public void setPeriodicTrigger (boolean value) {
      setPeriodicTrigger (new Asn1Boolean(value));
   }

   /**
    * Accessor/mutator methods for AreaEventTrigger
    */
   public Asn1Boolean getAreaEventTrigger () {
      return areaEventTrigger;
   }

   public void setAreaEventTrigger (Asn1Boolean value) {
      this.areaEventTrigger = value;
   }

   public void setAreaEventTrigger (boolean value) {
      setAreaEventTrigger (new Asn1Boolean(value));
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return periodicTrigger;
         case 1: return areaEventTrigger;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "periodicTrigger";
         case 1: return "areaEventTrigger";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode periodicTrigger

      buffer.getContext().eventDispatcher.startElement("periodicTrigger", -1);

      periodicTrigger = new Asn1Boolean();
      periodicTrigger.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("periodicTrigger", -1);

      // decode areaEventTrigger

      buffer.getContext().eventDispatcher.startElement("areaEventTrigger", -1);

      areaEventTrigger = new Asn1Boolean();
      areaEventTrigger.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("areaEventTrigger", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode periodicTrigger

      if (periodicTrigger != null) {
         buffer.getContext().eventDispatcher.startElement("periodicTrigger", -1);

         periodicTrigger.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("periodicTrigger", -1);
      }
      else throw new Asn1MissingRequiredException ("periodicTrigger");

      // encode areaEventTrigger

      if (areaEventTrigger != null) {
         buffer.getContext().eventDispatcher.startElement("areaEventTrigger", -1);

         areaEventTrigger.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("areaEventTrigger", -1);
      }
      else throw new Asn1MissingRequiredException ("areaEventTrigger");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
