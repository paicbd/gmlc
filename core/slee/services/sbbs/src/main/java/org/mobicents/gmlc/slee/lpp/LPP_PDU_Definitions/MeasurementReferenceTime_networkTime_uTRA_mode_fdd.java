/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class MeasurementReferenceTime_networkTime_uTRA_mode_fdd extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "SEQUENCE";
   }

   protected Asn1Integer primary_CPICH_Info;
   protected Asn1OpenExt extElem1;

   public MeasurementReferenceTime_networkTime_uTRA_mode_fdd () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public MeasurementReferenceTime_networkTime_uTRA_mode_fdd (
      Asn1Integer primary_CPICH_Info_
   ) throws Asn1Exception {
      super();
      setPrimary_CPICH_Info (primary_CPICH_Info_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public MeasurementReferenceTime_networkTime_uTRA_mode_fdd (long primary_CPICH_Info_
   ) throws Asn1Exception {
      super();
      setPrimary_CPICH_Info (primary_CPICH_Info_);
   }

   public void init () {
      primary_CPICH_Info = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof MeasurementReferenceTime_networkTime_uTRA_mode_fdd) ) return false;

      MeasurementReferenceTime_networkTime_uTRA_mode_fdd rhs = (MeasurementReferenceTime_networkTime_uTRA_mode_fdd) obj;

      if (primary_CPICH_Info == null) {
         if (rhs.primary_CPICH_Info != null) return false;
      }
      else {
         if (!primary_CPICH_Info.equals(rhs.primary_CPICH_Info)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (primary_CPICH_Info != null) __code = 31*__code + primary_CPICH_Info.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Primary_CPICH_Info
    */
   public Asn1Integer getPrimary_CPICH_Info () {
      return primary_CPICH_Info;
   }

   public void setPrimary_CPICH_Info (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 511))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.primary_CPICH_Info = value;
   }

   public void setPrimary_CPICH_Info (long value) 
      throws Asn1Exception
   {
      setPrimary_CPICH_Info (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 2; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return primary_CPICH_Info;
         case 1: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "primary-CPICH-Info";
         case 1: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode primary_CPICH_Info

      buffer.getContext().eventDispatcher.startElement("primary_CPICH_Info", -1);

      primary_CPICH_Info = new Asn1Integer();
      primary_CPICH_Info.decode (buffer, 0, 511);
      buffer.getContext().eventDispatcher.endElement("primary_CPICH_Info", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode primary_CPICH_Info

      if (primary_CPICH_Info != null) {
         buffer.getContext().eventDispatcher.startElement("primary_CPICH_Info", -1);

         primary_CPICH_Info.encode (buffer, 0, 511);

         buffer.getContext().eventDispatcher.endElement("primary_CPICH_Info", -1);
      }
      else throw new Asn1MissingRequiredException ("primary_CPICH_Info");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
