/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.ULP_Components;

import com.objsys.asn1j.runtime.*;

public class Horandvervel extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_ULP_ComponentsRtkey._rtkey);
      Asn1Type._setLicLocation(_ULP_ComponentsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "Horandvervel";
   }

   protected Asn1BitString verdirect;
   protected Asn1BitString bearing;
   protected Asn1BitString horspeed;
   protected Asn1BitString verspeed;
   protected Asn1OpenExt extElem1;

   public Horandvervel () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public Horandvervel (
      Asn1BitString verdirect_,
      Asn1BitString bearing_,
      Asn1BitString horspeed_,
      Asn1BitString verspeed_
   ) throws Asn1Exception {
      super();
      setVerdirect (verdirect_);
      setBearing (bearing_);
      setHorspeed (horspeed_);
      setVerspeed (verspeed_);
   }

   public void init () {
      verdirect = null;
      bearing = null;
      horspeed = null;
      verspeed = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof Horandvervel) ) return false;

      Horandvervel rhs = (Horandvervel) obj;

      if (verdirect == null) {
         if (rhs.verdirect != null) return false;
      }
      else {
         if (!verdirect.equals(rhs.verdirect)) {
            return false;
         }
      }

      if (bearing == null) {
         if (rhs.bearing != null) return false;
      }
      else {
         if (!bearing.equals(rhs.bearing)) {
            return false;
         }
      }

      if (horspeed == null) {
         if (rhs.horspeed != null) return false;
      }
      else {
         if (!horspeed.equals(rhs.horspeed)) {
            return false;
         }
      }

      if (verspeed == null) {
         if (rhs.verspeed != null) return false;
      }
      else {
         if (!verspeed.equals(rhs.verspeed)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (verdirect != null) __code = 31*__code + verdirect.hashCode();
      if (bearing != null) __code = 31*__code + bearing.hashCode();
      if (horspeed != null) __code = 31*__code + horspeed.hashCode();
      if (verspeed != null) __code = 31*__code + verspeed.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Verdirect
    */
   public Asn1BitString getVerdirect () {
      return verdirect;
   }

   public void setVerdirect (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 1)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.verdirect = value;
   }

   /**
    * Accessor/mutator methods for Bearing
    */
   public Asn1BitString getBearing () {
      return bearing;
   }

   public void setBearing (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 9)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.bearing = value;
   }

   /**
    * Accessor/mutator methods for Horspeed
    */
   public Asn1BitString getHorspeed () {
      return horspeed;
   }

   public void setHorspeed (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 16)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.horspeed = value;
   }

   /**
    * Accessor/mutator methods for Verspeed
    */
   public Asn1BitString getVerspeed () {
      return verspeed;
   }

   public void setVerspeed (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 8)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.verspeed = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 5; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return verdirect;
         case 1: return bearing;
         case 2: return horspeed;
         case 3: return verspeed;
         case 4: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "verdirect";
         case 1: return "bearing";
         case 2: return "horspeed";
         case 3: return "verspeed";
         case 4: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode verdirect

      buffer.getContext().eventDispatcher.startElement("verdirect", -1);

      verdirect = new Asn1BitString();
      verdirect.decode (buffer, 1, 1);
      buffer.getContext().eventDispatcher.endElement("verdirect", -1);

      // decode bearing

      buffer.getContext().eventDispatcher.startElement("bearing", -1);

      bearing = new Asn1BitString();
      bearing.decode (buffer, 9, 9);
      buffer.getContext().eventDispatcher.endElement("bearing", -1);

      // decode horspeed

      buffer.getContext().eventDispatcher.startElement("horspeed", -1);

      horspeed = new Asn1BitString();
      horspeed.decode (buffer, 16, 16);
      buffer.getContext().eventDispatcher.endElement("horspeed", -1);

      // decode verspeed

      buffer.getContext().eventDispatcher.startElement("verspeed", -1);

      verspeed = new Asn1BitString();
      verspeed.decode (buffer, 8, 8);
      buffer.getContext().eventDispatcher.endElement("verspeed", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode verdirect

      if (verdirect != null) {
         buffer.getContext().eventDispatcher.startElement("verdirect", -1);

         verdirect.encode (buffer, 1, 1);

         buffer.getContext().eventDispatcher.endElement("verdirect", -1);
      }
      else throw new Asn1MissingRequiredException ("verdirect");

      // encode bearing

      if (bearing != null) {
         buffer.getContext().eventDispatcher.startElement("bearing", -1);

         bearing.encode (buffer, 9, 9);

         buffer.getContext().eventDispatcher.endElement("bearing", -1);
      }
      else throw new Asn1MissingRequiredException ("bearing");

      // encode horspeed

      if (horspeed != null) {
         buffer.getContext().eventDispatcher.startElement("horspeed", -1);

         horspeed.encode (buffer, 16, 16);

         buffer.getContext().eventDispatcher.endElement("horspeed", -1);
      }
      else throw new Asn1MissingRequiredException ("horspeed");

      // encode verspeed

      if (verspeed != null) {
         buffer.getContext().eventDispatcher.startElement("verspeed", -1);

         verspeed.encode (buffer, 8, 8);

         buffer.getContext().eventDispatcher.endElement("verspeed", -1);
      }
      else throw new Asn1MissingRequiredException ("verspeed");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
