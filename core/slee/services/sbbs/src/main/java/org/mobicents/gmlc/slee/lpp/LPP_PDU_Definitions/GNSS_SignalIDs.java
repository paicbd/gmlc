/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class GNSS_SignalIDs extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "GNSS-SignalIDs";
   }

   protected Asn1BitString gnss_SignalIDs;
   protected Asn1BitString gnss_SignalIDs_Ext_r15;  // optional
   protected Asn1OpenExt extElem1;

   public GNSS_SignalIDs () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public GNSS_SignalIDs (
      Asn1BitString gnss_SignalIDs_,
      Asn1BitString gnss_SignalIDs_Ext_r15_
   ) throws Asn1Exception {
      super();
      setGnss_SignalIDs (gnss_SignalIDs_);
      setGnss_SignalIDs_Ext_r15 (gnss_SignalIDs_Ext_r15_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public GNSS_SignalIDs (
      Asn1BitString gnss_SignalIDs_
   ) throws Asn1Exception {
      super();
      setGnss_SignalIDs (gnss_SignalIDs_);
   }

   public void init () {
      gnss_SignalIDs = null;
      gnss_SignalIDs_Ext_r15 = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof GNSS_SignalIDs) ) return false;

      GNSS_SignalIDs rhs = (GNSS_SignalIDs) obj;

      if (gnss_SignalIDs == null) {
         if (rhs.gnss_SignalIDs != null) return false;
      }
      else {
         if (!gnss_SignalIDs.equals(rhs.gnss_SignalIDs)) {
            return false;
         }
      }

      if (gnss_SignalIDs_Ext_r15 == null) {
         if (rhs.gnss_SignalIDs_Ext_r15 != null) return false;
      }
      else {
         if (!gnss_SignalIDs_Ext_r15.equals(rhs.gnss_SignalIDs_Ext_r15)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (gnss_SignalIDs != null) __code = 31*__code + gnss_SignalIDs.hashCode();
      if (gnss_SignalIDs_Ext_r15 != null) __code = 31*__code + gnss_SignalIDs_Ext_r15.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Gnss_SignalIDs
    */
   public Asn1BitString getGnss_SignalIDs () {
      return gnss_SignalIDs;
   }

   public void setGnss_SignalIDs (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 8)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.gnss_SignalIDs = value;
   }

   /**
    * Accessor/mutator methods for Gnss_SignalIDs_Ext_r15
    */
   public Asn1BitString getGnss_SignalIDs_Ext_r15 () {
      return gnss_SignalIDs_Ext_r15;
   }

   public void setGnss_SignalIDs_Ext_r15 (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 16)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.gnss_SignalIDs_Ext_r15 = value;
   }

   public boolean hasGnss_SignalIDs_Ext_r15 () {
      return (gnss_SignalIDs_Ext_r15 != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return gnss_SignalIDs;
         case 1: return gnss_SignalIDs_Ext_r15;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "gnss-SignalIDs";
         case 1: return "gnss-SignalIDs-Ext-r15";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      // decode gnss_SignalIDs

      buffer.getContext().eventDispatcher.startElement("gnss_SignalIDs", -1);

      gnss_SignalIDs = new Asn1BitString();
      gnss_SignalIDs.decode (buffer, 8, 8);
      buffer.getContext().eventDispatcher.endElement("gnss_SignalIDs", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("gnss_SignalIDs_Ext_r15Present");

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode gnss_SignalIDs_Ext_r15

         if (i < bitcnt && bitmap[i++]) {
            buffer.getContext().eventDispatcher.startElement("gnss_SignalIDs_Ext_r15", -1);

            long _curidx = buffer.getBitOffset();
            bitidx = buffer.decodeExtLength ();
            if (bitidx - _curidx >= 131088)  {
               throw new Asn1Exception ("PER fragmentation not enabled: add -perindef to decode");
            }

            gnss_SignalIDs_Ext_r15 = new Asn1BitString();
            gnss_SignalIDs_Ext_r15.decode (buffer, 16, 16);

            buffer.moveBitCursor (bitidx);
            buffer.getContext().eventDispatcher.endElement("gnss_SignalIDs_Ext_r15", -1);

         }
         else {
            gnss_SignalIDs_Ext_r15 = null;
         }

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = ((gnss_SignalIDs_Ext_r15 != null) ||
      ((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode gnss_SignalIDs

      if (gnss_SignalIDs != null) {
         buffer.getContext().eventDispatcher.startElement("gnss_SignalIDs", -1);

         gnss_SignalIDs.encode (buffer, 8, 8);

         buffer.getContext().eventDispatcher.endElement("gnss_SignalIDs", -1);
      }
      else throw new Asn1MissingRequiredException ("gnss_SignalIDs");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 1;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         buffer.encodeBit ((gnss_SignalIDs_Ext_r15 != null), null);

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         Asn1PerEncodeBuffer mainBuffer = buffer;
         buffer = new Asn1PerEncodeBuffer (buffer.isAligned());

         // gnss_SignalIDs_Ext_r15

         if (gnss_SignalIDs_Ext_r15 != null) {
            buffer.reset();
            gnss_SignalIDs_Ext_r15.encode (buffer, 16, 16);
            buffer.getContext().eventDispatcher.startElement("gnss_SignalIDs_Ext_r15", -1);

            mainBuffer.encodeOpenType (buffer, null);

            buffer.getContext().eventDispatcher.endElement("gnss_SignalIDs_Ext_r15", -1);
         }

         buffer = mainBuffer;

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
