/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.Ver2_ULP_Components;

import com.objsys.asn1j.runtime.*;

public class SupportedWLANApData extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_Ver2_ULP_ComponentsRtkey._rtkey);
      Asn1Type._setLicLocation(_Ver2_ULP_ComponentsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "SupportedWLANApData";
   }

   protected Asn1BitString apMACAddress;
   protected SupportedWLANApData_apDevType apDevType = null;
   protected Asn1OpenExt extElem1;

   public SupportedWLANApData () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public SupportedWLANApData (
      Asn1BitString apMACAddress_,
      SupportedWLANApData_apDevType apDevType_
   ) throws Asn1Exception {
      super();
      setApMACAddress (apMACAddress_);
      setApDevType (apDevType_);
   }

   public void init () {
      apMACAddress = null;
      apDevType = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof SupportedWLANApData) ) return false;

      SupportedWLANApData rhs = (SupportedWLANApData) obj;

      if (apMACAddress == null) {
         if (rhs.apMACAddress != null) return false;
      }
      else {
         if (!apMACAddress.equals(rhs.apMACAddress)) {
            return false;
         }
      }

      if (apDevType == null) {
         if (rhs.apDevType != null) return false;
      }
      else {
         if (!apDevType.equals(rhs.apDevType)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (apMACAddress != null) __code = 31*__code + apMACAddress.hashCode();
      if (apDevType != null) __code = 31*__code + apDevType.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for ApMACAddress
    */
   public Asn1BitString getApMACAddress () {
      return apMACAddress;
   }

   public void setApMACAddress (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 48)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.apMACAddress = value;
   }

   /**
    * Accessor/mutator methods for ApDevType
    */
   public SupportedWLANApData_apDevType getApDevType () {
      return apDevType;
   }

   public void setApDevType (SupportedWLANApData_apDevType value) {
      this.apDevType = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return apMACAddress;
         case 1: return apDevType;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "apMACAddress";
         case 1: return "apDevType";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode apMACAddress

      buffer.getContext().eventDispatcher.startElement("apMACAddress", -1);

      apMACAddress = new Asn1BitString();
      apMACAddress.decode (buffer, 48, 48);
      buffer.getContext().eventDispatcher.endElement("apMACAddress", -1);

      // decode apDevType

      buffer.getContext().eventDispatcher.startElement("apDevType", -1);

      {
         int tval = SupportedWLANApData_apDevType.decodeEnumValue (buffer);
         apDevType = SupportedWLANApData_apDevType.valueOf (tval);
      }
      buffer.getContext().eventDispatcher.endElement("apDevType", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode apMACAddress

      if (apMACAddress != null) {
         buffer.getContext().eventDispatcher.startElement("apMACAddress", -1);

         apMACAddress.encode (buffer, 48, 48);

         buffer.getContext().eventDispatcher.endElement("apMACAddress", -1);
      }
      else throw new Asn1MissingRequiredException ("apMACAddress");

      // encode apDevType

      if (apDevType != null) {
         buffer.getContext().eventDispatcher.startElement("apDevType", -1);

         apDevType.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("apDevType", -1);
      }
      else throw new Asn1MissingRequiredException ("apDevType");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
