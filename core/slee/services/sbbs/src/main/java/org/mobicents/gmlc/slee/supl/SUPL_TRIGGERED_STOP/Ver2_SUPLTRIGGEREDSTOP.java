/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.SUPL_TRIGGERED_STOP;

import com.objsys.asn1j.runtime.*;
import org.mobicents.gmlc.slee.supl.ULP_Components.StatusCode;

public class Ver2_SUPLTRIGGEREDSTOP extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_SUPL_TRIGGERED_STOPRtkey._rtkey);
      Asn1Type._setLicLocation(_SUPL_TRIGGERED_STOPRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "Ver2-SUPLTRIGGEREDSTOP";
   }

   protected StatusCode statusCode = null;  // optional
   protected Asn1OpenExt extElem1;

   public Ver2_SUPLTRIGGEREDSTOP () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public Ver2_SUPLTRIGGEREDSTOP (
      StatusCode statusCode_
   ) {
      super();
      setStatusCode (statusCode_);
   }

   public void init () {
      statusCode = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof Ver2_SUPLTRIGGEREDSTOP) ) return false;

      Ver2_SUPLTRIGGEREDSTOP rhs = (Ver2_SUPLTRIGGEREDSTOP) obj;

      if (statusCode == null) {
         if (rhs.statusCode != null) return false;
      }
      else {
         if (!statusCode.equals(rhs.statusCode)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (statusCode != null) __code = 31*__code + statusCode.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for StatusCode
    */
   public StatusCode getStatusCode () {
      return statusCode;
   }

   public void setStatusCode (StatusCode value) {
      this.statusCode = value;
   }

   public boolean hasStatusCode () {
      return (statusCode != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 2; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return statusCode;
         case 1: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "statusCode";
         case 1: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean statusCodePresent = buffer.decodeBit ("statusCodePresent");

      // decode statusCode

      if (statusCodePresent) {
         buffer.getContext().eventDispatcher.startElement("statusCode", -1);

         int tval = StatusCode.decodeEnumValue (buffer);
         statusCode = StatusCode.valueOf (tval);
         buffer.getContext().eventDispatcher.endElement("statusCode", -1);
      }
      else {
         statusCode = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((statusCode != null), null);

      // encode statusCode

      if (statusCode != null) {
         buffer.getContext().eventDispatcher.startElement("statusCode", -1);

         statusCode.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("statusCode", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
