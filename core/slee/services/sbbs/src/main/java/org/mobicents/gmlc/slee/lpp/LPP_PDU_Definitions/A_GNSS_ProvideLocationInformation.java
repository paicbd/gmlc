/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class A_GNSS_ProvideLocationInformation extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "A-GNSS-ProvideLocationInformation";
   }

   protected GNSS_SignalMeasurementInformation gnss_SignalMeasurementInformation;  // optional
   protected GNSS_LocationInformation gnss_LocationInformation;  // optional
   protected A_GNSS_Error gnss_Error;  // optional
   protected Asn1OpenExt extElem1;

   public A_GNSS_ProvideLocationInformation () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public A_GNSS_ProvideLocationInformation (
      GNSS_SignalMeasurementInformation gnss_SignalMeasurementInformation_,
      GNSS_LocationInformation gnss_LocationInformation_,
      A_GNSS_Error gnss_Error_
   ) {
      super();
      setGnss_SignalMeasurementInformation (gnss_SignalMeasurementInformation_);
      setGnss_LocationInformation (gnss_LocationInformation_);
      setGnss_Error (gnss_Error_);
   }

   public void init () {
      gnss_SignalMeasurementInformation = null;
      gnss_LocationInformation = null;
      gnss_Error = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof A_GNSS_ProvideLocationInformation) ) return false;

      A_GNSS_ProvideLocationInformation rhs = (A_GNSS_ProvideLocationInformation) obj;

      if (gnss_SignalMeasurementInformation == null) {
         if (rhs.gnss_SignalMeasurementInformation != null) return false;
      }
      else {
         if (!gnss_SignalMeasurementInformation.equals(rhs.gnss_SignalMeasurementInformation)) {
            return false;
         }
      }

      if (gnss_LocationInformation == null) {
         if (rhs.gnss_LocationInformation != null) return false;
      }
      else {
         if (!gnss_LocationInformation.equals(rhs.gnss_LocationInformation)) {
            return false;
         }
      }

      if (gnss_Error == null) {
         if (rhs.gnss_Error != null) return false;
      }
      else {
         if (!gnss_Error.equals(rhs.gnss_Error)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (gnss_SignalMeasurementInformation != null) __code = 31*__code + gnss_SignalMeasurementInformation.hashCode();
      if (gnss_LocationInformation != null) __code = 31*__code + gnss_LocationInformation.hashCode();
      if (gnss_Error != null) __code = 31*__code + gnss_Error.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Gnss_SignalMeasurementInformation
    */
   public GNSS_SignalMeasurementInformation getGnss_SignalMeasurementInformation () {
      return gnss_SignalMeasurementInformation;
   }

   public void setGnss_SignalMeasurementInformation (GNSS_SignalMeasurementInformation value) {
      this.gnss_SignalMeasurementInformation = value;
   }

   public boolean hasGnss_SignalMeasurementInformation () {
      return (gnss_SignalMeasurementInformation != null);
   }

   /**
    * Accessor/mutator methods for Gnss_LocationInformation
    */
   public GNSS_LocationInformation getGnss_LocationInformation () {
      return gnss_LocationInformation;
   }

   public void setGnss_LocationInformation (GNSS_LocationInformation value) {
      this.gnss_LocationInformation = value;
   }

   public boolean hasGnss_LocationInformation () {
      return (gnss_LocationInformation != null);
   }

   /**
    * Accessor/mutator methods for Gnss_Error
    */
   public A_GNSS_Error getGnss_Error () {
      return gnss_Error;
   }

   public void setGnss_Error (A_GNSS_Error value) {
      this.gnss_Error = value;
   }

   public boolean hasGnss_Error () {
      return (gnss_Error != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 4; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return gnss_SignalMeasurementInformation;
         case 1: return gnss_LocationInformation;
         case 2: return gnss_Error;
         case 3: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "gnss-SignalMeasurementInformation";
         case 1: return "gnss-LocationInformation";
         case 2: return "gnss-Error";
         case 3: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean gnss_SignalMeasurementInformationPresent = buffer.decodeBit ("gnss_SignalMeasurementInformationPresent");
      boolean gnss_LocationInformationPresent = buffer.decodeBit ("gnss_LocationInformationPresent");
      boolean gnss_ErrorPresent = buffer.decodeBit ("gnss_ErrorPresent");

      // decode gnss_SignalMeasurementInformation

      if (gnss_SignalMeasurementInformationPresent) {
         buffer.getContext().eventDispatcher.startElement("gnss_SignalMeasurementInformation", -1);

         gnss_SignalMeasurementInformation = new GNSS_SignalMeasurementInformation();
         gnss_SignalMeasurementInformation.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("gnss_SignalMeasurementInformation", -1);
      }
      else {
         gnss_SignalMeasurementInformation = null;
      }

      // decode gnss_LocationInformation

      if (gnss_LocationInformationPresent) {
         buffer.getContext().eventDispatcher.startElement("gnss_LocationInformation", -1);

         gnss_LocationInformation = new GNSS_LocationInformation();
         gnss_LocationInformation.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("gnss_LocationInformation", -1);
      }
      else {
         gnss_LocationInformation = null;
      }

      // decode gnss_Error

      if (gnss_ErrorPresent) {
         buffer.getContext().eventDispatcher.startElement("gnss_Error", -1);

         gnss_Error = new A_GNSS_Error();
         gnss_Error.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("gnss_Error", -1);
      }
      else {
         gnss_Error = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((gnss_SignalMeasurementInformation != null), null);
      buffer.encodeBit ((gnss_LocationInformation != null), null);
      buffer.encodeBit ((gnss_Error != null), null);

      // encode gnss_SignalMeasurementInformation

      if (gnss_SignalMeasurementInformation != null) {
         buffer.getContext().eventDispatcher.startElement("gnss_SignalMeasurementInformation", -1);

         gnss_SignalMeasurementInformation.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("gnss_SignalMeasurementInformation", -1);
      }

      // encode gnss_LocationInformation

      if (gnss_LocationInformation != null) {
         buffer.getContext().eventDispatcher.startElement("gnss_LocationInformation", -1);

         gnss_LocationInformation.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("gnss_LocationInformation", -1);
      }

      // encode gnss_Error

      if (gnss_Error != null) {
         buffer.getContext().eventDispatcher.startElement("gnss_Error", -1);

         gnss_Error.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("gnss_Error", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
