/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class GNSS_NavigationModel extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "GNSS-NavigationModel";
   }

   protected Asn1Integer nonBroadcastIndFlag;
   protected GNSS_NavModelSatelliteList gnss_SatelliteList;
   protected Asn1OpenExt extElem1;

   public GNSS_NavigationModel () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public GNSS_NavigationModel (
      Asn1Integer nonBroadcastIndFlag_,
      GNSS_NavModelSatelliteList gnss_SatelliteList_
   ) throws Asn1Exception {
      super();
      setNonBroadcastIndFlag (nonBroadcastIndFlag_);
      setGnss_SatelliteList (gnss_SatelliteList_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public GNSS_NavigationModel (long nonBroadcastIndFlag_,
      GNSS_NavModelSatelliteList gnss_SatelliteList_
   ) throws Asn1Exception {
      super();
      setNonBroadcastIndFlag (nonBroadcastIndFlag_);
      setGnss_SatelliteList (gnss_SatelliteList_);
   }

   public void init () {
      nonBroadcastIndFlag = null;
      gnss_SatelliteList = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof GNSS_NavigationModel) ) return false;

      GNSS_NavigationModel rhs = (GNSS_NavigationModel) obj;

      if (nonBroadcastIndFlag == null) {
         if (rhs.nonBroadcastIndFlag != null) return false;
      }
      else {
         if (!nonBroadcastIndFlag.equals(rhs.nonBroadcastIndFlag)) {
            return false;
         }
      }

      if (gnss_SatelliteList == null) {
         if (rhs.gnss_SatelliteList != null) return false;
      }
      else {
         if (!gnss_SatelliteList.equals(rhs.gnss_SatelliteList)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (nonBroadcastIndFlag != null) __code = 31*__code + nonBroadcastIndFlag.hashCode();
      if (gnss_SatelliteList != null) __code = 31*__code + gnss_SatelliteList.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for NonBroadcastIndFlag
    */
   public Asn1Integer getNonBroadcastIndFlag () {
      return nonBroadcastIndFlag;
   }

   public void setNonBroadcastIndFlag (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 1))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.nonBroadcastIndFlag = value;
   }

   public void setNonBroadcastIndFlag (long value) 
      throws Asn1Exception
   {
      setNonBroadcastIndFlag (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for Gnss_SatelliteList
    */
   public GNSS_NavModelSatelliteList getGnss_SatelliteList () {
      return gnss_SatelliteList;
   }

   public void setGnss_SatelliteList (GNSS_NavModelSatelliteList value) 
      throws Asn1Exception
   {
      if (!((value.getElements().length >= 1 && value.getElements().length <= 64))) {
         throw new Asn1ConsVioException ("value.getElements().length", value.getElements().length);
      }

      this.gnss_SatelliteList = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return nonBroadcastIndFlag;
         case 1: return gnss_SatelliteList;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "nonBroadcastIndFlag";
         case 1: return "gnss-SatelliteList";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode nonBroadcastIndFlag

      buffer.getContext().eventDispatcher.startElement("nonBroadcastIndFlag", -1);

      nonBroadcastIndFlag = new Asn1Integer();
      nonBroadcastIndFlag.decode (buffer, 0, 1);
      buffer.getContext().eventDispatcher.endElement("nonBroadcastIndFlag", -1);

      // decode gnss_SatelliteList

      buffer.getContext().eventDispatcher.startElement("gnss_SatelliteList", -1);

      gnss_SatelliteList = new GNSS_NavModelSatelliteList();
      gnss_SatelliteList.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("gnss_SatelliteList", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode nonBroadcastIndFlag

      if (nonBroadcastIndFlag != null) {
         buffer.getContext().eventDispatcher.startElement("nonBroadcastIndFlag", -1);

         nonBroadcastIndFlag.encode (buffer, 0, 1);

         buffer.getContext().eventDispatcher.endElement("nonBroadcastIndFlag", -1);
      }
      else throw new Asn1MissingRequiredException ("nonBroadcastIndFlag");

      // encode gnss_SatelliteList

      if (gnss_SatelliteList != null) {
         buffer.getContext().eventDispatcher.startElement("gnss_SatelliteList", -1);

         gnss_SatelliteList.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("gnss_SatelliteList", -1);
      }
      else throw new Asn1MissingRequiredException ("gnss_SatelliteList");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
