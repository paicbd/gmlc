/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.ULP_Version_2_parameter_extensions;

import com.objsys.asn1j.runtime.*;

public class GeoAreaShapesSupported extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_ULP_Version_2_parameter_extensionsRtkey._rtkey);
      Asn1Type._setLicLocation(_ULP_Version_2_parameter_extensionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "GeoAreaShapesSupported";
   }

   protected Asn1Boolean ellipticalArea;
   protected Asn1Boolean polygonArea;
   protected Asn1OpenExt extElem1;

   public GeoAreaShapesSupported () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public GeoAreaShapesSupported (
      Asn1Boolean ellipticalArea_,
      Asn1Boolean polygonArea_
   ) {
      super();
      setEllipticalArea (ellipticalArea_);
      setPolygonArea (polygonArea_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public GeoAreaShapesSupported (boolean ellipticalArea_,
      boolean polygonArea_
   ) {
      super();
      setEllipticalArea (ellipticalArea_);
      setPolygonArea (polygonArea_);
   }

   public void init () {
      ellipticalArea = null;
      polygonArea = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof GeoAreaShapesSupported) ) return false;

      GeoAreaShapesSupported rhs = (GeoAreaShapesSupported) obj;

      if (ellipticalArea == null) {
         if (rhs.ellipticalArea != null) return false;
      }
      else {
         if (!ellipticalArea.equals(rhs.ellipticalArea)) {
            return false;
         }
      }

      if (polygonArea == null) {
         if (rhs.polygonArea != null) return false;
      }
      else {
         if (!polygonArea.equals(rhs.polygonArea)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (ellipticalArea != null) __code = 31*__code + ellipticalArea.hashCode();
      if (polygonArea != null) __code = 31*__code + polygonArea.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for EllipticalArea
    */
   public Asn1Boolean getEllipticalArea () {
      return ellipticalArea;
   }

   public void setEllipticalArea (Asn1Boolean value) {
      this.ellipticalArea = value;
   }

   public void setEllipticalArea (boolean value) {
      setEllipticalArea (new Asn1Boolean(value));
   }

   /**
    * Accessor/mutator methods for PolygonArea
    */
   public Asn1Boolean getPolygonArea () {
      return polygonArea;
   }

   public void setPolygonArea (Asn1Boolean value) {
      this.polygonArea = value;
   }

   public void setPolygonArea (boolean value) {
      setPolygonArea (new Asn1Boolean(value));
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return ellipticalArea;
         case 1: return polygonArea;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "ellipticalArea";
         case 1: return "polygonArea";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode ellipticalArea

      buffer.getContext().eventDispatcher.startElement("ellipticalArea", -1);

      ellipticalArea = new Asn1Boolean();
      ellipticalArea.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("ellipticalArea", -1);

      // decode polygonArea

      buffer.getContext().eventDispatcher.startElement("polygonArea", -1);

      polygonArea = new Asn1Boolean();
      polygonArea.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("polygonArea", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode ellipticalArea

      if (ellipticalArea != null) {
         buffer.getContext().eventDispatcher.startElement("ellipticalArea", -1);

         ellipticalArea.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("ellipticalArea", -1);
      }
      else throw new Asn1MissingRequiredException ("ellipticalArea");

      // encode polygonArea

      if (polygonArea != null) {
         buffer.getContext().eventDispatcher.startElement("polygonArea", -1);

         polygonArea.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("polygonArea", -1);
      }
      else throw new Asn1MissingRequiredException ("polygonArea");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
