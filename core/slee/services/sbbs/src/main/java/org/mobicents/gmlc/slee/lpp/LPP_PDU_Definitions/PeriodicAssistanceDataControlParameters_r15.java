/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class PeriodicAssistanceDataControlParameters_r15 extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "PeriodicAssistanceDataControlParameters-r15";
   }

   protected PeriodicSessionID_r15 periodicSessionID_r15;
   protected UpdateCapabilities_r15 updateCapabilities_r15;  // optional
   protected Asn1OpenExt extElem1;

   public PeriodicAssistanceDataControlParameters_r15 () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public PeriodicAssistanceDataControlParameters_r15 (
      PeriodicSessionID_r15 periodicSessionID_r15_,
      UpdateCapabilities_r15 updateCapabilities_r15_
   ) throws Asn1Exception {
      super();
      setPeriodicSessionID_r15 (periodicSessionID_r15_);
      setUpdateCapabilities_r15 (updateCapabilities_r15_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public PeriodicAssistanceDataControlParameters_r15 (
      PeriodicSessionID_r15 periodicSessionID_r15_
   ) throws Asn1Exception {
      super();
      setPeriodicSessionID_r15 (periodicSessionID_r15_);
   }

   public void init () {
      periodicSessionID_r15 = null;
      updateCapabilities_r15 = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof PeriodicAssistanceDataControlParameters_r15) ) return false;

      PeriodicAssistanceDataControlParameters_r15 rhs = (PeriodicAssistanceDataControlParameters_r15) obj;

      if (periodicSessionID_r15 == null) {
         if (rhs.periodicSessionID_r15 != null) return false;
      }
      else {
         if (!periodicSessionID_r15.equals(rhs.periodicSessionID_r15)) {
            return false;
         }
      }

      if (updateCapabilities_r15 == null) {
         if (rhs.updateCapabilities_r15 != null) return false;
      }
      else {
         if (!updateCapabilities_r15.equals(rhs.updateCapabilities_r15)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (periodicSessionID_r15 != null) __code = 31*__code + periodicSessionID_r15.hashCode();
      if (updateCapabilities_r15 != null) __code = 31*__code + updateCapabilities_r15.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for PeriodicSessionID_r15
    */
   public PeriodicSessionID_r15 getPeriodicSessionID_r15 () {
      return periodicSessionID_r15;
   }

   public void setPeriodicSessionID_r15 (PeriodicSessionID_r15 value) {
      this.periodicSessionID_r15 = value;
   }

   /**
    * Accessor/mutator methods for UpdateCapabilities_r15
    */
   public UpdateCapabilities_r15 getUpdateCapabilities_r15 () {
      return updateCapabilities_r15;
   }

   public void setUpdateCapabilities_r15 (UpdateCapabilities_r15 value) 
      throws Asn1Exception
   {
      if (!((value.getLength() >= 1 && value.getLength() <= 8))) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.updateCapabilities_r15 = value;
   }

   public boolean hasUpdateCapabilities_r15 () {
      return (updateCapabilities_r15 != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return periodicSessionID_r15;
         case 1: return updateCapabilities_r15;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "periodicSessionID-r15";
         case 1: return "updateCapabilities-r15";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      // decode periodicSessionID_r15

      buffer.getContext().eventDispatcher.startElement("periodicSessionID_r15", -1);

      periodicSessionID_r15 = new PeriodicSessionID_r15();
      periodicSessionID_r15.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("periodicSessionID_r15", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("updateCapabilities_r15Present");

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode updateCapabilities_r15

         if (i < bitcnt && bitmap[i++]) {
            buffer.getContext().eventDispatcher.startElement("updateCapabilities_r15", -1);

            long _curidx = buffer.getBitOffset();
            bitidx = buffer.decodeExtLength ();
            if (bitidx - _curidx >= 131088)  {
               throw new Asn1Exception ("PER fragmentation not enabled: add -perindef to decode");
            }

            updateCapabilities_r15 = new UpdateCapabilities_r15();
            updateCapabilities_r15.decode (buffer);

            buffer.moveBitCursor (bitidx);
            buffer.getContext().eventDispatcher.endElement("updateCapabilities_r15", -1);

         }
         else {
            updateCapabilities_r15 = null;
         }

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = ((updateCapabilities_r15 != null) ||
      ((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode periodicSessionID_r15

      if (periodicSessionID_r15 != null) {
         buffer.getContext().eventDispatcher.startElement("periodicSessionID_r15", -1);

         periodicSessionID_r15.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("periodicSessionID_r15", -1);
      }
      else throw new Asn1MissingRequiredException ("periodicSessionID_r15");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 1;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         buffer.encodeBit ((updateCapabilities_r15 != null), null);

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         Asn1PerEncodeBuffer mainBuffer = buffer;
         buffer = new Asn1PerEncodeBuffer (buffer.isAligned());

         // updateCapabilities_r15

         if (updateCapabilities_r15 != null) {
            buffer.reset();
            updateCapabilities_r15.encode (buffer);
            buffer.getContext().eventDispatcher.startElement("updateCapabilities_r15", -1);

            mainBuffer.encodeOpenType (buffer, null);

            buffer.getContext().eventDispatcher.endElement("updateCapabilities_r15", -1);
         }

         buffer = mainBuffer;

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
