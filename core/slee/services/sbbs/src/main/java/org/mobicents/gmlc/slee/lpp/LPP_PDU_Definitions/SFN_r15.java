/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class SFN_r15 extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "SFN-r15";
   }

   protected Asn1BitString sfn_r15;
   protected Asn1BitString hyperSFN_r15;  // optional
   protected Asn1OpenExt extElem1;

   public SFN_r15 () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public SFN_r15 (
      Asn1BitString sfn_r15_,
      Asn1BitString hyperSFN_r15_
   ) throws Asn1Exception {
      super();
      setSfn_r15 (sfn_r15_);
      setHyperSFN_r15 (hyperSFN_r15_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public SFN_r15 (
      Asn1BitString sfn_r15_
   ) throws Asn1Exception {
      super();
      setSfn_r15 (sfn_r15_);
   }

   public void init () {
      sfn_r15 = null;
      hyperSFN_r15 = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof SFN_r15) ) return false;

      SFN_r15 rhs = (SFN_r15) obj;

      if (sfn_r15 == null) {
         if (rhs.sfn_r15 != null) return false;
      }
      else {
         if (!sfn_r15.equals(rhs.sfn_r15)) {
            return false;
         }
      }

      if (hyperSFN_r15 == null) {
         if (rhs.hyperSFN_r15 != null) return false;
      }
      else {
         if (!hyperSFN_r15.equals(rhs.hyperSFN_r15)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (sfn_r15 != null) __code = 31*__code + sfn_r15.hashCode();
      if (hyperSFN_r15 != null) __code = 31*__code + hyperSFN_r15.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Sfn_r15
    */
   public Asn1BitString getSfn_r15 () {
      return sfn_r15;
   }

   public void setSfn_r15 (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 10)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.sfn_r15 = value;
   }

   /**
    * Accessor/mutator methods for HyperSFN_r15
    */
   public Asn1BitString getHyperSFN_r15 () {
      return hyperSFN_r15;
   }

   public void setHyperSFN_r15 (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 10)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.hyperSFN_r15 = value;
   }

   public boolean hasHyperSFN_r15 () {
      return (hyperSFN_r15 != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return sfn_r15;
         case 1: return hyperSFN_r15;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "sfn-r15";
         case 1: return "hyperSFN-r15";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean hyperSFN_r15Present = buffer.decodeBit ("hyperSFN_r15Present");

      // decode sfn_r15

      buffer.getContext().eventDispatcher.startElement("sfn_r15", -1);

      sfn_r15 = new Asn1BitString();
      sfn_r15.decode (buffer, 10, 10);
      buffer.getContext().eventDispatcher.endElement("sfn_r15", -1);

      // decode hyperSFN_r15

      if (hyperSFN_r15Present) {
         buffer.getContext().eventDispatcher.startElement("hyperSFN_r15", -1);

         hyperSFN_r15 = new Asn1BitString();
         hyperSFN_r15.decode (buffer, 10, 10);
         buffer.getContext().eventDispatcher.endElement("hyperSFN_r15", -1);
      }
      else {
         hyperSFN_r15 = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((hyperSFN_r15 != null), null);

      // encode sfn_r15

      if (sfn_r15 != null) {
         buffer.getContext().eventDispatcher.startElement("sfn_r15", -1);

         sfn_r15.encode (buffer, 10, 10);

         buffer.getContext().eventDispatcher.endElement("sfn_r15", -1);
      }
      else throw new Asn1MissingRequiredException ("sfn_r15");

      // encode hyperSFN_r15

      if (hyperSFN_r15 != null) {
         buffer.getContext().eventDispatcher.startElement("hyperSFN_r15", -1);

         hyperSFN_r15.encode (buffer, 10, 10);

         buffer.getContext().eventDispatcher.endElement("hyperSFN_r15", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
