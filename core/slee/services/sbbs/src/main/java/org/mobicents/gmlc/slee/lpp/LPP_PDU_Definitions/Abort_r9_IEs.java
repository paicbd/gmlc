/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class Abort_r9_IEs extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "Abort-r9-IEs";
   }

   protected CommonIEsAbort commonIEsAbort;  // optional
   protected EPDU_Sequence epdu_Abort;  // optional
   protected Asn1OpenExt extElem1;

   public Abort_r9_IEs () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public Abort_r9_IEs (
      CommonIEsAbort commonIEsAbort_,
      EPDU_Sequence epdu_Abort_
   ) throws Asn1Exception {
      super();
      setCommonIEsAbort (commonIEsAbort_);
      setEpdu_Abort (epdu_Abort_);
   }

   public void init () {
      commonIEsAbort = null;
      epdu_Abort = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof Abort_r9_IEs) ) return false;

      Abort_r9_IEs rhs = (Abort_r9_IEs) obj;

      if (commonIEsAbort == null) {
         if (rhs.commonIEsAbort != null) return false;
      }
      else {
         if (!commonIEsAbort.equals(rhs.commonIEsAbort)) {
            return false;
         }
      }

      if (epdu_Abort == null) {
         if (rhs.epdu_Abort != null) return false;
      }
      else {
         if (!epdu_Abort.equals(rhs.epdu_Abort)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (commonIEsAbort != null) __code = 31*__code + commonIEsAbort.hashCode();
      if (epdu_Abort != null) __code = 31*__code + epdu_Abort.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for CommonIEsAbort
    */
   public CommonIEsAbort getCommonIEsAbort () {
      return commonIEsAbort;
   }

   public void setCommonIEsAbort (CommonIEsAbort value) {
      this.commonIEsAbort = value;
   }

   public boolean hasCommonIEsAbort () {
      return (commonIEsAbort != null);
   }

   /**
    * Accessor/mutator methods for Epdu_Abort
    */
   public EPDU_Sequence getEpdu_Abort () {
      return epdu_Abort;
   }

   public void setEpdu_Abort (EPDU_Sequence value) 
      throws Asn1Exception
   {
      if (!((value.getElements().length >= 1 && value.getElements().length <= 16))) {
         throw new Asn1ConsVioException ("value.getElements().length", value.getElements().length);
      }

      this.epdu_Abort = value;
   }

   public boolean hasEpdu_Abort () {
      return (epdu_Abort != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return commonIEsAbort;
         case 1: return epdu_Abort;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "commonIEsAbort";
         case 1: return "epdu-Abort";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean commonIEsAbortPresent = buffer.decodeBit ("commonIEsAbortPresent");

      // decode commonIEsAbort

      if (commonIEsAbortPresent) {
         buffer.getContext().eventDispatcher.startElement("commonIEsAbort", -1);

         commonIEsAbort = new CommonIEsAbort();
         commonIEsAbort.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("commonIEsAbort", -1);
      }
      else {
         commonIEsAbort = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("epdu_AbortPresent");

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode epdu_Abort

         if (i < bitcnt && bitmap[i++]) {
            buffer.getContext().eventDispatcher.startElement("epdu_Abort", -1);

            long _curidx = buffer.getBitOffset();
            bitidx = buffer.decodeExtLength ();
            if (bitidx - _curidx >= 131088)  {
               throw new Asn1Exception ("PER fragmentation not enabled: add -perindef to decode");
            }

            epdu_Abort = new EPDU_Sequence();
            epdu_Abort.decode (buffer);

            buffer.moveBitCursor (bitidx);
            buffer.getContext().eventDispatcher.endElement("epdu_Abort", -1);

         }
         else {
            epdu_Abort = null;
         }

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = ((epdu_Abort != null) ||
      ((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((commonIEsAbort != null), null);

      // encode commonIEsAbort

      if (commonIEsAbort != null) {
         buffer.getContext().eventDispatcher.startElement("commonIEsAbort", -1);

         commonIEsAbort.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("commonIEsAbort", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 1;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         buffer.encodeBit ((epdu_Abort != null), null);

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         Asn1PerEncodeBuffer mainBuffer = buffer;
         buffer = new Asn1PerEncodeBuffer (buffer.isAligned());

         // epdu_Abort

         if (epdu_Abort != null) {
            buffer.reset();
            epdu_Abort.encode (buffer);
            buffer.getContext().eventDispatcher.startElement("epdu_Abort", -1);

            mainBuffer.encodeOpenType (buffer, null);

            buffer.getContext().eventDispatcher.endElement("epdu_Abort", -1);
         }

         buffer = mainBuffer;

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
