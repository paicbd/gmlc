/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.Ver2_ULP_Components;

import com.objsys.asn1j.runtime.*;

public class UTRAN_GANSSReferenceTimeResult extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_Ver2_ULP_ComponentsRtkey._rtkey);
      Asn1Type._setLicLocation(_Ver2_ULP_ComponentsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "UTRAN-GANSSReferenceTimeResult";
   }

   protected Asn1Integer ganssTimeID;
   protected SET_GANSSReferenceTime set_GANSSReferenceTime;
   protected Asn1OpenExt extElem1;

   public UTRAN_GANSSReferenceTimeResult () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public UTRAN_GANSSReferenceTimeResult (
      Asn1Integer ganssTimeID_,
      SET_GANSSReferenceTime set_GANSSReferenceTime_
   ) throws Asn1Exception {
      super();
      setGanssTimeID (ganssTimeID_);
      setSet_GANSSReferenceTime (set_GANSSReferenceTime_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public UTRAN_GANSSReferenceTimeResult (long ganssTimeID_,
      SET_GANSSReferenceTime set_GANSSReferenceTime_
   ) throws Asn1Exception {
      super();
      setGanssTimeID (ganssTimeID_);
      setSet_GANSSReferenceTime (set_GANSSReferenceTime_);
   }

   public void init () {
      ganssTimeID = null;
      set_GANSSReferenceTime = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof UTRAN_GANSSReferenceTimeResult) ) return false;

      UTRAN_GANSSReferenceTimeResult rhs = (UTRAN_GANSSReferenceTimeResult) obj;

      if (ganssTimeID == null) {
         if (rhs.ganssTimeID != null) return false;
      }
      else {
         if (!ganssTimeID.equals(rhs.ganssTimeID)) {
            return false;
         }
      }

      if (set_GANSSReferenceTime == null) {
         if (rhs.set_GANSSReferenceTime != null) return false;
      }
      else {
         if (!set_GANSSReferenceTime.equals(rhs.set_GANSSReferenceTime)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (ganssTimeID != null) __code = 31*__code + ganssTimeID.hashCode();
      if (set_GANSSReferenceTime != null) __code = 31*__code + set_GANSSReferenceTime.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for GanssTimeID
    */
   public Asn1Integer getGanssTimeID () {
      return ganssTimeID;
   }

   public void setGanssTimeID (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 15))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.ganssTimeID = value;
   }

   public void setGanssTimeID (long value) 
      throws Asn1Exception
   {
      setGanssTimeID (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for Set_GANSSReferenceTime
    */
   public SET_GANSSReferenceTime getSet_GANSSReferenceTime () {
      return set_GANSSReferenceTime;
   }

   public void setSet_GANSSReferenceTime (SET_GANSSReferenceTime value) {
      this.set_GANSSReferenceTime = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return ganssTimeID;
         case 1: return set_GANSSReferenceTime;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "ganssTimeID";
         case 1: return "set-GANSSReferenceTime";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode ganssTimeID

      buffer.getContext().eventDispatcher.startElement("ganssTimeID", -1);

      ganssTimeID = new Asn1Integer();
      ganssTimeID.decode (buffer, 0, 15);
      buffer.getContext().eventDispatcher.endElement("ganssTimeID", -1);

      // decode set_GANSSReferenceTime

      buffer.getContext().eventDispatcher.startElement("set_GANSSReferenceTime", -1);

      set_GANSSReferenceTime = new SET_GANSSReferenceTime();
      set_GANSSReferenceTime.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("set_GANSSReferenceTime", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode ganssTimeID

      if (ganssTimeID != null) {
         buffer.getContext().eventDispatcher.startElement("ganssTimeID", -1);

         ganssTimeID.encode (buffer, 0, 15);

         buffer.getContext().eventDispatcher.endElement("ganssTimeID", -1);
      }
      else throw new Asn1MissingRequiredException ("ganssTimeID");

      // encode set_GANSSReferenceTime

      if (set_GANSSReferenceTime != null) {
         buffer.getContext().eventDispatcher.startElement("set_GANSSReferenceTime", -1);

         set_GANSSReferenceTime.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("set_GANSSReferenceTime", -1);
      }
      else throw new Asn1MissingRequiredException ("set_GANSSReferenceTime");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
