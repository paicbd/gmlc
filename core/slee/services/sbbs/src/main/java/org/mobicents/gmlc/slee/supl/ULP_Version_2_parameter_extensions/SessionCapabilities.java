/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.ULP_Version_2_parameter_extensions;

import com.objsys.asn1j.runtime.*;

public class SessionCapabilities extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_ULP_Version_2_parameter_extensionsRtkey._rtkey);
      Asn1Type._setLicLocation(_ULP_Version_2_parameter_extensionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "SessionCapabilities";
   }

   protected Asn1Integer maxNumberTotalSessions;
   protected Asn1Integer maxNumberPeriodicSessions;
   protected Asn1Integer maxNumberTriggeredSessions;
   protected Asn1OpenExt extElem1;

   public SessionCapabilities () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public SessionCapabilities (
      Asn1Integer maxNumberTotalSessions_,
      Asn1Integer maxNumberPeriodicSessions_,
      Asn1Integer maxNumberTriggeredSessions_
   ) throws Asn1Exception {
      super();
      setMaxNumberTotalSessions (maxNumberTotalSessions_);
      setMaxNumberPeriodicSessions (maxNumberPeriodicSessions_);
      setMaxNumberTriggeredSessions (maxNumberTriggeredSessions_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public SessionCapabilities (long maxNumberTotalSessions_,
      long maxNumberPeriodicSessions_,
      long maxNumberTriggeredSessions_
   ) throws Asn1Exception {
      super();
      setMaxNumberTotalSessions (maxNumberTotalSessions_);
      setMaxNumberPeriodicSessions (maxNumberPeriodicSessions_);
      setMaxNumberTriggeredSessions (maxNumberTriggeredSessions_);
   }

   public void init () {
      maxNumberTotalSessions = null;
      maxNumberPeriodicSessions = null;
      maxNumberTriggeredSessions = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof SessionCapabilities) ) return false;

      SessionCapabilities rhs = (SessionCapabilities) obj;

      if (maxNumberTotalSessions == null) {
         if (rhs.maxNumberTotalSessions != null) return false;
      }
      else {
         if (!maxNumberTotalSessions.equals(rhs.maxNumberTotalSessions)) {
            return false;
         }
      }

      if (maxNumberPeriodicSessions == null) {
         if (rhs.maxNumberPeriodicSessions != null) return false;
      }
      else {
         if (!maxNumberPeriodicSessions.equals(rhs.maxNumberPeriodicSessions)) {
            return false;
         }
      }

      if (maxNumberTriggeredSessions == null) {
         if (rhs.maxNumberTriggeredSessions != null) return false;
      }
      else {
         if (!maxNumberTriggeredSessions.equals(rhs.maxNumberTriggeredSessions)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (maxNumberTotalSessions != null) __code = 31*__code + maxNumberTotalSessions.hashCode();
      if (maxNumberPeriodicSessions != null) __code = 31*__code + maxNumberPeriodicSessions.hashCode();
      if (maxNumberTriggeredSessions != null) __code = 31*__code + maxNumberTriggeredSessions.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for MaxNumberTotalSessions
    */
   public Asn1Integer getMaxNumberTotalSessions () {
      return maxNumberTotalSessions;
   }

   public void setMaxNumberTotalSessions (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 1 && value.value <= 128))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.maxNumberTotalSessions = value;
   }

   public void setMaxNumberTotalSessions (long value) 
      throws Asn1Exception
   {
      setMaxNumberTotalSessions (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for MaxNumberPeriodicSessions
    */
   public Asn1Integer getMaxNumberPeriodicSessions () {
      return maxNumberPeriodicSessions;
   }

   public void setMaxNumberPeriodicSessions (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 1 && value.value <= 32))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.maxNumberPeriodicSessions = value;
   }

   public void setMaxNumberPeriodicSessions (long value) 
      throws Asn1Exception
   {
      setMaxNumberPeriodicSessions (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for MaxNumberTriggeredSessions
    */
   public Asn1Integer getMaxNumberTriggeredSessions () {
      return maxNumberTriggeredSessions;
   }

   public void setMaxNumberTriggeredSessions (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 1 && value.value <= 32))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.maxNumberTriggeredSessions = value;
   }

   public void setMaxNumberTriggeredSessions (long value) 
      throws Asn1Exception
   {
      setMaxNumberTriggeredSessions (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 4; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return maxNumberTotalSessions;
         case 1: return maxNumberPeriodicSessions;
         case 2: return maxNumberTriggeredSessions;
         case 3: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "maxNumberTotalSessions";
         case 1: return "maxNumberPeriodicSessions";
         case 2: return "maxNumberTriggeredSessions";
         case 3: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode maxNumberTotalSessions

      buffer.getContext().eventDispatcher.startElement("maxNumberTotalSessions", -1);

      maxNumberTotalSessions = new Asn1Integer();
      maxNumberTotalSessions.decode (buffer, 1, 128);
      buffer.getContext().eventDispatcher.endElement("maxNumberTotalSessions", -1);

      // decode maxNumberPeriodicSessions

      buffer.getContext().eventDispatcher.startElement("maxNumberPeriodicSessions", -1);

      maxNumberPeriodicSessions = new Asn1Integer();
      maxNumberPeriodicSessions.decode (buffer, 1, 32);
      buffer.getContext().eventDispatcher.endElement("maxNumberPeriodicSessions", -1);

      // decode maxNumberTriggeredSessions

      buffer.getContext().eventDispatcher.startElement("maxNumberTriggeredSessions", -1);

      maxNumberTriggeredSessions = new Asn1Integer();
      maxNumberTriggeredSessions.decode (buffer, 1, 32);
      buffer.getContext().eventDispatcher.endElement("maxNumberTriggeredSessions", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode maxNumberTotalSessions

      if (maxNumberTotalSessions != null) {
         buffer.getContext().eventDispatcher.startElement("maxNumberTotalSessions", -1);

         maxNumberTotalSessions.encode (buffer, 1, 128);

         buffer.getContext().eventDispatcher.endElement("maxNumberTotalSessions", -1);
      }
      else throw new Asn1MissingRequiredException ("maxNumberTotalSessions");

      // encode maxNumberPeriodicSessions

      if (maxNumberPeriodicSessions != null) {
         buffer.getContext().eventDispatcher.startElement("maxNumberPeriodicSessions", -1);

         maxNumberPeriodicSessions.encode (buffer, 1, 32);

         buffer.getContext().eventDispatcher.endElement("maxNumberPeriodicSessions", -1);
      }
      else throw new Asn1MissingRequiredException ("maxNumberPeriodicSessions");

      // encode maxNumberTriggeredSessions

      if (maxNumberTriggeredSessions != null) {
         buffer.getContext().eventDispatcher.startElement("maxNumberTriggeredSessions", -1);

         maxNumberTriggeredSessions.encode (buffer, 1, 32);

         buffer.getContext().eventDispatcher.endElement("maxNumberTriggeredSessions", -1);
      }
      else throw new Asn1MissingRequiredException ("maxNumberTriggeredSessions");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
