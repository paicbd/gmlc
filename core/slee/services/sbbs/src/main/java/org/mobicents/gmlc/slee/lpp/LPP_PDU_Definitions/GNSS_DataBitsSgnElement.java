/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class GNSS_DataBitsSgnElement extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "GNSS-DataBitsSgnElement";
   }

   protected GNSS_SignalID gnss_SignalType;
   protected Asn1BitString gnss_DataBits;
   protected Asn1OpenExt extElem1;

   public GNSS_DataBitsSgnElement () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public GNSS_DataBitsSgnElement (
      GNSS_SignalID gnss_SignalType_,
      Asn1BitString gnss_DataBits_
   ) throws Asn1Exception {
      super();
      setGnss_SignalType (gnss_SignalType_);
      setGnss_DataBits (gnss_DataBits_);
   }

   public void init () {
      gnss_SignalType = null;
      gnss_DataBits = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof GNSS_DataBitsSgnElement) ) return false;

      GNSS_DataBitsSgnElement rhs = (GNSS_DataBitsSgnElement) obj;

      if (gnss_SignalType == null) {
         if (rhs.gnss_SignalType != null) return false;
      }
      else {
         if (!gnss_SignalType.equals(rhs.gnss_SignalType)) {
            return false;
         }
      }

      if (gnss_DataBits == null) {
         if (rhs.gnss_DataBits != null) return false;
      }
      else {
         if (!gnss_DataBits.equals(rhs.gnss_DataBits)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (gnss_SignalType != null) __code = 31*__code + gnss_SignalType.hashCode();
      if (gnss_DataBits != null) __code = 31*__code + gnss_DataBits.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Gnss_SignalType
    */
   public GNSS_SignalID getGnss_SignalType () {
      return gnss_SignalType;
   }

   public void setGnss_SignalType (GNSS_SignalID value) {
      this.gnss_SignalType = value;
   }

   /**
    * Accessor/mutator methods for Gnss_DataBits
    */
   public Asn1BitString getGnss_DataBits () {
      return gnss_DataBits;
   }

   public void setGnss_DataBits (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!((value.getLength() >= 1 && value.getLength() <= 1024))) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.gnss_DataBits = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return gnss_SignalType;
         case 1: return gnss_DataBits;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "gnss-SignalType";
         case 1: return "gnss-DataBits";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode gnss_SignalType

      buffer.getContext().eventDispatcher.startElement("gnss_SignalType", -1);

      gnss_SignalType = new GNSS_SignalID();
      gnss_SignalType.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("gnss_SignalType", -1);

      // decode gnss_DataBits

      buffer.getContext().eventDispatcher.startElement("gnss_DataBits", -1);

      gnss_DataBits = new Asn1BitString();
      gnss_DataBits.decode (buffer, 1, 1024);
      buffer.getContext().eventDispatcher.endElement("gnss_DataBits", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode gnss_SignalType

      if (gnss_SignalType != null) {
         buffer.getContext().eventDispatcher.startElement("gnss_SignalType", -1);

         gnss_SignalType.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("gnss_SignalType", -1);
      }
      else throw new Asn1MissingRequiredException ("gnss_SignalType");

      // encode gnss_DataBits

      if (gnss_DataBits != null) {
         buffer.getContext().eventDispatcher.startElement("gnss_DataBits", -1);

         gnss_DataBits.encode (buffer, 1, 1024);

         buffer.getContext().eventDispatcher.endElement("gnss_DataBits", -1);
      }
      else throw new Asn1MissingRequiredException ("gnss_DataBits");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
