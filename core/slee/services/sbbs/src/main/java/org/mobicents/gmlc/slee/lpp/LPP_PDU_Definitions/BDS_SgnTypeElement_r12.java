/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class BDS_SgnTypeElement_r12 extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "BDS-SgnTypeElement-r12";
   }

   protected GNSS_SignalID gnss_SignalID;  // optional
   protected DBDS_CorrectionList_r12 dbds_CorrectionList_r12;
   protected Asn1OpenExt extElem1;

   public BDS_SgnTypeElement_r12 () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public BDS_SgnTypeElement_r12 (
      GNSS_SignalID gnss_SignalID_,
      DBDS_CorrectionList_r12 dbds_CorrectionList_r12_
   ) throws Asn1Exception {
      super();
      setGnss_SignalID (gnss_SignalID_);
      setDbds_CorrectionList_r12 (dbds_CorrectionList_r12_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public BDS_SgnTypeElement_r12 (
      DBDS_CorrectionList_r12 dbds_CorrectionList_r12_
   ) throws Asn1Exception {
      super();
      setDbds_CorrectionList_r12 (dbds_CorrectionList_r12_);
   }

   public void init () {
      gnss_SignalID = null;
      dbds_CorrectionList_r12 = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof BDS_SgnTypeElement_r12) ) return false;

      BDS_SgnTypeElement_r12 rhs = (BDS_SgnTypeElement_r12) obj;

      if (gnss_SignalID == null) {
         if (rhs.gnss_SignalID != null) return false;
      }
      else {
         if (!gnss_SignalID.equals(rhs.gnss_SignalID)) {
            return false;
         }
      }

      if (dbds_CorrectionList_r12 == null) {
         if (rhs.dbds_CorrectionList_r12 != null) return false;
      }
      else {
         if (!dbds_CorrectionList_r12.equals(rhs.dbds_CorrectionList_r12)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (gnss_SignalID != null) __code = 31*__code + gnss_SignalID.hashCode();
      if (dbds_CorrectionList_r12 != null) __code = 31*__code + dbds_CorrectionList_r12.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Gnss_SignalID
    */
   public GNSS_SignalID getGnss_SignalID () {
      return gnss_SignalID;
   }

   public void setGnss_SignalID (GNSS_SignalID value) {
      this.gnss_SignalID = value;
   }

   public boolean hasGnss_SignalID () {
      return (gnss_SignalID != null);
   }

   /**
    * Accessor/mutator methods for Dbds_CorrectionList_r12
    */
   public DBDS_CorrectionList_r12 getDbds_CorrectionList_r12 () {
      return dbds_CorrectionList_r12;
   }

   public void setDbds_CorrectionList_r12 (DBDS_CorrectionList_r12 value) 
      throws Asn1Exception
   {
      if (!((value.getElements().length >= 1 && value.getElements().length <= 64))) {
         throw new Asn1ConsVioException ("value.getElements().length", value.getElements().length);
      }

      this.dbds_CorrectionList_r12 = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return gnss_SignalID;
         case 1: return dbds_CorrectionList_r12;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "gnss-SignalID";
         case 1: return "dbds-CorrectionList-r12";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean gnss_SignalIDPresent = buffer.decodeBit ("gnss_SignalIDPresent");

      // decode gnss_SignalID

      if (gnss_SignalIDPresent) {
         buffer.getContext().eventDispatcher.startElement("gnss_SignalID", -1);

         gnss_SignalID = new GNSS_SignalID();
         gnss_SignalID.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("gnss_SignalID", -1);
      }
      else {
         gnss_SignalID = null;
      }

      // decode dbds_CorrectionList_r12

      buffer.getContext().eventDispatcher.startElement("dbds_CorrectionList_r12", -1);

      dbds_CorrectionList_r12 = new DBDS_CorrectionList_r12();
      dbds_CorrectionList_r12.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("dbds_CorrectionList_r12", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((gnss_SignalID != null), null);

      // encode gnss_SignalID

      if (gnss_SignalID != null) {
         buffer.getContext().eventDispatcher.startElement("gnss_SignalID", -1);

         gnss_SignalID.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("gnss_SignalID", -1);
      }

      // encode dbds_CorrectionList_r12

      if (dbds_CorrectionList_r12 != null) {
         buffer.getContext().eventDispatcher.startElement("dbds_CorrectionList_r12", -1);

         dbds_CorrectionList_r12.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("dbds_CorrectionList_r12", -1);
      }
      else throw new Asn1MissingRequiredException ("dbds_CorrectionList_r12");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
