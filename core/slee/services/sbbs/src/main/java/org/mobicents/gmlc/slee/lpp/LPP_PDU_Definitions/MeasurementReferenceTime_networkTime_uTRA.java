/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class MeasurementReferenceTime_networkTime_uTRA extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "SEQUENCE";
   }

   protected MeasurementReferenceTime_networkTime_uTRA_mode mode;
   protected CellGlobalIdEUTRA_AndUTRA cellGlobalId;  // optional
   protected Asn1Integer referenceSystemFrameNumber;
   protected Asn1OpenExt extElem1;

   public MeasurementReferenceTime_networkTime_uTRA () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public MeasurementReferenceTime_networkTime_uTRA (
      MeasurementReferenceTime_networkTime_uTRA_mode mode_,
      CellGlobalIdEUTRA_AndUTRA cellGlobalId_,
      Asn1Integer referenceSystemFrameNumber_
   ) throws Asn1Exception {
      super();
      setMode (mode_);
      setCellGlobalId (cellGlobalId_);
      setReferenceSystemFrameNumber (referenceSystemFrameNumber_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public MeasurementReferenceTime_networkTime_uTRA (
      MeasurementReferenceTime_networkTime_uTRA_mode mode_,
      Asn1Integer referenceSystemFrameNumber_
   ) throws Asn1Exception {
      super();
      setMode (mode_);
      setReferenceSystemFrameNumber (referenceSystemFrameNumber_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public MeasurementReferenceTime_networkTime_uTRA (MeasurementReferenceTime_networkTime_uTRA_mode mode_,
      CellGlobalIdEUTRA_AndUTRA cellGlobalId_,
      long referenceSystemFrameNumber_
   ) throws Asn1Exception {
      super();
      setMode (mode_);
      setCellGlobalId (cellGlobalId_);
      setReferenceSystemFrameNumber (referenceSystemFrameNumber_);
   }

   /**
    * This constructor is for required elements only.  It allows 
    * primitive data to be passed for all primitive elements.  
    * It will create new object wrappers for the primitive data 
    * and set other elements to references to the given objects. 
    */
   public MeasurementReferenceTime_networkTime_uTRA (
      MeasurementReferenceTime_networkTime_uTRA_mode mode_,
      long referenceSystemFrameNumber_
   ) throws Asn1Exception {
      super();
      setMode (mode_);
      setReferenceSystemFrameNumber (referenceSystemFrameNumber_);
   }

   public void init () {
      mode = null;
      cellGlobalId = null;
      referenceSystemFrameNumber = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof MeasurementReferenceTime_networkTime_uTRA) ) return false;

      MeasurementReferenceTime_networkTime_uTRA rhs = (MeasurementReferenceTime_networkTime_uTRA) obj;

      if (mode == null) {
         if (rhs.mode != null) return false;
      }
      else {
         if (!mode.equals(rhs.mode)) {
            return false;
         }
      }

      if (cellGlobalId == null) {
         if (rhs.cellGlobalId != null) return false;
      }
      else {
         if (!cellGlobalId.equals(rhs.cellGlobalId)) {
            return false;
         }
      }

      if (referenceSystemFrameNumber == null) {
         if (rhs.referenceSystemFrameNumber != null) return false;
      }
      else {
         if (!referenceSystemFrameNumber.equals(rhs.referenceSystemFrameNumber)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (mode != null) __code = 31*__code + mode.hashCode();
      if (cellGlobalId != null) __code = 31*__code + cellGlobalId.hashCode();
      if (referenceSystemFrameNumber != null) __code = 31*__code + referenceSystemFrameNumber.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Mode
    */
   public MeasurementReferenceTime_networkTime_uTRA_mode getMode () {
      return mode;
   }

   public void setMode (MeasurementReferenceTime_networkTime_uTRA_mode value) {
      this.mode = value;
   }

   /**
    * Accessor/mutator methods for CellGlobalId
    */
   public CellGlobalIdEUTRA_AndUTRA getCellGlobalId () {
      return cellGlobalId;
   }

   public void setCellGlobalId (CellGlobalIdEUTRA_AndUTRA value) {
      this.cellGlobalId = value;
   }

   public boolean hasCellGlobalId () {
      return (cellGlobalId != null);
   }

   /**
    * Accessor/mutator methods for ReferenceSystemFrameNumber
    */
   public Asn1Integer getReferenceSystemFrameNumber () {
      return referenceSystemFrameNumber;
   }

   public void setReferenceSystemFrameNumber (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 4095))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.referenceSystemFrameNumber = value;
   }

   public void setReferenceSystemFrameNumber (long value) 
      throws Asn1Exception
   {
      setReferenceSystemFrameNumber (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 4; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return mode;
         case 1: return cellGlobalId;
         case 2: return referenceSystemFrameNumber;
         case 3: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "mode";
         case 1: return "cellGlobalId";
         case 2: return "referenceSystemFrameNumber";
         case 3: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean cellGlobalIdPresent = buffer.decodeBit ("cellGlobalIdPresent");

      // decode mode

      buffer.getContext().eventDispatcher.startElement("mode", -1);

      mode = new MeasurementReferenceTime_networkTime_uTRA_mode();
      mode.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("mode", -1);

      // decode cellGlobalId

      if (cellGlobalIdPresent) {
         buffer.getContext().eventDispatcher.startElement("cellGlobalId", -1);

         cellGlobalId = new CellGlobalIdEUTRA_AndUTRA();
         cellGlobalId.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("cellGlobalId", -1);
      }
      else {
         cellGlobalId = null;
      }

      // decode referenceSystemFrameNumber

      buffer.getContext().eventDispatcher.startElement("referenceSystemFrameNumber", -1);

      referenceSystemFrameNumber = new Asn1Integer();
      referenceSystemFrameNumber.decode (buffer, 0, 4095);
      buffer.getContext().eventDispatcher.endElement("referenceSystemFrameNumber", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((cellGlobalId != null), null);

      // encode mode

      if (mode != null) {
         buffer.getContext().eventDispatcher.startElement("mode", -1);

         mode.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("mode", -1);
      }
      else throw new Asn1MissingRequiredException ("mode");

      // encode cellGlobalId

      if (cellGlobalId != null) {
         buffer.getContext().eventDispatcher.startElement("cellGlobalId", -1);

         cellGlobalId.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("cellGlobalId", -1);
      }

      // encode referenceSystemFrameNumber

      if (referenceSystemFrameNumber != null) {
         buffer.getContext().eventDispatcher.startElement("referenceSystemFrameNumber", -1);

         referenceSystemFrameNumber.encode (buffer, 0, 4095);

         buffer.getContext().eventDispatcher.endElement("referenceSystemFrameNumber", -1);
      }
      else throw new Asn1MissingRequiredException ("referenceSystemFrameNumber");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
