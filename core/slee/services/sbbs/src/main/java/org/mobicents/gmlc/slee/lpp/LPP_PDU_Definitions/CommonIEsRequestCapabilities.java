/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class CommonIEsRequestCapabilities extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "CommonIEsRequestCapabilities";
   }

   protected CommonIEsRequestCapabilities_lpp_message_segmentation_req_r14 lpp_message_segmentation_req_r14;  // optional
   protected Asn1OpenExt extElem1;

   public CommonIEsRequestCapabilities () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public CommonIEsRequestCapabilities (
      CommonIEsRequestCapabilities_lpp_message_segmentation_req_r14 lpp_message_segmentation_req_r14_
   ) {
      super();
      setLpp_message_segmentation_req_r14 (lpp_message_segmentation_req_r14_);
   }

   public void init () {
      lpp_message_segmentation_req_r14 = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof CommonIEsRequestCapabilities) ) return false;

      CommonIEsRequestCapabilities rhs = (CommonIEsRequestCapabilities) obj;

      if (lpp_message_segmentation_req_r14 == null) {
         if (rhs.lpp_message_segmentation_req_r14 != null) return false;
      }
      else {
         if (!lpp_message_segmentation_req_r14.equals(rhs.lpp_message_segmentation_req_r14)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (lpp_message_segmentation_req_r14 != null) __code = 31*__code + lpp_message_segmentation_req_r14.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Lpp_message_segmentation_req_r14
    */
   public CommonIEsRequestCapabilities_lpp_message_segmentation_req_r14 getLpp_message_segmentation_req_r14 () {
      return lpp_message_segmentation_req_r14;
   }

   public void setLpp_message_segmentation_req_r14 (CommonIEsRequestCapabilities_lpp_message_segmentation_req_r14 value) {
      this.lpp_message_segmentation_req_r14 = value;
   }

   public boolean hasLpp_message_segmentation_req_r14 () {
      return (lpp_message_segmentation_req_r14 != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 2; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return lpp_message_segmentation_req_r14;
         case 1: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "lpp-message-segmentation-req-r14";
         case 1: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("lpp_message_segmentation_req_r14Present");

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode lpp_message_segmentation_req_r14

         if (i < bitcnt && bitmap[i++]) {
            buffer.getContext().eventDispatcher.startElement("lpp_message_segmentation_req_r14", -1);

            long _curidx = buffer.getBitOffset();
            bitidx = buffer.decodeExtLength ();
            if (bitidx - _curidx >= 131088)  {
               throw new Asn1Exception ("PER fragmentation not enabled: add -perindef to decode");
            }

            lpp_message_segmentation_req_r14 = new CommonIEsRequestCapabilities_lpp_message_segmentation_req_r14();
            lpp_message_segmentation_req_r14.decode (buffer);

            buffer.moveBitCursor (bitidx);
            buffer.getContext().eventDispatcher.endElement("lpp_message_segmentation_req_r14", -1);

         }
         else {
            lpp_message_segmentation_req_r14 = null;
         }

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = ((lpp_message_segmentation_req_r14 != null) ||
      ((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 1;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         buffer.encodeBit ((lpp_message_segmentation_req_r14 != null), null);

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         Asn1PerEncodeBuffer mainBuffer = buffer;
         buffer = new Asn1PerEncodeBuffer (buffer.isAligned());

         // lpp_message_segmentation_req_r14

         if (lpp_message_segmentation_req_r14 != null) {
            buffer.reset();
            lpp_message_segmentation_req_r14.encode (buffer);
            buffer.getContext().eventDispatcher.startElement("lpp_message_segmentation_req_r14", -1);

            mainBuffer.encodeOpenType (buffer, null);

            buffer.getContext().eventDispatcher.endElement("lpp_message_segmentation_req_r14", -1);
         }

         buffer = mainBuffer;

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
