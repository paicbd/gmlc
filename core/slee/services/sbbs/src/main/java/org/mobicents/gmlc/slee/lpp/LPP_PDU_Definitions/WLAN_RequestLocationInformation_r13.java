/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class WLAN_RequestLocationInformation_r13 extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "WLAN-RequestLocationInformation-r13";
   }

   protected WLAN_RequestLocationInformation_r13_requestedMeasurements_r13 requestedMeasurements_r13;
   protected Asn1Boolean assistanceAvailability_r14;  // optional
   protected Asn1OpenExt extElem1;

   public WLAN_RequestLocationInformation_r13 () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public WLAN_RequestLocationInformation_r13 (
      WLAN_RequestLocationInformation_r13_requestedMeasurements_r13 requestedMeasurements_r13_,
      Asn1Boolean assistanceAvailability_r14_
   ) throws Asn1Exception {
      super();
      setRequestedMeasurements_r13 (requestedMeasurements_r13_);
      setAssistanceAvailability_r14 (assistanceAvailability_r14_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public WLAN_RequestLocationInformation_r13 (
      WLAN_RequestLocationInformation_r13_requestedMeasurements_r13 requestedMeasurements_r13_
   ) throws Asn1Exception {
      super();
      setRequestedMeasurements_r13 (requestedMeasurements_r13_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public WLAN_RequestLocationInformation_r13 (WLAN_RequestLocationInformation_r13_requestedMeasurements_r13 requestedMeasurements_r13_,
      boolean assistanceAvailability_r14_
   ) throws Asn1Exception {
      super();
      setRequestedMeasurements_r13 (requestedMeasurements_r13_);
      setAssistanceAvailability_r14 (assistanceAvailability_r14_);
   }

   public void init () {
      requestedMeasurements_r13 = null;
      assistanceAvailability_r14 = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof WLAN_RequestLocationInformation_r13) ) return false;

      WLAN_RequestLocationInformation_r13 rhs = (WLAN_RequestLocationInformation_r13) obj;

      if (requestedMeasurements_r13 == null) {
         if (rhs.requestedMeasurements_r13 != null) return false;
      }
      else {
         if (!requestedMeasurements_r13.equals(rhs.requestedMeasurements_r13)) {
            return false;
         }
      }

      if (assistanceAvailability_r14 == null) {
         if (rhs.assistanceAvailability_r14 != null) return false;
      }
      else {
         if (!assistanceAvailability_r14.equals(rhs.assistanceAvailability_r14)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (requestedMeasurements_r13 != null) __code = 31*__code + requestedMeasurements_r13.hashCode();
      if (assistanceAvailability_r14 != null) __code = 31*__code + assistanceAvailability_r14.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for RequestedMeasurements_r13
    */
   public WLAN_RequestLocationInformation_r13_requestedMeasurements_r13 getRequestedMeasurements_r13 () {
      return requestedMeasurements_r13;
   }

   public void setRequestedMeasurements_r13 (WLAN_RequestLocationInformation_r13_requestedMeasurements_r13 value) 
      throws Asn1Exception
   {
      if (!((value.getLength() >= 1 && value.getLength() <= 8))) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.requestedMeasurements_r13 = value;
   }

   /**
    * Accessor/mutator methods for AssistanceAvailability_r14
    */
   public Asn1Boolean getAssistanceAvailability_r14 () {
      return assistanceAvailability_r14;
   }

   public void setAssistanceAvailability_r14 (Asn1Boolean value) {
      this.assistanceAvailability_r14 = value;
   }

   public void setAssistanceAvailability_r14 (boolean value) {
      setAssistanceAvailability_r14 (new Asn1Boolean(value));
   }
   public boolean hasAssistanceAvailability_r14 () {
      return (assistanceAvailability_r14 != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return requestedMeasurements_r13;
         case 1: return assistanceAvailability_r14;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "requestedMeasurements-r13";
         case 1: return "assistanceAvailability-r14";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      // decode requestedMeasurements_r13

      buffer.getContext().eventDispatcher.startElement("requestedMeasurements_r13", -1);

      requestedMeasurements_r13 = new WLAN_RequestLocationInformation_r13_requestedMeasurements_r13();
      requestedMeasurements_r13.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("requestedMeasurements_r13", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("assistanceAvailability_r14Present");

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode assistanceAvailability_r14

         if (i < bitcnt && bitmap[i++]) {
            buffer.getContext().eventDispatcher.startElement("assistanceAvailability_r14", -1);

            long _curidx = buffer.getBitOffset();
            bitidx = buffer.decodeExtLength ();
            if (bitidx - _curidx >= 131088)  {
               throw new Asn1Exception ("PER fragmentation not enabled: add -perindef to decode");
            }

            assistanceAvailability_r14 = new Asn1Boolean();
            assistanceAvailability_r14.decode (buffer);

            buffer.moveBitCursor (bitidx);
            buffer.getContext().eventDispatcher.endElement("assistanceAvailability_r14", -1);

         }
         else {
            assistanceAvailability_r14 = null;
         }

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = ((assistanceAvailability_r14 != null) ||
      ((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode requestedMeasurements_r13

      if (requestedMeasurements_r13 != null) {
         buffer.getContext().eventDispatcher.startElement("requestedMeasurements_r13", -1);

         requestedMeasurements_r13.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("requestedMeasurements_r13", -1);
      }
      else throw new Asn1MissingRequiredException ("requestedMeasurements_r13");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 1;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         buffer.encodeBit ((assistanceAvailability_r14 != null), null);

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         Asn1PerEncodeBuffer mainBuffer = buffer;
         buffer = new Asn1PerEncodeBuffer (buffer.isAligned());

         // assistanceAvailability_r14

         if (assistanceAvailability_r14 != null) {
            buffer.reset();
            assistanceAvailability_r14.encode (buffer);
            buffer.getContext().eventDispatcher.startElement("assistanceAvailability_r14", -1);

            mainBuffer.encodeOpenType (buffer, null);

            buffer.getContext().eventDispatcher.endElement("assistanceAvailability_r14", -1);
         }

         buffer = mainBuffer;

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
