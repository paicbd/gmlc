/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class MeasurementReferenceTime_networkTime_gSM_referenceFrame extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "SEQUENCE";
   }

   protected Asn1Integer referenceFN;
   protected Asn1Integer referenceFNMSB;  // optional
   protected Asn1OpenExt extElem1;

   public MeasurementReferenceTime_networkTime_gSM_referenceFrame () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public MeasurementReferenceTime_networkTime_gSM_referenceFrame (
      Asn1Integer referenceFN_,
      Asn1Integer referenceFNMSB_
   ) throws Asn1Exception {
      super();
      setReferenceFN (referenceFN_);
      setReferenceFNMSB (referenceFNMSB_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public MeasurementReferenceTime_networkTime_gSM_referenceFrame (
      Asn1Integer referenceFN_
   ) throws Asn1Exception {
      super();
      setReferenceFN (referenceFN_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public MeasurementReferenceTime_networkTime_gSM_referenceFrame (long referenceFN_,
      long referenceFNMSB_
   ) throws Asn1Exception {
      super();
      setReferenceFN (referenceFN_);
      setReferenceFNMSB (referenceFNMSB_);
   }

   /**
    * This constructor is for required elements only.  It allows 
    * primitive data to be passed for all primitive elements.  
    * It will create new object wrappers for the primitive data 
    * and set other elements to references to the given objects. 
    */
   public MeasurementReferenceTime_networkTime_gSM_referenceFrame (
      long referenceFN_
   ) throws Asn1Exception {
      super();
      setReferenceFN (referenceFN_);
   }

   public void init () {
      referenceFN = null;
      referenceFNMSB = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof MeasurementReferenceTime_networkTime_gSM_referenceFrame) ) return false;

      MeasurementReferenceTime_networkTime_gSM_referenceFrame rhs = (MeasurementReferenceTime_networkTime_gSM_referenceFrame) obj;

      if (referenceFN == null) {
         if (rhs.referenceFN != null) return false;
      }
      else {
         if (!referenceFN.equals(rhs.referenceFN)) {
            return false;
         }
      }

      if (referenceFNMSB == null) {
         if (rhs.referenceFNMSB != null) return false;
      }
      else {
         if (!referenceFNMSB.equals(rhs.referenceFNMSB)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (referenceFN != null) __code = 31*__code + referenceFN.hashCode();
      if (referenceFNMSB != null) __code = 31*__code + referenceFNMSB.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for ReferenceFN
    */
   public Asn1Integer getReferenceFN () {
      return referenceFN;
   }

   public void setReferenceFN (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 65535))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.referenceFN = value;
   }

   public void setReferenceFN (long value) 
      throws Asn1Exception
   {
      setReferenceFN (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for ReferenceFNMSB
    */
   public Asn1Integer getReferenceFNMSB () {
      return referenceFNMSB;
   }

   public void setReferenceFNMSB (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 63))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.referenceFNMSB = value;
   }

   public void setReferenceFNMSB (long value) 
      throws Asn1Exception
   {
      setReferenceFNMSB (new Asn1Integer(value));
   }
   public boolean hasReferenceFNMSB () {
      return (referenceFNMSB != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return referenceFN;
         case 1: return referenceFNMSB;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "referenceFN";
         case 1: return "referenceFNMSB";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean referenceFNMSBPresent = buffer.decodeBit ("referenceFNMSBPresent");

      // decode referenceFN

      buffer.getContext().eventDispatcher.startElement("referenceFN", -1);

      referenceFN = new Asn1Integer();
      referenceFN.decode (buffer, 0, 65535);
      buffer.getContext().eventDispatcher.endElement("referenceFN", -1);

      // decode referenceFNMSB

      if (referenceFNMSBPresent) {
         buffer.getContext().eventDispatcher.startElement("referenceFNMSB", -1);

         referenceFNMSB = new Asn1Integer();
         referenceFNMSB.decode (buffer, 0, 63);
         buffer.getContext().eventDispatcher.endElement("referenceFNMSB", -1);
      }
      else {
         referenceFNMSB = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((referenceFNMSB != null), null);

      // encode referenceFN

      if (referenceFN != null) {
         buffer.getContext().eventDispatcher.startElement("referenceFN", -1);

         referenceFN.encode (buffer, 0, 65535);

         buffer.getContext().eventDispatcher.endElement("referenceFN", -1);
      }
      else throw new Asn1MissingRequiredException ("referenceFN");

      // encode referenceFNMSB

      if (referenceFNMSB != null) {
         buffer.getContext().eventDispatcher.startElement("referenceFNMSB", -1);

         referenceFNMSB.encode (buffer, 0, 63);

         buffer.getContext().eventDispatcher.endElement("referenceFNMSB", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
