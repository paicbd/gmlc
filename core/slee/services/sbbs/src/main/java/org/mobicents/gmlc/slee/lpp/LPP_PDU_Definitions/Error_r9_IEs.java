/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class Error_r9_IEs extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "Error-r9-IEs";
   }

   protected CommonIEsError commonIEsError;  // optional
   protected EPDU_Sequence epdu_Error;  // optional
   protected Asn1OpenExt extElem1;

   public Error_r9_IEs () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public Error_r9_IEs (
      CommonIEsError commonIEsError_,
      EPDU_Sequence epdu_Error_
   ) throws Asn1Exception {
      super();
      setCommonIEsError (commonIEsError_);
      setEpdu_Error (epdu_Error_);
   }

   public void init () {
      commonIEsError = null;
      epdu_Error = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof Error_r9_IEs) ) return false;

      Error_r9_IEs rhs = (Error_r9_IEs) obj;

      if (commonIEsError == null) {
         if (rhs.commonIEsError != null) return false;
      }
      else {
         if (!commonIEsError.equals(rhs.commonIEsError)) {
            return false;
         }
      }

      if (epdu_Error == null) {
         if (rhs.epdu_Error != null) return false;
      }
      else {
         if (!epdu_Error.equals(rhs.epdu_Error)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (commonIEsError != null) __code = 31*__code + commonIEsError.hashCode();
      if (epdu_Error != null) __code = 31*__code + epdu_Error.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for CommonIEsError
    */
   public CommonIEsError getCommonIEsError () {
      return commonIEsError;
   }

   public void setCommonIEsError (CommonIEsError value) {
      this.commonIEsError = value;
   }

   public boolean hasCommonIEsError () {
      return (commonIEsError != null);
   }

   /**
    * Accessor/mutator methods for Epdu_Error
    */
   public EPDU_Sequence getEpdu_Error () {
      return epdu_Error;
   }

   public void setEpdu_Error (EPDU_Sequence value) 
      throws Asn1Exception
   {
      if (!((value.getElements().length >= 1 && value.getElements().length <= 16))) {
         throw new Asn1ConsVioException ("value.getElements().length", value.getElements().length);
      }

      this.epdu_Error = value;
   }

   public boolean hasEpdu_Error () {
      return (epdu_Error != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return commonIEsError;
         case 1: return epdu_Error;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "commonIEsError";
         case 1: return "epdu-Error";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean commonIEsErrorPresent = buffer.decodeBit ("commonIEsErrorPresent");

      // decode commonIEsError

      if (commonIEsErrorPresent) {
         buffer.getContext().eventDispatcher.startElement("commonIEsError", -1);

         commonIEsError = new CommonIEsError();
         commonIEsError.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("commonIEsError", -1);
      }
      else {
         commonIEsError = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("epdu_ErrorPresent");

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode epdu_Error

         if (i < bitcnt && bitmap[i++]) {
            buffer.getContext().eventDispatcher.startElement("epdu_Error", -1);

            long _curidx = buffer.getBitOffset();
            bitidx = buffer.decodeExtLength ();
            if (bitidx - _curidx >= 131088)  {
               throw new Asn1Exception ("PER fragmentation not enabled: add -perindef to decode");
            }

            epdu_Error = new EPDU_Sequence();
            epdu_Error.decode (buffer);

            buffer.moveBitCursor (bitidx);
            buffer.getContext().eventDispatcher.endElement("epdu_Error", -1);

         }
         else {
            epdu_Error = null;
         }

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = ((epdu_Error != null) ||
      ((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((commonIEsError != null), null);

      // encode commonIEsError

      if (commonIEsError != null) {
         buffer.getContext().eventDispatcher.startElement("commonIEsError", -1);

         commonIEsError.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("commonIEsError", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 1;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         buffer.encodeBit ((epdu_Error != null), null);

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         Asn1PerEncodeBuffer mainBuffer = buffer;
         buffer = new Asn1PerEncodeBuffer (buffer.isAligned());

         // epdu_Error

         if (epdu_Error != null) {
            buffer.reset();
            epdu_Error.encode (buffer);
            buffer.getContext().eventDispatcher.startElement("epdu_Error", -1);

            mainBuffer.encodeOpenType (buffer, null);

            buffer.getContext().eventDispatcher.endElement("epdu_Error", -1);
         }

         buffer = mainBuffer;

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
