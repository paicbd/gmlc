/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class BT_MeasurementElement_r13 extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "BT-MeasurementElement-r13";
   }

   protected Asn1BitString btAddr_r13;
   protected Asn1Integer rssi_r13;  // optional
   protected Asn1OpenExt extElem1;

   public BT_MeasurementElement_r13 () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public BT_MeasurementElement_r13 (
      Asn1BitString btAddr_r13_,
      Asn1Integer rssi_r13_
   ) throws Asn1Exception {
      super();
      setBtAddr_r13 (btAddr_r13_);
      setRssi_r13 (rssi_r13_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public BT_MeasurementElement_r13 (
      Asn1BitString btAddr_r13_
   ) throws Asn1Exception {
      super();
      setBtAddr_r13 (btAddr_r13_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public BT_MeasurementElement_r13 (Asn1BitString btAddr_r13_,
      long rssi_r13_
   ) throws Asn1Exception {
      super();
      setBtAddr_r13 (btAddr_r13_);
      setRssi_r13 (rssi_r13_);
   }

   public void init () {
      btAddr_r13 = null;
      rssi_r13 = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof BT_MeasurementElement_r13) ) return false;

      BT_MeasurementElement_r13 rhs = (BT_MeasurementElement_r13) obj;

      if (btAddr_r13 == null) {
         if (rhs.btAddr_r13 != null) return false;
      }
      else {
         if (!btAddr_r13.equals(rhs.btAddr_r13)) {
            return false;
         }
      }

      if (rssi_r13 == null) {
         if (rhs.rssi_r13 != null) return false;
      }
      else {
         if (!rssi_r13.equals(rhs.rssi_r13)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (btAddr_r13 != null) __code = 31*__code + btAddr_r13.hashCode();
      if (rssi_r13 != null) __code = 31*__code + rssi_r13.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for BtAddr_r13
    */
   public Asn1BitString getBtAddr_r13 () {
      return btAddr_r13;
   }

   public void setBtAddr_r13 (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 48)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.btAddr_r13 = value;
   }

   /**
    * Accessor/mutator methods for Rssi_r13
    */
   public Asn1Integer getRssi_r13 () {
      return rssi_r13;
   }

   public void setRssi_r13 (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= -128 && value.value <= 127))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.rssi_r13 = value;
   }

   public void setRssi_r13 (long value) 
      throws Asn1Exception
   {
      setRssi_r13 (new Asn1Integer(value));
   }
   public boolean hasRssi_r13 () {
      return (rssi_r13 != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return btAddr_r13;
         case 1: return rssi_r13;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "btAddr-r13";
         case 1: return "rssi-r13";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean rssi_r13Present = buffer.decodeBit ("rssi_r13Present");

      // decode btAddr_r13

      buffer.getContext().eventDispatcher.startElement("btAddr_r13", -1);

      btAddr_r13 = new Asn1BitString();
      btAddr_r13.decode (buffer, 48, 48);
      buffer.getContext().eventDispatcher.endElement("btAddr_r13", -1);

      // decode rssi_r13

      if (rssi_r13Present) {
         buffer.getContext().eventDispatcher.startElement("rssi_r13", -1);

         rssi_r13 = new Asn1Integer();
         rssi_r13.decode (buffer, -128, 127);
         buffer.getContext().eventDispatcher.endElement("rssi_r13", -1);
      }
      else {
         rssi_r13 = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((rssi_r13 != null), null);

      // encode btAddr_r13

      if (btAddr_r13 != null) {
         buffer.getContext().eventDispatcher.startElement("btAddr_r13", -1);

         btAddr_r13.encode (buffer, 48, 48);

         buffer.getContext().eventDispatcher.endElement("btAddr_r13", -1);
      }
      else throw new Asn1MissingRequiredException ("btAddr_r13");

      // encode rssi_r13

      if (rssi_r13 != null) {
         buffer.getContext().eventDispatcher.startElement("rssi_r13", -1);

         rssi_r13.encode (buffer, -128, 127);

         buffer.getContext().eventDispatcher.endElement("rssi_r13", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
