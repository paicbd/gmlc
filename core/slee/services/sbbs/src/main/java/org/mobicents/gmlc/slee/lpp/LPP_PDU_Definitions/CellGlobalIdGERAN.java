/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class CellGlobalIdGERAN extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "CellGlobalIdGERAN";
   }

   protected CellGlobalIdGERAN_plmn_Identity plmn_Identity;
   protected Asn1BitString locationAreaCode;
   protected Asn1BitString cellIdentity;
   protected Asn1OpenExt extElem1;

   public CellGlobalIdGERAN () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public CellGlobalIdGERAN (
      CellGlobalIdGERAN_plmn_Identity plmn_Identity_,
      Asn1BitString locationAreaCode_,
      Asn1BitString cellIdentity_
   ) throws Asn1Exception {
      super();
      setPlmn_Identity (plmn_Identity_);
      setLocationAreaCode (locationAreaCode_);
      setCellIdentity (cellIdentity_);
   }

   public void init () {
      plmn_Identity = null;
      locationAreaCode = null;
      cellIdentity = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof CellGlobalIdGERAN) ) return false;

      CellGlobalIdGERAN rhs = (CellGlobalIdGERAN) obj;

      if (plmn_Identity == null) {
         if (rhs.plmn_Identity != null) return false;
      }
      else {
         if (!plmn_Identity.equals(rhs.plmn_Identity)) {
            return false;
         }
      }

      if (locationAreaCode == null) {
         if (rhs.locationAreaCode != null) return false;
      }
      else {
         if (!locationAreaCode.equals(rhs.locationAreaCode)) {
            return false;
         }
      }

      if (cellIdentity == null) {
         if (rhs.cellIdentity != null) return false;
      }
      else {
         if (!cellIdentity.equals(rhs.cellIdentity)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (plmn_Identity != null) __code = 31*__code + plmn_Identity.hashCode();
      if (locationAreaCode != null) __code = 31*__code + locationAreaCode.hashCode();
      if (cellIdentity != null) __code = 31*__code + cellIdentity.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Plmn_Identity
    */
   public CellGlobalIdGERAN_plmn_Identity getPlmn_Identity () {
      return plmn_Identity;
   }

   public void setPlmn_Identity (CellGlobalIdGERAN_plmn_Identity value) {
      this.plmn_Identity = value;
   }

   /**
    * Accessor/mutator methods for LocationAreaCode
    */
   public Asn1BitString getLocationAreaCode () {
      return locationAreaCode;
   }

   public void setLocationAreaCode (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 16)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.locationAreaCode = value;
   }

   /**
    * Accessor/mutator methods for CellIdentity
    */
   public Asn1BitString getCellIdentity () {
      return cellIdentity;
   }

   public void setCellIdentity (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 16)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.cellIdentity = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 4; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return plmn_Identity;
         case 1: return locationAreaCode;
         case 2: return cellIdentity;
         case 3: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "plmn-Identity";
         case 1: return "locationAreaCode";
         case 2: return "cellIdentity";
         case 3: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode plmn_Identity

      buffer.getContext().eventDispatcher.startElement("plmn_Identity", -1);

      plmn_Identity = new CellGlobalIdGERAN_plmn_Identity();
      plmn_Identity.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("plmn_Identity", -1);

      // decode locationAreaCode

      buffer.getContext().eventDispatcher.startElement("locationAreaCode", -1);

      locationAreaCode = new Asn1BitString();
      locationAreaCode.decode (buffer, 16, 16);
      buffer.getContext().eventDispatcher.endElement("locationAreaCode", -1);

      // decode cellIdentity

      buffer.getContext().eventDispatcher.startElement("cellIdentity", -1);

      cellIdentity = new Asn1BitString();
      cellIdentity.decode (buffer, 16, 16);
      buffer.getContext().eventDispatcher.endElement("cellIdentity", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode plmn_Identity

      if (plmn_Identity != null) {
         buffer.getContext().eventDispatcher.startElement("plmn_Identity", -1);

         plmn_Identity.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("plmn_Identity", -1);
      }
      else throw new Asn1MissingRequiredException ("plmn_Identity");

      // encode locationAreaCode

      if (locationAreaCode != null) {
         buffer.getContext().eventDispatcher.startElement("locationAreaCode", -1);

         locationAreaCode.encode (buffer, 16, 16);

         buffer.getContext().eventDispatcher.endElement("locationAreaCode", -1);
      }
      else throw new Asn1MissingRequiredException ("locationAreaCode");

      // encode cellIdentity

      if (cellIdentity != null) {
         buffer.getContext().eventDispatcher.startElement("cellIdentity", -1);

         cellIdentity.encode (buffer, 16, 16);

         buffer.getContext().eventDispatcher.endElement("cellIdentity", -1);
      }
      else throw new Asn1MissingRequiredException ("cellIdentity");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
