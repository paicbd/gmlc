/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class GNSS_SSR_PhaseBiasSupport_r16 extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "GNSS-SSR-PhaseBiasSupport-r16";
   }

   protected GNSS_SignalIDs signal_and_tracking_mode_ID_Sup_r16;
   protected Asn1OpenExt extElem1;

   public GNSS_SSR_PhaseBiasSupport_r16 () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public GNSS_SSR_PhaseBiasSupport_r16 (
      GNSS_SignalIDs signal_and_tracking_mode_ID_Sup_r16_
   ) {
      super();
      setSignal_and_tracking_mode_ID_Sup_r16 (signal_and_tracking_mode_ID_Sup_r16_);
   }

   public void init () {
      signal_and_tracking_mode_ID_Sup_r16 = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof GNSS_SSR_PhaseBiasSupport_r16) ) return false;

      GNSS_SSR_PhaseBiasSupport_r16 rhs = (GNSS_SSR_PhaseBiasSupport_r16) obj;

      if (signal_and_tracking_mode_ID_Sup_r16 == null) {
         if (rhs.signal_and_tracking_mode_ID_Sup_r16 != null) return false;
      }
      else {
         if (!signal_and_tracking_mode_ID_Sup_r16.equals(rhs.signal_and_tracking_mode_ID_Sup_r16)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (signal_and_tracking_mode_ID_Sup_r16 != null) __code = 31*__code + signal_and_tracking_mode_ID_Sup_r16.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Signal_and_tracking_mode_ID_Sup_r16
    */
   public GNSS_SignalIDs getSignal_and_tracking_mode_ID_Sup_r16 () {
      return signal_and_tracking_mode_ID_Sup_r16;
   }

   public void setSignal_and_tracking_mode_ID_Sup_r16 (GNSS_SignalIDs value) {
      this.signal_and_tracking_mode_ID_Sup_r16 = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 2; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return signal_and_tracking_mode_ID_Sup_r16;
         case 1: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "signal-and-tracking-mode-ID-Sup-r16";
         case 1: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode signal_and_tracking_mode_ID_Sup_r16

      buffer.getContext().eventDispatcher.startElement("signal_and_tracking_mode_ID_Sup_r16", -1);

      signal_and_tracking_mode_ID_Sup_r16 = new GNSS_SignalIDs();
      signal_and_tracking_mode_ID_Sup_r16.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("signal_and_tracking_mode_ID_Sup_r16", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode signal_and_tracking_mode_ID_Sup_r16

      if (signal_and_tracking_mode_ID_Sup_r16 != null) {
         buffer.getContext().eventDispatcher.startElement("signal_and_tracking_mode_ID_Sup_r16", -1);

         signal_and_tracking_mode_ID_Sup_r16.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("signal_and_tracking_mode_ID_Sup_r16", -1);
      }
      else throw new Asn1MissingRequiredException ("signal_and_tracking_mode_ID_Sup_r16");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
