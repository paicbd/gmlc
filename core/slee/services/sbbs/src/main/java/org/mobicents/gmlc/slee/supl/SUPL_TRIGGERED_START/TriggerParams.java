/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.SUPL_TRIGGERED_START;

import com.objsys.asn1j.runtime.*;

public class TriggerParams extends Asn1Choice {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_SUPL_TRIGGERED_STARTRtkey._rtkey);
      Asn1Type._setLicLocation(_SUPL_TRIGGERED_STARTRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "TriggerParams";
   }

   // Choice element identifier constants
   public final static byte _PERIODICPARAMS = 1;
   public final static byte _AREAEVENTPARAMS = 2;
   public final static byte _EXTELEM1 = 3;

   public TriggerParams () {
      super();
   }

   public TriggerParams (byte choiceId_, Asn1Type element_) {
      super();
      setElement (choiceId_, element_);
   }

   public String getElemName () {
      switch (choiceID) {
      case _PERIODICPARAMS: return "periodicParams";
      case _AREAEVENTPARAMS: return "areaEventParams";
      case _EXTELEM1: return "...";
      default: return "UNDEFINED";
      }
   }

   /**
    * Accessor/mutator methods for periodicParams
    */
   public PeriodicParams getPeriodicParams () {
      return (choiceID == _PERIODICPARAMS) ? (PeriodicParams)element : null;
   }
   public void setPeriodicParams (PeriodicParams value) {
      setElement (_PERIODICPARAMS, value);
   }
   public boolean isPeriodicParams () {
      return (choiceID == _PERIODICPARAMS);
   }

   /**
    * Accessor/mutator methods for areaEventParams
    */
   public AreaEventParams getAreaEventParams () {
      return (choiceID == _AREAEVENTPARAMS) ? (AreaEventParams)element : null;
   }
   public void setAreaEventParams (AreaEventParams value) {
      setElement (_AREAEVENTPARAMS, value);
   }
   public boolean isAreaEventParams () {
      return (choiceID == _AREAEVENTPARAMS);
   }

   /**
    * Accessor/mutator methods for extElem1
    */
   public Asn1OpenType getExtElem1 () {
      return (choiceID == _EXTELEM1) ? (Asn1OpenType)element : null;
   }
   public void setExtElem1 (Asn1OpenType value) {
      setElement (_EXTELEM1, value);
   }
   public boolean isExtElem1 () {
      return (choiceID == _EXTELEM1);
   }

   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      Asn1PerDecodeBuffer savedBuffer = null;
      boolean extbit = buffer.decodeBit ("extbit");

      if (!extbit) {
         int idx = (int) buffer.decodeConsWholeNumber (2, "index");
         choiceID = idx + 1;
         buffer.getContext().eventDispatcher.startElement (getElemName(), -1);

         switch (idx) {
            // periodicParams
            case 0: { 
               PeriodicParams periodicParams;
               element = periodicParams = new PeriodicParams();
               periodicParams.decode (buffer);
               break;
            }

            // areaEventParams
            case 1: { 
               AreaEventParams areaEventParams;
               element = areaEventParams = new AreaEventParams();
               areaEventParams.decode (buffer);
               break;
            }

            default:
            throw new Asn1InvalidChoiceOptionException (buffer, idx);
         }
      }
      else {
         int idx = (int) buffer.decodeSmallNonNegWholeNumber ();
         choiceID = idx + 3;
         buffer.getContext().eventDispatcher.startElement (getElemName(), -1);

         buffer.byteAlign();

         Asn1OpenType openType = new Asn1OpenType ();
         openType.decode (buffer);

         setElement (_EXTELEM1, openType);
      }

      buffer.getContext().eventDispatcher.endElement (getElemName(), -1);

      if (savedBuffer != null) {
         buffer = savedBuffer;
      }
   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (choiceID > 2);
      buffer.encodeBit (extbit, "extbit");

      if (!extbit) {

         // Encode choice index value

         buffer.encodeConsWholeNumber (choiceID - 1, 2, "index");

         buffer.getContext().eventDispatcher.startElement("getElemName()", -1);

         switch (choiceID) {
            // periodicParams
            case _PERIODICPARAMS:
               PeriodicParams periodicParams = (PeriodicParams) element;
               periodicParams.encode (buffer);
               break;

            // areaEventParams
            case _AREAEVENTPARAMS:
               AreaEventParams areaEventParams = (AreaEventParams) element;
               areaEventParams.encode (buffer);
               break;

            default:
            throw new Asn1InvalidChoiceOptionException();
         }
      }
      else {
         // Encode extension choice index value

         buffer.encodeSmallNonNegWholeNumber (choiceID - 3);

         // Encode extension element data value

         buffer.getContext().eventDispatcher.startElement("getElemName()", -1);

         Asn1OpenType openType = (Asn1OpenType) element;
         buffer.byteAlign();
         openType.encode (buffer);
      }

      buffer.getContext().eventDispatcher.endElement("getElemName()", -1);
   }

}
