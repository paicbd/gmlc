/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.Ver2_ULP_Components;

import com.objsys.asn1j.runtime.*;

public class NRCellInformation extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_Ver2_ULP_ComponentsRtkey._rtkey);
      Asn1Type._setLicLocation(_Ver2_ULP_ComponentsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "NRCellInformation";
   }

   protected ServingCellInformationNR servingCellInformation;
   protected MeasResultListNR measuredResultsListNR;  // optional
   protected Asn1OpenExt extElem1;

   public NRCellInformation () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public NRCellInformation (
      ServingCellInformationNR servingCellInformation_,
      MeasResultListNR measuredResultsListNR_
   ) throws Asn1Exception {
      super();
      setServingCellInformation (servingCellInformation_);
      setMeasuredResultsListNR (measuredResultsListNR_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public NRCellInformation (
      ServingCellInformationNR servingCellInformation_
   ) throws Asn1Exception {
      super();
      setServingCellInformation (servingCellInformation_);
   }

   public void init () {
      servingCellInformation = null;
      measuredResultsListNR = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof NRCellInformation) ) return false;

      NRCellInformation rhs = (NRCellInformation) obj;

      if (servingCellInformation == null) {
         if (rhs.servingCellInformation != null) return false;
      }
      else {
         if (!servingCellInformation.equals(rhs.servingCellInformation)) {
            return false;
         }
      }

      if (measuredResultsListNR == null) {
         if (rhs.measuredResultsListNR != null) return false;
      }
      else {
         if (!measuredResultsListNR.equals(rhs.measuredResultsListNR)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (servingCellInformation != null) __code = 31*__code + servingCellInformation.hashCode();
      if (measuredResultsListNR != null) __code = 31*__code + measuredResultsListNR.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for ServingCellInformation
    */
   public ServingCellInformationNR getServingCellInformation () {
      return servingCellInformation;
   }

   public void setServingCellInformation (ServingCellInformationNR value) 
      throws Asn1Exception
   {
      if (!((value.getElements().length >= 1 && value.getElements().length <= 32))) {
         throw new Asn1ConsVioException ("value.getElements().length", value.getElements().length);
      }

      this.servingCellInformation = value;
   }

   /**
    * Accessor/mutator methods for MeasuredResultsListNR
    */
   public MeasResultListNR getMeasuredResultsListNR () {
      return measuredResultsListNR;
   }

   public void setMeasuredResultsListNR (MeasResultListNR value) 
      throws Asn1Exception
   {
      if (!((value.getElements().length >= 1 && value.getElements().length <= 32))) {
         throw new Asn1ConsVioException ("value.getElements().length", value.getElements().length);
      }

      this.measuredResultsListNR = value;
   }

   public boolean hasMeasuredResultsListNR () {
      return (measuredResultsListNR != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return servingCellInformation;
         case 1: return measuredResultsListNR;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "servingCellInformation";
         case 1: return "measuredResultsListNR";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean measuredResultsListNRPresent = buffer.decodeBit ("measuredResultsListNRPresent");

      // decode servingCellInformation

      buffer.getContext().eventDispatcher.startElement("servingCellInformation", -1);

      servingCellInformation = new ServingCellInformationNR();
      servingCellInformation.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("servingCellInformation", -1);

      // decode measuredResultsListNR

      if (measuredResultsListNRPresent) {
         buffer.getContext().eventDispatcher.startElement("measuredResultsListNR", -1);

         measuredResultsListNR = new MeasResultListNR();
         measuredResultsListNR.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("measuredResultsListNR", -1);
      }
      else {
         measuredResultsListNR = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((measuredResultsListNR != null), null);

      // encode servingCellInformation

      if (servingCellInformation != null) {
         buffer.getContext().eventDispatcher.startElement("servingCellInformation", -1);

         servingCellInformation.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("servingCellInformation", -1);
      }
      else throw new Asn1MissingRequiredException ("servingCellInformation");

      // encode measuredResultsListNR

      if (measuredResultsListNR != null) {
         buffer.getContext().eventDispatcher.startElement("measuredResultsListNR", -1);

         measuredResultsListNR.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("measuredResultsListNR", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
