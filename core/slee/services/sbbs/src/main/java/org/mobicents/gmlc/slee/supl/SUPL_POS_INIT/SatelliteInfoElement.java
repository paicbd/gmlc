/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.SUPL_POS_INIT;

import com.objsys.asn1j.runtime.*;

public class SatelliteInfoElement extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_SUPL_POS_INITRtkey._rtkey);
      Asn1Type._setLicLocation(_SUPL_POS_INITRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "SatelliteInfoElement";
   }

   protected Asn1Integer satId;
   protected Asn1Integer iode;
   protected Asn1OpenExt extElem1;

   public SatelliteInfoElement () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public SatelliteInfoElement (
      Asn1Integer satId_,
      Asn1Integer iode_
   ) throws Asn1Exception {
      super();
      setSatId (satId_);
      setIode (iode_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public SatelliteInfoElement (long satId_,
      long iode_
   ) throws Asn1Exception {
      super();
      setSatId (satId_);
      setIode (iode_);
   }

   public void init () {
      satId = null;
      iode = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof SatelliteInfoElement) ) return false;

      SatelliteInfoElement rhs = (SatelliteInfoElement) obj;

      if (satId == null) {
         if (rhs.satId != null) return false;
      }
      else {
         if (!satId.equals(rhs.satId)) {
            return false;
         }
      }

      if (iode == null) {
         if (rhs.iode != null) return false;
      }
      else {
         if (!iode.equals(rhs.iode)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (satId != null) __code = 31*__code + satId.hashCode();
      if (iode != null) __code = 31*__code + iode.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for SatId
    */
   public Asn1Integer getSatId () {
      return satId;
   }

   public void setSatId (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 63))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.satId = value;
   }

   public void setSatId (long value) 
      throws Asn1Exception
   {
      setSatId (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for Iode
    */
   public Asn1Integer getIode () {
      return iode;
   }

   public void setIode (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 255))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.iode = value;
   }

   public void setIode (long value) 
      throws Asn1Exception
   {
      setIode (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return satId;
         case 1: return iode;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "satId";
         case 1: return "iode";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode satId

      buffer.getContext().eventDispatcher.startElement("satId", -1);

      satId = new Asn1Integer();
      satId.decode (buffer, 0, 63);
      buffer.getContext().eventDispatcher.endElement("satId", -1);

      // decode iode

      buffer.getContext().eventDispatcher.startElement("iode", -1);

      iode = new Asn1Integer();
      iode.decode (buffer, 0, 255);
      buffer.getContext().eventDispatcher.endElement("iode", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode satId

      if (satId != null) {
         buffer.getContext().eventDispatcher.startElement("satId", -1);

         satId.encode (buffer, 0, 63);

         buffer.getContext().eventDispatcher.endElement("satId", -1);
      }
      else throw new Asn1MissingRequiredException ("satId");

      // encode iode

      if (iode != null) {
         buffer.getContext().eventDispatcher.startElement("iode", -1);

         iode.encode (buffer, 0, 255);

         buffer.getContext().eventDispatcher.endElement("iode", -1);
      }
      else throw new Asn1MissingRequiredException ("iode");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
