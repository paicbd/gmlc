/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.Ver2_ULP_Components;

import com.objsys.asn1j.runtime.*;

public class LocationData extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_Ver2_ULP_ComponentsRtkey._rtkey);
      Asn1Type._setLicLocation(_Ver2_ULP_ComponentsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "LocationData";
   }

   protected Asn1Integer locationAccuracy;  // optional
   protected Asn1OctetString locationValue;
   protected Asn1OpenExt extElem1;

   public LocationData () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public LocationData (
      Asn1Integer locationAccuracy_,
      Asn1OctetString locationValue_
   ) throws Asn1Exception {
      super();
      setLocationAccuracy (locationAccuracy_);
      setLocationValue (locationValue_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public LocationData (
      Asn1OctetString locationValue_
   ) throws Asn1Exception {
      super();
      setLocationValue (locationValue_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public LocationData (long locationAccuracy_,
      byte[] locationValue_
   ) throws Asn1Exception {
      super();
      setLocationAccuracy (locationAccuracy_);
      setLocationValue (locationValue_);
   }

   /**
    * This constructor is for required elements only.  It allows 
    * primitive data to be passed for all primitive elements.  
    * It will create new object wrappers for the primitive data 
    * and set other elements to references to the given objects. 
    */
   public LocationData (
      byte[] locationValue_
   ) throws Asn1Exception {
      super();
      setLocationValue (locationValue_);
   }

   public void init () {
      locationAccuracy = null;
      locationValue = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof LocationData) ) return false;

      LocationData rhs = (LocationData) obj;

      if (locationAccuracy == null) {
         if (rhs.locationAccuracy != null) return false;
      }
      else {
         if (!locationAccuracy.equals(rhs.locationAccuracy)) {
            return false;
         }
      }

      if (locationValue == null) {
         if (rhs.locationValue != null) return false;
      }
      else {
         if (!locationValue.equals(rhs.locationValue)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (locationAccuracy != null) __code = 31*__code + locationAccuracy.hashCode();
      if (locationValue != null) __code = 31*__code + locationValue.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for LocationAccuracy
    */
   public Asn1Integer getLocationAccuracy () {
      return locationAccuracy;
   }

   public void setLocationAccuracy (Asn1Integer value) 
      throws Asn1Exception
   {
      this.locationAccuracy = value;
   }

   public void setLocationAccuracy (long value) 
      throws Asn1Exception
   {
      setLocationAccuracy (new Asn1Integer(value));
   }
   public boolean hasLocationAccuracy () {
      return (locationAccuracy != null);
   }

   /**
    * Accessor/mutator methods for LocationValue
    */
   public Asn1OctetString getLocationValue () {
      return locationValue;
   }

   public void setLocationValue (Asn1OctetString value) 
      throws Asn1Exception
   {
      if (!((value.getLength() >= 1 && value.getLength() <= 128))) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.locationValue = value;
   }

   public void setLocationValue (byte[] value) 
      throws Asn1Exception
   {
      setLocationValue (new Asn1OctetString(value));
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return locationAccuracy;
         case 1: return locationValue;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "locationAccuracy";
         case 1: return "locationValue";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean locationAccuracyPresent = buffer.decodeBit ("locationAccuracyPresent");

      // decode locationAccuracy

      if (locationAccuracyPresent) {
         buffer.getContext().eventDispatcher.startElement("locationAccuracy", -1);

         locationAccuracy = new Asn1Integer();
         locationAccuracy.decode (buffer, 0, 4294967295L);
         buffer.getContext().eventDispatcher.endElement("locationAccuracy", -1);
      }
      else {
         locationAccuracy = null;
      }

      // decode locationValue

      buffer.getContext().eventDispatcher.startElement("locationValue", -1);

      locationValue = new Asn1OctetString();
      locationValue.decode (buffer, 1, 128);
      buffer.getContext().eventDispatcher.endElement("locationValue", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((locationAccuracy != null), null);

      // encode locationAccuracy

      if (locationAccuracy != null) {
         buffer.getContext().eventDispatcher.startElement("locationAccuracy", -1);

         locationAccuracy.encode (buffer, 0, 4294967295L);

         buffer.getContext().eventDispatcher.endElement("locationAccuracy", -1);
      }

      // encode locationValue

      if (locationValue != null) {
         buffer.getContext().eventDispatcher.startElement("locationValue", -1);

         locationValue.encode (buffer, 1, 128);

         buffer.getContext().eventDispatcher.endElement("locationValue", -1);
      }
      else throw new Asn1MissingRequiredException ("locationValue");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
