/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class CarrierFreq_NB_r14 extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "CarrierFreq-NB-r14";
   }

   protected ARFCN_ValueEUTRA_r14 carrierFreq_r14;
   protected CarrierFreqOffsetNB_r14 carrierFreqOffset_r14 = null;  // optional
   protected Asn1OpenExt extElem1;

   public CarrierFreq_NB_r14 () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public CarrierFreq_NB_r14 (
      ARFCN_ValueEUTRA_r14 carrierFreq_r14_,
      CarrierFreqOffsetNB_r14 carrierFreqOffset_r14_
   ) throws Asn1Exception {
      super();
      setCarrierFreq_r14 (carrierFreq_r14_);
      setCarrierFreqOffset_r14 (carrierFreqOffset_r14_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public CarrierFreq_NB_r14 (
      ARFCN_ValueEUTRA_r14 carrierFreq_r14_
   ) throws Asn1Exception {
      super();
      setCarrierFreq_r14 (carrierFreq_r14_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public CarrierFreq_NB_r14 (long carrierFreq_r14_,
      CarrierFreqOffsetNB_r14 carrierFreqOffset_r14_
   ) throws Asn1Exception {
      super();
      setCarrierFreq_r14 (carrierFreq_r14_);
      setCarrierFreqOffset_r14 (carrierFreqOffset_r14_);
   }

   /**
    * This constructor is for required elements only.  It allows 
    * primitive data to be passed for all primitive elements.  
    * It will create new object wrappers for the primitive data 
    * and set other elements to references to the given objects. 
    */
   public CarrierFreq_NB_r14 (
      long carrierFreq_r14_
   ) throws Asn1Exception {
      super();
      setCarrierFreq_r14 (carrierFreq_r14_);
   }

   public void init () {
      carrierFreq_r14 = null;
      carrierFreqOffset_r14 = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof CarrierFreq_NB_r14) ) return false;

      CarrierFreq_NB_r14 rhs = (CarrierFreq_NB_r14) obj;

      if (carrierFreq_r14 == null) {
         if (rhs.carrierFreq_r14 != null) return false;
      }
      else {
         if (!carrierFreq_r14.equals(rhs.carrierFreq_r14)) {
            return false;
         }
      }

      if (carrierFreqOffset_r14 == null) {
         if (rhs.carrierFreqOffset_r14 != null) return false;
      }
      else {
         if (!carrierFreqOffset_r14.equals(rhs.carrierFreqOffset_r14)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (carrierFreq_r14 != null) __code = 31*__code + carrierFreq_r14.hashCode();
      if (carrierFreqOffset_r14 != null) __code = 31*__code + carrierFreqOffset_r14.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for CarrierFreq_r14
    */
   public ARFCN_ValueEUTRA_r14 getCarrierFreq_r14 () {
      return carrierFreq_r14;
   }

   public void setCarrierFreq_r14 (ARFCN_ValueEUTRA_r14 value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 262143))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.carrierFreq_r14 = value;
   }

   public void setCarrierFreq_r14 (long value) 
      throws Asn1Exception
   {
      setCarrierFreq_r14 (new ARFCN_ValueEUTRA_r14(value));
   }

   /**
    * Accessor/mutator methods for CarrierFreqOffset_r14
    */
   public CarrierFreqOffsetNB_r14 getCarrierFreqOffset_r14 () {
      return carrierFreqOffset_r14;
   }

   public void setCarrierFreqOffset_r14 (CarrierFreqOffsetNB_r14 value) {
      this.carrierFreqOffset_r14 = value;
   }

   public boolean hasCarrierFreqOffset_r14 () {
      return (carrierFreqOffset_r14 != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return carrierFreq_r14;
         case 1: return carrierFreqOffset_r14;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "carrierFreq-r14";
         case 1: return "carrierFreqOffset-r14";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean carrierFreqOffset_r14Present = buffer.decodeBit ("carrierFreqOffset_r14Present");

      // decode carrierFreq_r14

      buffer.getContext().eventDispatcher.startElement("carrierFreq_r14", -1);

      carrierFreq_r14 = new ARFCN_ValueEUTRA_r14();
      carrierFreq_r14.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("carrierFreq_r14", -1);

      // decode carrierFreqOffset_r14

      if (carrierFreqOffset_r14Present) {
         buffer.getContext().eventDispatcher.startElement("carrierFreqOffset_r14", -1);

         int tval = CarrierFreqOffsetNB_r14.decodeEnumValue (buffer);
         carrierFreqOffset_r14 = CarrierFreqOffsetNB_r14.valueOf (tval);
         buffer.getContext().eventDispatcher.endElement("carrierFreqOffset_r14", -1);
      }
      else {
         carrierFreqOffset_r14 = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((carrierFreqOffset_r14 != null), null);

      // encode carrierFreq_r14

      if (carrierFreq_r14 != null) {
         buffer.getContext().eventDispatcher.startElement("carrierFreq_r14", -1);

         carrierFreq_r14.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("carrierFreq_r14", -1);
      }
      else throw new Asn1MissingRequiredException ("carrierFreq_r14");

      // encode carrierFreqOffset_r14

      if (carrierFreqOffset_r14 != null) {
         buffer.getContext().eventDispatcher.startElement("carrierFreqOffset_r14", -1);

         carrierFreqOffset_r14.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("carrierFreqOffset_r14", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
