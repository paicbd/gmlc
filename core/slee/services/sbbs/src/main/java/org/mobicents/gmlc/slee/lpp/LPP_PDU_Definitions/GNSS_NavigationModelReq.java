/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class GNSS_NavigationModelReq extends Asn1Choice {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "GNSS-NavigationModelReq";
   }

   // Choice element identifier constants
   public final static byte _STOREDNAVLIST = 1;
   public final static byte _REQNAVLIST = 2;
   public final static byte _EXTELEM1 = 3;

   public GNSS_NavigationModelReq () {
      super();
   }

   public GNSS_NavigationModelReq (byte choiceId_, Asn1Type element_) {
      super();
      setElement (choiceId_, element_);
   }

   public String getElemName () {
      switch (choiceID) {
      case _STOREDNAVLIST: return "storedNavList";
      case _REQNAVLIST: return "reqNavList";
      case _EXTELEM1: return "...";
      default: return "UNDEFINED";
      }
   }

   /**
    * Accessor/mutator methods for storedNavList
    */
   public StoredNavListInfo getStoredNavList () {
      return (choiceID == _STOREDNAVLIST) ? (StoredNavListInfo)element : null;
   }
   public void setStoredNavList (StoredNavListInfo value) {
      setElement (_STOREDNAVLIST, value);
   }
   public boolean isStoredNavList () {
      return (choiceID == _STOREDNAVLIST);
   }

   /**
    * Accessor/mutator methods for reqNavList
    */
   public ReqNavListInfo getReqNavList () {
      return (choiceID == _REQNAVLIST) ? (ReqNavListInfo)element : null;
   }
   public void setReqNavList (ReqNavListInfo value) {
      setElement (_REQNAVLIST, value);
   }
   public boolean isReqNavList () {
      return (choiceID == _REQNAVLIST);
   }

   /**
    * Accessor/mutator methods for extElem1
    */
   public Asn1OpenType getExtElem1 () {
      return (choiceID == _EXTELEM1) ? (Asn1OpenType)element : null;
   }
   public void setExtElem1 (Asn1OpenType value) {
      setElement (_EXTELEM1, value);
   }
   public boolean isExtElem1 () {
      return (choiceID == _EXTELEM1);
   }

   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      Asn1PerDecodeBuffer savedBuffer = null;
      boolean extbit = buffer.decodeBit ("extbit");

      if (!extbit) {
         int idx = (int) buffer.decodeConsWholeNumber (2, "index");
         choiceID = idx + 1;
         buffer.getContext().eventDispatcher.startElement (getElemName(), -1);

         switch (idx) {
            // storedNavList
            case 0: { 
               StoredNavListInfo storedNavList;
               element = storedNavList = new StoredNavListInfo();
               storedNavList.decode (buffer);
               break;
            }

            // reqNavList
            case 1: { 
               ReqNavListInfo reqNavList;
               element = reqNavList = new ReqNavListInfo();
               reqNavList.decode (buffer);
               break;
            }

            default:
            throw new Asn1InvalidChoiceOptionException (buffer, idx);
         }
      }
      else {
         int idx = (int) buffer.decodeSmallNonNegWholeNumber ();
         choiceID = idx + 3;
         buffer.getContext().eventDispatcher.startElement (getElemName(), -1);

         buffer.byteAlign();

         Asn1OpenType openType = new Asn1OpenType ();
         openType.decode (buffer);

         setElement (_EXTELEM1, openType);
      }

      buffer.getContext().eventDispatcher.endElement (getElemName(), -1);

      if (savedBuffer != null) {
         buffer = savedBuffer;
      }
   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (choiceID > 2);
      buffer.encodeBit (extbit, "extbit");

      if (!extbit) {

         // Encode choice index value

         buffer.encodeConsWholeNumber (choiceID - 1, 2, "index");

         buffer.getContext().eventDispatcher.startElement("getElemName()", -1);

         switch (choiceID) {
            // storedNavList
            case _STOREDNAVLIST:
               StoredNavListInfo storedNavList = (StoredNavListInfo) element;
               storedNavList.encode (buffer);
               break;

            // reqNavList
            case _REQNAVLIST:
               ReqNavListInfo reqNavList = (ReqNavListInfo) element;
               reqNavList.encode (buffer);
               break;

            default:
            throw new Asn1InvalidChoiceOptionException();
         }
      }
      else {
         // Encode extension choice index value

         buffer.encodeSmallNonNegWholeNumber (choiceID - 3);

         // Encode extension element data value

         buffer.getContext().eventDispatcher.startElement("getElemName()", -1);

         Asn1OpenType openType = (Asn1OpenType) element;
         buffer.byteAlign();
         openType.encode (buffer);
      }

      buffer.getContext().eventDispatcher.endElement("getElemName()", -1);
   }

}
