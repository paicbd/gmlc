/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class OTDOA_RequestLocationInformation extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "OTDOA-RequestLocationInformation";
   }

   protected Asn1Boolean assistanceAvailability;
   protected OTDOA_RequestLocationInformation_multipathRSTD_r14 multipathRSTD_r14 = null;  // optional
   protected Asn1Integer maxNoOfRSTDmeas_r14;  // optional
   protected OTDOA_RequestLocationInformation_motionMeasurements_r15 motionMeasurements_r15 = null;  // optional
   protected Asn1OpenExt extElem1;

   public OTDOA_RequestLocationInformation () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public OTDOA_RequestLocationInformation (
      Asn1Boolean assistanceAvailability_,
      OTDOA_RequestLocationInformation_multipathRSTD_r14 multipathRSTD_r14_,
      Asn1Integer maxNoOfRSTDmeas_r14_,
      OTDOA_RequestLocationInformation_motionMeasurements_r15 motionMeasurements_r15_
   ) throws Asn1Exception {
      super();
      setAssistanceAvailability (assistanceAvailability_);
      setMultipathRSTD_r14 (multipathRSTD_r14_);
      setMaxNoOfRSTDmeas_r14 (maxNoOfRSTDmeas_r14_);
      setMotionMeasurements_r15 (motionMeasurements_r15_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public OTDOA_RequestLocationInformation (
      Asn1Boolean assistanceAvailability_
   ) throws Asn1Exception {
      super();
      setAssistanceAvailability (assistanceAvailability_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public OTDOA_RequestLocationInformation (boolean assistanceAvailability_,
      OTDOA_RequestLocationInformation_multipathRSTD_r14 multipathRSTD_r14_,
      long maxNoOfRSTDmeas_r14_,
      OTDOA_RequestLocationInformation_motionMeasurements_r15 motionMeasurements_r15_
   ) throws Asn1Exception {
      super();
      setAssistanceAvailability (assistanceAvailability_);
      setMultipathRSTD_r14 (multipathRSTD_r14_);
      setMaxNoOfRSTDmeas_r14 (maxNoOfRSTDmeas_r14_);
      setMotionMeasurements_r15 (motionMeasurements_r15_);
   }

   /**
    * This constructor is for required elements only.  It allows 
    * primitive data to be passed for all primitive elements.  
    * It will create new object wrappers for the primitive data 
    * and set other elements to references to the given objects. 
    */
   public OTDOA_RequestLocationInformation (
      boolean assistanceAvailability_
   ) throws Asn1Exception {
      super();
      setAssistanceAvailability (assistanceAvailability_);
   }

   public void init () {
      assistanceAvailability = null;
      multipathRSTD_r14 = null;
      maxNoOfRSTDmeas_r14 = null;
      motionMeasurements_r15 = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof OTDOA_RequestLocationInformation) ) return false;

      OTDOA_RequestLocationInformation rhs = (OTDOA_RequestLocationInformation) obj;

      if (assistanceAvailability == null) {
         if (rhs.assistanceAvailability != null) return false;
      }
      else {
         if (!assistanceAvailability.equals(rhs.assistanceAvailability)) {
            return false;
         }
      }

      if (multipathRSTD_r14 == null) {
         if (rhs.multipathRSTD_r14 != null) return false;
      }
      else {
         if (!multipathRSTD_r14.equals(rhs.multipathRSTD_r14)) {
            return false;
         }
      }

      if (maxNoOfRSTDmeas_r14 == null) {
         if (rhs.maxNoOfRSTDmeas_r14 != null) return false;
      }
      else {
         if (!maxNoOfRSTDmeas_r14.equals(rhs.maxNoOfRSTDmeas_r14)) {
            return false;
         }
      }

      if (motionMeasurements_r15 == null) {
         if (rhs.motionMeasurements_r15 != null) return false;
      }
      else {
         if (!motionMeasurements_r15.equals(rhs.motionMeasurements_r15)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (assistanceAvailability != null) __code = 31*__code + assistanceAvailability.hashCode();
      if (multipathRSTD_r14 != null) __code = 31*__code + multipathRSTD_r14.hashCode();
      if (maxNoOfRSTDmeas_r14 != null) __code = 31*__code + maxNoOfRSTDmeas_r14.hashCode();
      if (motionMeasurements_r15 != null) __code = 31*__code + motionMeasurements_r15.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for AssistanceAvailability
    */
   public Asn1Boolean getAssistanceAvailability () {
      return assistanceAvailability;
   }

   public void setAssistanceAvailability (Asn1Boolean value) {
      this.assistanceAvailability = value;
   }

   public void setAssistanceAvailability (boolean value) {
      setAssistanceAvailability (new Asn1Boolean(value));
   }

   /**
    * Accessor/mutator methods for MultipathRSTD_r14
    */
   public OTDOA_RequestLocationInformation_multipathRSTD_r14 getMultipathRSTD_r14 () {
      return multipathRSTD_r14;
   }

   public void setMultipathRSTD_r14 (OTDOA_RequestLocationInformation_multipathRSTD_r14 value) {
      this.multipathRSTD_r14 = value;
   }

   public boolean hasMultipathRSTD_r14 () {
      return (multipathRSTD_r14 != null);
   }

   /**
    * Accessor/mutator methods for MaxNoOfRSTDmeas_r14
    */
   public Asn1Integer getMaxNoOfRSTDmeas_r14 () {
      return maxNoOfRSTDmeas_r14;
   }

   public void setMaxNoOfRSTDmeas_r14 (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 1 && value.value <= 32))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.maxNoOfRSTDmeas_r14 = value;
   }

   public void setMaxNoOfRSTDmeas_r14 (long value) 
      throws Asn1Exception
   {
      setMaxNoOfRSTDmeas_r14 (new Asn1Integer(value));
   }
   public boolean hasMaxNoOfRSTDmeas_r14 () {
      return (maxNoOfRSTDmeas_r14 != null);
   }

   /**
    * Accessor/mutator methods for MotionMeasurements_r15
    */
   public OTDOA_RequestLocationInformation_motionMeasurements_r15 getMotionMeasurements_r15 () {
      return motionMeasurements_r15;
   }

   public void setMotionMeasurements_r15 (OTDOA_RequestLocationInformation_motionMeasurements_r15 value) {
      this.motionMeasurements_r15 = value;
   }

   public boolean hasMotionMeasurements_r15 () {
      return (motionMeasurements_r15 != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 5; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return assistanceAvailability;
         case 1: return multipathRSTD_r14;
         case 2: return maxNoOfRSTDmeas_r14;
         case 3: return motionMeasurements_r15;
         case 4: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "assistanceAvailability";
         case 1: return "multipathRSTD-r14";
         case 2: return "maxNoOfRSTDmeas-r14";
         case 3: return "motionMeasurements-r15";
         case 4: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      // decode assistanceAvailability

      buffer.getContext().eventDispatcher.startElement("assistanceAvailability", -1);

      assistanceAvailability = new Asn1Boolean();
      assistanceAvailability.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("assistanceAvailability", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("multipathRSTD_r14Present");
         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("maxNoOfRSTDmeas_r14Present");
         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("motionMeasurements_r15Present");

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode multipathRSTD_r14

         if (i < bitcnt && bitmap[i++]) {
            buffer.getContext().eventDispatcher.startElement("multipathRSTD_r14", -1);

            long _curidx = buffer.getBitOffset();
            bitidx = buffer.decodeExtLength ();
            if (bitidx - _curidx >= 131088)  {
               throw new Asn1Exception ("PER fragmentation not enabled: add -perindef to decode");
            }

            {
               int tval = OTDOA_RequestLocationInformation_multipathRSTD_r14.decodeEnumValue (buffer);
               multipathRSTD_r14 = OTDOA_RequestLocationInformation_multipathRSTD_r14.valueOf (tval);
               buffer.moveBitCursor (bitidx);
            }
            buffer.getContext().eventDispatcher.endElement("multipathRSTD_r14", -1);

         }
         else {
            multipathRSTD_r14 = null;
         }

         // decode maxNoOfRSTDmeas_r14

         if (i < bitcnt && bitmap[i++]) {
            buffer.getContext().eventDispatcher.startElement("maxNoOfRSTDmeas_r14", -1);

            long _curidx = buffer.getBitOffset();
            bitidx = buffer.decodeExtLength ();
            if (bitidx - _curidx >= 131088)  {
               throw new Asn1Exception ("PER fragmentation not enabled: add -perindef to decode");
            }

            maxNoOfRSTDmeas_r14 = new Asn1Integer();
            maxNoOfRSTDmeas_r14.decode (buffer, 1, 32);

            buffer.moveBitCursor (bitidx);
            buffer.getContext().eventDispatcher.endElement("maxNoOfRSTDmeas_r14", -1);

         }
         else {
            maxNoOfRSTDmeas_r14 = null;
         }

         // decode motionMeasurements_r15

         if (i < bitcnt && bitmap[i++]) {
            buffer.getContext().eventDispatcher.startElement("motionMeasurements_r15", -1);

            long _curidx = buffer.getBitOffset();
            bitidx = buffer.decodeExtLength ();
            if (bitidx - _curidx >= 131088)  {
               throw new Asn1Exception ("PER fragmentation not enabled: add -perindef to decode");
            }

            {
               int tval = OTDOA_RequestLocationInformation_motionMeasurements_r15.decodeEnumValue (buffer);
               motionMeasurements_r15 = OTDOA_RequestLocationInformation_motionMeasurements_r15.valueOf (tval);
               buffer.moveBitCursor (bitidx);
            }
            buffer.getContext().eventDispatcher.endElement("motionMeasurements_r15", -1);

         }
         else {
            motionMeasurements_r15 = null;
         }

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = ((multipathRSTD_r14 != null) ||
      (maxNoOfRSTDmeas_r14 != null) ||
      (motionMeasurements_r15 != null) ||
      ((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode assistanceAvailability

      if (assistanceAvailability != null) {
         buffer.getContext().eventDispatcher.startElement("assistanceAvailability", -1);

         assistanceAvailability.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("assistanceAvailability", -1);
      }
      else throw new Asn1MissingRequiredException ("assistanceAvailability");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 3;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         buffer.encodeBit ((multipathRSTD_r14 != null), null);
         buffer.encodeBit ((maxNoOfRSTDmeas_r14 != null), null);
         buffer.encodeBit ((motionMeasurements_r15 != null), null);

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         Asn1PerEncodeBuffer mainBuffer = buffer;
         buffer = new Asn1PerEncodeBuffer (buffer.isAligned());

         // multipathRSTD_r14

         if (multipathRSTD_r14 != null) {
            buffer.reset();
            multipathRSTD_r14.encode (buffer);
            buffer.getContext().eventDispatcher.startElement("multipathRSTD_r14", -1);

            mainBuffer.encodeOpenType (buffer, null);

            buffer.getContext().eventDispatcher.endElement("multipathRSTD_r14", -1);
         }

         // maxNoOfRSTDmeas_r14

         if (maxNoOfRSTDmeas_r14 != null) {
            buffer.reset();
            maxNoOfRSTDmeas_r14.encode (buffer, 1, 32);
            buffer.getContext().eventDispatcher.startElement("maxNoOfRSTDmeas_r14", -1);

            mainBuffer.encodeOpenType (buffer, null);

            buffer.getContext().eventDispatcher.endElement("maxNoOfRSTDmeas_r14", -1);
         }

         // motionMeasurements_r15

         if (motionMeasurements_r15 != null) {
            buffer.reset();
            motionMeasurements_r15.encode (buffer);
            buffer.getContext().eventDispatcher.startElement("motionMeasurements_r15", -1);

            mainBuffer.encodeOpenType (buffer, null);

            buffer.getContext().eventDispatcher.endElement("motionMeasurements_r15", -1);
         }

         buffer = mainBuffer;

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
