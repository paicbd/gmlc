/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class DGNSS_CorrectionsElement extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "DGNSS-CorrectionsElement";
   }

   protected SV_ID svID;
   protected Asn1BitString iod;
   protected Asn1Integer udre;
   protected Asn1Integer pseudoRangeCor;
   protected Asn1Integer rangeRateCor;
   protected Asn1Integer udreGrowthRate;  // optional
   protected Asn1Integer udreValidityTime;  // optional
   protected Asn1OpenExt extElem1;

   public DGNSS_CorrectionsElement () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public DGNSS_CorrectionsElement (
      SV_ID svID_,
      Asn1BitString iod_,
      Asn1Integer udre_,
      Asn1Integer pseudoRangeCor_,
      Asn1Integer rangeRateCor_,
      Asn1Integer udreGrowthRate_,
      Asn1Integer udreValidityTime_
   ) throws Asn1Exception {
      super();
      setSvID (svID_);
      setIod (iod_);
      setUdre (udre_);
      setPseudoRangeCor (pseudoRangeCor_);
      setRangeRateCor (rangeRateCor_);
      setUdreGrowthRate (udreGrowthRate_);
      setUdreValidityTime (udreValidityTime_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public DGNSS_CorrectionsElement (
      SV_ID svID_,
      Asn1BitString iod_,
      Asn1Integer udre_,
      Asn1Integer pseudoRangeCor_,
      Asn1Integer rangeRateCor_
   ) throws Asn1Exception {
      super();
      setSvID (svID_);
      setIod (iod_);
      setUdre (udre_);
      setPseudoRangeCor (pseudoRangeCor_);
      setRangeRateCor (rangeRateCor_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public DGNSS_CorrectionsElement (SV_ID svID_,
      Asn1BitString iod_,
      long udre_,
      long pseudoRangeCor_,
      long rangeRateCor_,
      long udreGrowthRate_,
      long udreValidityTime_
   ) throws Asn1Exception {
      super();
      setSvID (svID_);
      setIod (iod_);
      setUdre (udre_);
      setPseudoRangeCor (pseudoRangeCor_);
      setRangeRateCor (rangeRateCor_);
      setUdreGrowthRate (udreGrowthRate_);
      setUdreValidityTime (udreValidityTime_);
   }

   /**
    * This constructor is for required elements only.  It allows 
    * primitive data to be passed for all primitive elements.  
    * It will create new object wrappers for the primitive data 
    * and set other elements to references to the given objects. 
    */
   public DGNSS_CorrectionsElement (
      SV_ID svID_,
      Asn1BitString iod_,
      long udre_,
      long pseudoRangeCor_,
      long rangeRateCor_
   ) throws Asn1Exception {
      super();
      setSvID (svID_);
      setIod (iod_);
      setUdre (udre_);
      setPseudoRangeCor (pseudoRangeCor_);
      setRangeRateCor (rangeRateCor_);
   }

   public void init () {
      svID = null;
      iod = null;
      udre = null;
      pseudoRangeCor = null;
      rangeRateCor = null;
      udreGrowthRate = null;
      udreValidityTime = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof DGNSS_CorrectionsElement) ) return false;

      DGNSS_CorrectionsElement rhs = (DGNSS_CorrectionsElement) obj;

      if (svID == null) {
         if (rhs.svID != null) return false;
      }
      else {
         if (!svID.equals(rhs.svID)) {
            return false;
         }
      }

      if (iod == null) {
         if (rhs.iod != null) return false;
      }
      else {
         if (!iod.equals(rhs.iod)) {
            return false;
         }
      }

      if (udre == null) {
         if (rhs.udre != null) return false;
      }
      else {
         if (!udre.equals(rhs.udre)) {
            return false;
         }
      }

      if (pseudoRangeCor == null) {
         if (rhs.pseudoRangeCor != null) return false;
      }
      else {
         if (!pseudoRangeCor.equals(rhs.pseudoRangeCor)) {
            return false;
         }
      }

      if (rangeRateCor == null) {
         if (rhs.rangeRateCor != null) return false;
      }
      else {
         if (!rangeRateCor.equals(rhs.rangeRateCor)) {
            return false;
         }
      }

      if (udreGrowthRate == null) {
         if (rhs.udreGrowthRate != null) return false;
      }
      else {
         if (!udreGrowthRate.equals(rhs.udreGrowthRate)) {
            return false;
         }
      }

      if (udreValidityTime == null) {
         if (rhs.udreValidityTime != null) return false;
      }
      else {
         if (!udreValidityTime.equals(rhs.udreValidityTime)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (svID != null) __code = 31*__code + svID.hashCode();
      if (iod != null) __code = 31*__code + iod.hashCode();
      if (udre != null) __code = 31*__code + udre.hashCode();
      if (pseudoRangeCor != null) __code = 31*__code + pseudoRangeCor.hashCode();
      if (rangeRateCor != null) __code = 31*__code + rangeRateCor.hashCode();
      if (udreGrowthRate != null) __code = 31*__code + udreGrowthRate.hashCode();
      if (udreValidityTime != null) __code = 31*__code + udreValidityTime.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for SvID
    */
   public SV_ID getSvID () {
      return svID;
   }

   public void setSvID (SV_ID value) {
      this.svID = value;
   }

   /**
    * Accessor/mutator methods for Iod
    */
   public Asn1BitString getIod () {
      return iod;
   }

   public void setIod (Asn1BitString value) 
      throws Asn1Exception
   {
      if (!(value.getLength() == 11)) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.iod = value;
   }

   /**
    * Accessor/mutator methods for Udre
    */
   public Asn1Integer getUdre () {
      return udre;
   }

   public void setUdre (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 3))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.udre = value;
   }

   public void setUdre (long value) 
      throws Asn1Exception
   {
      setUdre (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for PseudoRangeCor
    */
   public Asn1Integer getPseudoRangeCor () {
      return pseudoRangeCor;
   }

   public void setPseudoRangeCor (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= -2047 && value.value <= 2047))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.pseudoRangeCor = value;
   }

   public void setPseudoRangeCor (long value) 
      throws Asn1Exception
   {
      setPseudoRangeCor (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for RangeRateCor
    */
   public Asn1Integer getRangeRateCor () {
      return rangeRateCor;
   }

   public void setRangeRateCor (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= -127 && value.value <= 127))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.rangeRateCor = value;
   }

   public void setRangeRateCor (long value) 
      throws Asn1Exception
   {
      setRangeRateCor (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for UdreGrowthRate
    */
   public Asn1Integer getUdreGrowthRate () {
      return udreGrowthRate;
   }

   public void setUdreGrowthRate (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 7))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.udreGrowthRate = value;
   }

   public void setUdreGrowthRate (long value) 
      throws Asn1Exception
   {
      setUdreGrowthRate (new Asn1Integer(value));
   }
   public boolean hasUdreGrowthRate () {
      return (udreGrowthRate != null);
   }

   /**
    * Accessor/mutator methods for UdreValidityTime
    */
   public Asn1Integer getUdreValidityTime () {
      return udreValidityTime;
   }

   public void setUdreValidityTime (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 7))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.udreValidityTime = value;
   }

   public void setUdreValidityTime (long value) 
      throws Asn1Exception
   {
      setUdreValidityTime (new Asn1Integer(value));
   }
   public boolean hasUdreValidityTime () {
      return (udreValidityTime != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 8; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return svID;
         case 1: return iod;
         case 2: return udre;
         case 3: return pseudoRangeCor;
         case 4: return rangeRateCor;
         case 5: return udreGrowthRate;
         case 6: return udreValidityTime;
         case 7: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "svID";
         case 1: return "iod";
         case 2: return "udre";
         case 3: return "pseudoRangeCor";
         case 4: return "rangeRateCor";
         case 5: return "udreGrowthRate";
         case 6: return "udreValidityTime";
         case 7: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean udreGrowthRatePresent = buffer.decodeBit ("udreGrowthRatePresent");
      boolean udreValidityTimePresent = buffer.decodeBit ("udreValidityTimePresent");

      // decode svID

      buffer.getContext().eventDispatcher.startElement("svID", -1);

      svID = new SV_ID();
      svID.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("svID", -1);

      // decode iod

      buffer.getContext().eventDispatcher.startElement("iod", -1);

      iod = new Asn1BitString();
      iod.decode (buffer, 11, 11);
      buffer.getContext().eventDispatcher.endElement("iod", -1);

      // decode udre

      buffer.getContext().eventDispatcher.startElement("udre", -1);

      udre = new Asn1Integer();
      udre.decode (buffer, 0, 3);
      buffer.getContext().eventDispatcher.endElement("udre", -1);

      // decode pseudoRangeCor

      buffer.getContext().eventDispatcher.startElement("pseudoRangeCor", -1);

      pseudoRangeCor = new Asn1Integer();
      pseudoRangeCor.decode (buffer, -2047, 2047);
      buffer.getContext().eventDispatcher.endElement("pseudoRangeCor", -1);

      // decode rangeRateCor

      buffer.getContext().eventDispatcher.startElement("rangeRateCor", -1);

      rangeRateCor = new Asn1Integer();
      rangeRateCor.decode (buffer, -127, 127);
      buffer.getContext().eventDispatcher.endElement("rangeRateCor", -1);

      // decode udreGrowthRate

      if (udreGrowthRatePresent) {
         buffer.getContext().eventDispatcher.startElement("udreGrowthRate", -1);

         udreGrowthRate = new Asn1Integer();
         udreGrowthRate.decode (buffer, 0, 7);
         buffer.getContext().eventDispatcher.endElement("udreGrowthRate", -1);
      }
      else {
         udreGrowthRate = null;
      }

      // decode udreValidityTime

      if (udreValidityTimePresent) {
         buffer.getContext().eventDispatcher.startElement("udreValidityTime", -1);

         udreValidityTime = new Asn1Integer();
         udreValidityTime.decode (buffer, 0, 7);
         buffer.getContext().eventDispatcher.endElement("udreValidityTime", -1);
      }
      else {
         udreValidityTime = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((udreGrowthRate != null), null);
      buffer.encodeBit ((udreValidityTime != null), null);

      // encode svID

      if (svID != null) {
         buffer.getContext().eventDispatcher.startElement("svID", -1);

         svID.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("svID", -1);
      }
      else throw new Asn1MissingRequiredException ("svID");

      // encode iod

      if (iod != null) {
         buffer.getContext().eventDispatcher.startElement("iod", -1);

         iod.encode (buffer, 11, 11);

         buffer.getContext().eventDispatcher.endElement("iod", -1);
      }
      else throw new Asn1MissingRequiredException ("iod");

      // encode udre

      if (udre != null) {
         buffer.getContext().eventDispatcher.startElement("udre", -1);

         udre.encode (buffer, 0, 3);

         buffer.getContext().eventDispatcher.endElement("udre", -1);
      }
      else throw new Asn1MissingRequiredException ("udre");

      // encode pseudoRangeCor

      if (pseudoRangeCor != null) {
         buffer.getContext().eventDispatcher.startElement("pseudoRangeCor", -1);

         pseudoRangeCor.encode (buffer, -2047, 2047);

         buffer.getContext().eventDispatcher.endElement("pseudoRangeCor", -1);
      }
      else throw new Asn1MissingRequiredException ("pseudoRangeCor");

      // encode rangeRateCor

      if (rangeRateCor != null) {
         buffer.getContext().eventDispatcher.startElement("rangeRateCor", -1);

         rangeRateCor.encode (buffer, -127, 127);

         buffer.getContext().eventDispatcher.endElement("rangeRateCor", -1);
      }
      else throw new Asn1MissingRequiredException ("rangeRateCor");

      // encode udreGrowthRate

      if (udreGrowthRate != null) {
         buffer.getContext().eventDispatcher.startElement("udreGrowthRate", -1);

         udreGrowthRate.encode (buffer, 0, 7);

         buffer.getContext().eventDispatcher.endElement("udreGrowthRate", -1);
      }

      // encode udreValidityTime

      if (udreValidityTime != null) {
         buffer.getContext().eventDispatcher.startElement("udreValidityTime", -1);

         udreValidityTime.encode (buffer, 0, 7);

         buffer.getContext().eventDispatcher.endElement("udreValidityTime", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
