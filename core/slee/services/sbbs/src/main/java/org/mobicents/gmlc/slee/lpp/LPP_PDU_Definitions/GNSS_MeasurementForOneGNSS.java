/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class GNSS_MeasurementForOneGNSS extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "GNSS-MeasurementForOneGNSS";
   }

   protected GNSS_ID gnss_ID;
   protected GNSS_SgnMeasList gnss_SgnMeasList;
   protected Asn1OpenExt extElem1;

   public GNSS_MeasurementForOneGNSS () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public GNSS_MeasurementForOneGNSS (
      GNSS_ID gnss_ID_,
      GNSS_SgnMeasList gnss_SgnMeasList_
   ) throws Asn1Exception {
      super();
      setGnss_ID (gnss_ID_);
      setGnss_SgnMeasList (gnss_SgnMeasList_);
   }

   public void init () {
      gnss_ID = null;
      gnss_SgnMeasList = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof GNSS_MeasurementForOneGNSS) ) return false;

      GNSS_MeasurementForOneGNSS rhs = (GNSS_MeasurementForOneGNSS) obj;

      if (gnss_ID == null) {
         if (rhs.gnss_ID != null) return false;
      }
      else {
         if (!gnss_ID.equals(rhs.gnss_ID)) {
            return false;
         }
      }

      if (gnss_SgnMeasList == null) {
         if (rhs.gnss_SgnMeasList != null) return false;
      }
      else {
         if (!gnss_SgnMeasList.equals(rhs.gnss_SgnMeasList)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (gnss_ID != null) __code = 31*__code + gnss_ID.hashCode();
      if (gnss_SgnMeasList != null) __code = 31*__code + gnss_SgnMeasList.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Gnss_ID
    */
   public GNSS_ID getGnss_ID () {
      return gnss_ID;
   }

   public void setGnss_ID (GNSS_ID value) {
      this.gnss_ID = value;
   }

   /**
    * Accessor/mutator methods for Gnss_SgnMeasList
    */
   public GNSS_SgnMeasList getGnss_SgnMeasList () {
      return gnss_SgnMeasList;
   }

   public void setGnss_SgnMeasList (GNSS_SgnMeasList value) 
      throws Asn1Exception
   {
      if (!((value.getElements().length >= 1 && value.getElements().length <= 8))) {
         throw new Asn1ConsVioException ("value.getElements().length", value.getElements().length);
      }

      this.gnss_SgnMeasList = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return gnss_ID;
         case 1: return gnss_SgnMeasList;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "gnss-ID";
         case 1: return "gnss-SgnMeasList";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode gnss_ID

      buffer.getContext().eventDispatcher.startElement("gnss_ID", -1);

      gnss_ID = new GNSS_ID();
      gnss_ID.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("gnss_ID", -1);

      // decode gnss_SgnMeasList

      buffer.getContext().eventDispatcher.startElement("gnss_SgnMeasList", -1);

      gnss_SgnMeasList = new GNSS_SgnMeasList();
      gnss_SgnMeasList.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("gnss_SgnMeasList", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode gnss_ID

      if (gnss_ID != null) {
         buffer.getContext().eventDispatcher.startElement("gnss_ID", -1);

         gnss_ID.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("gnss_ID", -1);
      }
      else throw new Asn1MissingRequiredException ("gnss_ID");

      // encode gnss_SgnMeasList

      if (gnss_SgnMeasList != null) {
         buffer.getContext().eventDispatcher.startElement("gnss_SgnMeasList", -1);

         gnss_SgnMeasList.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("gnss_SgnMeasList", -1);
      }
      else throw new Asn1MissingRequiredException ("gnss_SgnMeasList");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
