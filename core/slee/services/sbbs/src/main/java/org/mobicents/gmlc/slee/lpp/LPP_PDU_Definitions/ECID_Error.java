/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class ECID_Error extends Asn1Choice {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "ECID-Error";
   }

   // Choice element identifier constants
   public final static byte _LOCATIONSERVERERRORCAUSES = 1;
   public final static byte _TARGETDEVICEERRORCAUSES = 2;
   public final static byte _EXTELEM1 = 3;

   public ECID_Error () {
      super();
   }

   public ECID_Error (byte choiceId_, Asn1Type element_) {
      super();
      setElement (choiceId_, element_);
   }

   public String getElemName () {
      switch (choiceID) {
      case _LOCATIONSERVERERRORCAUSES: return "locationServerErrorCauses";
      case _TARGETDEVICEERRORCAUSES: return "targetDeviceErrorCauses";
      case _EXTELEM1: return "...";
      default: return "UNDEFINED";
      }
   }

   /**
    * Accessor/mutator methods for locationServerErrorCauses
    */
   public ECID_LocationServerErrorCauses getLocationServerErrorCauses () {
      return (choiceID == _LOCATIONSERVERERRORCAUSES) ? (ECID_LocationServerErrorCauses)element : null;
   }
   public void setLocationServerErrorCauses (ECID_LocationServerErrorCauses value) {
      setElement (_LOCATIONSERVERERRORCAUSES, value);
   }
   public boolean isLocationServerErrorCauses () {
      return (choiceID == _LOCATIONSERVERERRORCAUSES);
   }

   /**
    * Accessor/mutator methods for targetDeviceErrorCauses
    */
   public ECID_TargetDeviceErrorCauses getTargetDeviceErrorCauses () {
      return (choiceID == _TARGETDEVICEERRORCAUSES) ? (ECID_TargetDeviceErrorCauses)element : null;
   }
   public void setTargetDeviceErrorCauses (ECID_TargetDeviceErrorCauses value) {
      setElement (_TARGETDEVICEERRORCAUSES, value);
   }
   public boolean isTargetDeviceErrorCauses () {
      return (choiceID == _TARGETDEVICEERRORCAUSES);
   }

   /**
    * Accessor/mutator methods for extElem1
    */
   public Asn1OpenType getExtElem1 () {
      return (choiceID == _EXTELEM1) ? (Asn1OpenType)element : null;
   }
   public void setExtElem1 (Asn1OpenType value) {
      setElement (_EXTELEM1, value);
   }
   public boolean isExtElem1 () {
      return (choiceID == _EXTELEM1);
   }

   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      Asn1PerDecodeBuffer savedBuffer = null;
      boolean extbit = buffer.decodeBit ("extbit");

      if (!extbit) {
         int idx = (int) buffer.decodeConsWholeNumber (2, "index");
         choiceID = idx + 1;
         buffer.getContext().eventDispatcher.startElement (getElemName(), -1);

         switch (idx) {
            // locationServerErrorCauses
            case 0: { 
               ECID_LocationServerErrorCauses locationServerErrorCauses;
               element = locationServerErrorCauses = new ECID_LocationServerErrorCauses();
               locationServerErrorCauses.decode (buffer);
               break;
            }

            // targetDeviceErrorCauses
            case 1: { 
               ECID_TargetDeviceErrorCauses targetDeviceErrorCauses;
               element = targetDeviceErrorCauses = new ECID_TargetDeviceErrorCauses();
               targetDeviceErrorCauses.decode (buffer);
               break;
            }

            default:
            throw new Asn1InvalidChoiceOptionException (buffer, idx);
         }
      }
      else {
         int idx = (int) buffer.decodeSmallNonNegWholeNumber ();
         choiceID = idx + 3;
         buffer.getContext().eventDispatcher.startElement (getElemName(), -1);

         buffer.byteAlign();

         Asn1OpenType openType = new Asn1OpenType ();
         openType.decode (buffer);

         setElement (_EXTELEM1, openType);
      }

      buffer.getContext().eventDispatcher.endElement (getElemName(), -1);

      if (savedBuffer != null) {
         buffer = savedBuffer;
      }
   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (choiceID > 2);
      buffer.encodeBit (extbit, "extbit");

      if (!extbit) {

         // Encode choice index value

         buffer.encodeConsWholeNumber (choiceID - 1, 2, "index");

         buffer.getContext().eventDispatcher.startElement("getElemName()", -1);

         switch (choiceID) {
            // locationServerErrorCauses
            case _LOCATIONSERVERERRORCAUSES:
               ECID_LocationServerErrorCauses locationServerErrorCauses = (ECID_LocationServerErrorCauses) element;
               locationServerErrorCauses.encode (buffer);
               break;

            // targetDeviceErrorCauses
            case _TARGETDEVICEERRORCAUSES:
               ECID_TargetDeviceErrorCauses targetDeviceErrorCauses = (ECID_TargetDeviceErrorCauses) element;
               targetDeviceErrorCauses.encode (buffer);
               break;

            default:
            throw new Asn1InvalidChoiceOptionException();
         }
      }
      else {
         // Encode extension choice index value

         buffer.encodeSmallNonNegWholeNumber (choiceID - 3);

         // Encode extension element data value

         buffer.getContext().eventDispatcher.startElement("getElemName()", -1);

         Asn1OpenType openType = (Asn1OpenType) element;
         buffer.byteAlign();
         openType.encode (buffer);
      }

      buffer.getContext().eventDispatcher.endElement("getElemName()", -1);
   }

}
