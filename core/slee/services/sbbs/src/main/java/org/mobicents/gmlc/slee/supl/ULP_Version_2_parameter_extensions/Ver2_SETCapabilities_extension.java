/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.supl.ULP_Version_2_parameter_extensions;

import com.objsys.asn1j.runtime.*;

public class Ver2_SETCapabilities_extension extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_ULP_Version_2_parameter_extensionsRtkey._rtkey);
      Asn1Type._setLicLocation(_ULP_Version_2_parameter_extensionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "Ver2-SETCapabilities-extension";
   }

   protected ServiceCapabilities serviceCapabilities;  // optional
   protected SupportedBearers supportedBearers;  // optional
   protected Asn1OpenExt extElem1;

   public Ver2_SETCapabilities_extension () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public Ver2_SETCapabilities_extension (
      ServiceCapabilities serviceCapabilities_,
      SupportedBearers supportedBearers_
   ) {
      super();
      setServiceCapabilities (serviceCapabilities_);
      setSupportedBearers (supportedBearers_);
   }

   public void init () {
      serviceCapabilities = null;
      supportedBearers = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof Ver2_SETCapabilities_extension) ) return false;

      Ver2_SETCapabilities_extension rhs = (Ver2_SETCapabilities_extension) obj;

      if (serviceCapabilities == null) {
         if (rhs.serviceCapabilities != null) return false;
      }
      else {
         if (!serviceCapabilities.equals(rhs.serviceCapabilities)) {
            return false;
         }
      }

      if (supportedBearers == null) {
         if (rhs.supportedBearers != null) return false;
      }
      else {
         if (!supportedBearers.equals(rhs.supportedBearers)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (serviceCapabilities != null) __code = 31*__code + serviceCapabilities.hashCode();
      if (supportedBearers != null) __code = 31*__code + supportedBearers.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for ServiceCapabilities
    */
   public ServiceCapabilities getServiceCapabilities () {
      return serviceCapabilities;
   }

   public void setServiceCapabilities (ServiceCapabilities value) {
      this.serviceCapabilities = value;
   }

   public boolean hasServiceCapabilities () {
      return (serviceCapabilities != null);
   }

   /**
    * Accessor/mutator methods for SupportedBearers
    */
   public SupportedBearers getSupportedBearers () {
      return supportedBearers;
   }

   public void setSupportedBearers (SupportedBearers value) {
      this.supportedBearers = value;
   }

   public boolean hasSupportedBearers () {
      return (supportedBearers != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return serviceCapabilities;
         case 1: return supportedBearers;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "serviceCapabilities";
         case 1: return "supportedBearers";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean serviceCapabilitiesPresent = buffer.decodeBit ("serviceCapabilitiesPresent");

      // decode serviceCapabilities

      if (serviceCapabilitiesPresent) {
         buffer.getContext().eventDispatcher.startElement("serviceCapabilities", -1);

         serviceCapabilities = new ServiceCapabilities();
         serviceCapabilities.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("serviceCapabilities", -1);
      }
      else {
         serviceCapabilities = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         if (i < bitcnt) bitmap[i++] = buffer.decodeBit ("supportedBearersPresent");

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode supportedBearers

         if (i < bitcnt && bitmap[i++]) {
            buffer.getContext().eventDispatcher.startElement("supportedBearers", -1);

            long _curidx = buffer.getBitOffset();
            bitidx = buffer.decodeExtLength ();
            if (bitidx - _curidx >= 131088)  {
               throw new Asn1Exception ("PER fragmentation not enabled: add -perindef to decode");
            }

            supportedBearers = new SupportedBearers();
            supportedBearers.decode (buffer);

            buffer.moveBitCursor (bitidx);
            buffer.getContext().eventDispatcher.endElement("supportedBearers", -1);

         }
         else {
            supportedBearers = null;
         }

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = ((supportedBearers != null) ||
      ((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((serviceCapabilities != null), null);

      // encode serviceCapabilities

      if (serviceCapabilities != null) {
         buffer.getContext().eventDispatcher.startElement("serviceCapabilities", -1);

         serviceCapabilities.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("serviceCapabilities", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 1;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         buffer.encodeBit ((supportedBearers != null), null);

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         Asn1PerEncodeBuffer mainBuffer = buffer;
         buffer = new Asn1PerEncodeBuffer (buffer.isAligned());

         // supportedBearers

         if (supportedBearers != null) {
            buffer.reset();
            supportedBearers.encode (buffer);
            buffer.getContext().eventDispatcher.startElement("supportedBearers", -1);

            mainBuffer.encodeOpenType (buffer, null);

            buffer.getContext().eventDispatcher.endElement("supportedBearers", -1);
         }

         buffer = mainBuffer;

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
