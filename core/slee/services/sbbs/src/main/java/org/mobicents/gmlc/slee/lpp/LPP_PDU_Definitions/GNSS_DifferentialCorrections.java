/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class GNSS_DifferentialCorrections extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "GNSS-DifferentialCorrections";
   }

   protected Asn1Integer dgnss_RefTime;
   protected DGNSS_SgnTypeList dgnss_SgnTypeList;
   protected Asn1OpenExt extElem1;

   public GNSS_DifferentialCorrections () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public GNSS_DifferentialCorrections (
      Asn1Integer dgnss_RefTime_,
      DGNSS_SgnTypeList dgnss_SgnTypeList_
   ) throws Asn1Exception {
      super();
      setDgnss_RefTime (dgnss_RefTime_);
      setDgnss_SgnTypeList (dgnss_SgnTypeList_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public GNSS_DifferentialCorrections (long dgnss_RefTime_,
      DGNSS_SgnTypeList dgnss_SgnTypeList_
   ) throws Asn1Exception {
      super();
      setDgnss_RefTime (dgnss_RefTime_);
      setDgnss_SgnTypeList (dgnss_SgnTypeList_);
   }

   public void init () {
      dgnss_RefTime = null;
      dgnss_SgnTypeList = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof GNSS_DifferentialCorrections) ) return false;

      GNSS_DifferentialCorrections rhs = (GNSS_DifferentialCorrections) obj;

      if (dgnss_RefTime == null) {
         if (rhs.dgnss_RefTime != null) return false;
      }
      else {
         if (!dgnss_RefTime.equals(rhs.dgnss_RefTime)) {
            return false;
         }
      }

      if (dgnss_SgnTypeList == null) {
         if (rhs.dgnss_SgnTypeList != null) return false;
      }
      else {
         if (!dgnss_SgnTypeList.equals(rhs.dgnss_SgnTypeList)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (dgnss_RefTime != null) __code = 31*__code + dgnss_RefTime.hashCode();
      if (dgnss_SgnTypeList != null) __code = 31*__code + dgnss_SgnTypeList.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Dgnss_RefTime
    */
   public Asn1Integer getDgnss_RefTime () {
      return dgnss_RefTime;
   }

   public void setDgnss_RefTime (Asn1Integer value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 3599))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.dgnss_RefTime = value;
   }

   public void setDgnss_RefTime (long value) 
      throws Asn1Exception
   {
      setDgnss_RefTime (new Asn1Integer(value));
   }

   /**
    * Accessor/mutator methods for Dgnss_SgnTypeList
    */
   public DGNSS_SgnTypeList getDgnss_SgnTypeList () {
      return dgnss_SgnTypeList;
   }

   public void setDgnss_SgnTypeList (DGNSS_SgnTypeList value) 
      throws Asn1Exception
   {
      if (!((value.getElements().length >= 1 && value.getElements().length <= 3))) {
         throw new Asn1ConsVioException ("value.getElements().length", value.getElements().length);
      }

      this.dgnss_SgnTypeList = value;
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return dgnss_RefTime;
         case 1: return dgnss_SgnTypeList;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "dgnss-RefTime";
         case 1: return "dgnss-SgnTypeList";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode dgnss_RefTime

      buffer.getContext().eventDispatcher.startElement("dgnss_RefTime", -1);

      dgnss_RefTime = new Asn1Integer();
      dgnss_RefTime.decode (buffer, 0, 3599);
      buffer.getContext().eventDispatcher.endElement("dgnss_RefTime", -1);

      // decode dgnss_SgnTypeList

      buffer.getContext().eventDispatcher.startElement("dgnss_SgnTypeList", -1);

      dgnss_SgnTypeList = new DGNSS_SgnTypeList();
      dgnss_SgnTypeList.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("dgnss_SgnTypeList", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode dgnss_RefTime

      if (dgnss_RefTime != null) {
         buffer.getContext().eventDispatcher.startElement("dgnss_RefTime", -1);

         dgnss_RefTime.encode (buffer, 0, 3599);

         buffer.getContext().eventDispatcher.endElement("dgnss_RefTime", -1);
      }
      else throw new Asn1MissingRequiredException ("dgnss_RefTime");

      // encode dgnss_SgnTypeList

      if (dgnss_SgnTypeList != null) {
         buffer.getContext().eventDispatcher.startElement("dgnss_SgnTypeList", -1);

         dgnss_SgnTypeList.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("dgnss_SgnTypeList", -1);
      }
      else throw new Asn1MissingRequiredException ("dgnss_SgnTypeList");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
