/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class ECID_ProvideLocationInformation extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "ECID-ProvideLocationInformation";
   }

   protected ECID_SignalMeasurementInformation ecid_SignalMeasurementInformation;  // optional
   protected ECID_Error ecid_Error;  // optional
   protected Asn1OpenExt extElem1;

   public ECID_ProvideLocationInformation () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public ECID_ProvideLocationInformation (
      ECID_SignalMeasurementInformation ecid_SignalMeasurementInformation_,
      ECID_Error ecid_Error_
   ) {
      super();
      setEcid_SignalMeasurementInformation (ecid_SignalMeasurementInformation_);
      setEcid_Error (ecid_Error_);
   }

   public void init () {
      ecid_SignalMeasurementInformation = null;
      ecid_Error = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof ECID_ProvideLocationInformation) ) return false;

      ECID_ProvideLocationInformation rhs = (ECID_ProvideLocationInformation) obj;

      if (ecid_SignalMeasurementInformation == null) {
         if (rhs.ecid_SignalMeasurementInformation != null) return false;
      }
      else {
         if (!ecid_SignalMeasurementInformation.equals(rhs.ecid_SignalMeasurementInformation)) {
            return false;
         }
      }

      if (ecid_Error == null) {
         if (rhs.ecid_Error != null) return false;
      }
      else {
         if (!ecid_Error.equals(rhs.ecid_Error)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (ecid_SignalMeasurementInformation != null) __code = 31*__code + ecid_SignalMeasurementInformation.hashCode();
      if (ecid_Error != null) __code = 31*__code + ecid_Error.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for Ecid_SignalMeasurementInformation
    */
   public ECID_SignalMeasurementInformation getEcid_SignalMeasurementInformation () {
      return ecid_SignalMeasurementInformation;
   }

   public void setEcid_SignalMeasurementInformation (ECID_SignalMeasurementInformation value) {
      this.ecid_SignalMeasurementInformation = value;
   }

   public boolean hasEcid_SignalMeasurementInformation () {
      return (ecid_SignalMeasurementInformation != null);
   }

   /**
    * Accessor/mutator methods for Ecid_Error
    */
   public ECID_Error getEcid_Error () {
      return ecid_Error;
   }

   public void setEcid_Error (ECID_Error value) {
      this.ecid_Error = value;
   }

   public boolean hasEcid_Error () {
      return (ecid_Error != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return ecid_SignalMeasurementInformation;
         case 1: return ecid_Error;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "ecid-SignalMeasurementInformation";
         case 1: return "ecid-Error";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean ecid_SignalMeasurementInformationPresent = buffer.decodeBit ("ecid_SignalMeasurementInformationPresent");
      boolean ecid_ErrorPresent = buffer.decodeBit ("ecid_ErrorPresent");

      // decode ecid_SignalMeasurementInformation

      if (ecid_SignalMeasurementInformationPresent) {
         buffer.getContext().eventDispatcher.startElement("ecid_SignalMeasurementInformation", -1);

         ecid_SignalMeasurementInformation = new ECID_SignalMeasurementInformation();
         ecid_SignalMeasurementInformation.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("ecid_SignalMeasurementInformation", -1);
      }
      else {
         ecid_SignalMeasurementInformation = null;
      }

      // decode ecid_Error

      if (ecid_ErrorPresent) {
         buffer.getContext().eventDispatcher.startElement("ecid_Error", -1);

         ecid_Error = new ECID_Error();
         ecid_Error.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("ecid_Error", -1);
      }
      else {
         ecid_Error = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((ecid_SignalMeasurementInformation != null), null);
      buffer.encodeBit ((ecid_Error != null), null);

      // encode ecid_SignalMeasurementInformation

      if (ecid_SignalMeasurementInformation != null) {
         buffer.getContext().eventDispatcher.startElement("ecid_SignalMeasurementInformation", -1);

         ecid_SignalMeasurementInformation.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("ecid_SignalMeasurementInformation", -1);
      }

      // encode ecid_Error

      if (ecid_Error != null) {
         buffer.getContext().eventDispatcher.startElement("ecid_Error", -1);

         ecid_Error.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("ecid_Error", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
