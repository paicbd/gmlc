/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class EPDU_Identifier extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "EPDU-Identifier";
   }

   protected EPDU_ID ePDU_ID;
   protected EPDU_Name ePDU_Name;  // optional
   protected Asn1OpenExt extElem1;

   public EPDU_Identifier () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public EPDU_Identifier (
      EPDU_ID ePDU_ID_,
      EPDU_Name ePDU_Name_
   ) throws Asn1Exception {
      super();
      setEPDU_ID (ePDU_ID_);
      setEPDU_Name (ePDU_Name_);
   }

   /**
    * This constructor is for required elements only.  It sets 
    * all elements to references to the given objects
    */
   public EPDU_Identifier (
      EPDU_ID ePDU_ID_
   ) throws Asn1Exception {
      super();
      setEPDU_ID (ePDU_ID_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public EPDU_Identifier (long ePDU_ID_,
      String ePDU_Name_
   ) throws Asn1Exception {
      super();
      setEPDU_ID (ePDU_ID_);
      setEPDU_Name (ePDU_Name_);
   }

   /**
    * This constructor is for required elements only.  It allows 
    * primitive data to be passed for all primitive elements.  
    * It will create new object wrappers for the primitive data 
    * and set other elements to references to the given objects. 
    */
   public EPDU_Identifier (
      long ePDU_ID_
   ) throws Asn1Exception {
      super();
      setEPDU_ID (ePDU_ID_);
   }

   public void init () {
      ePDU_ID = null;
      ePDU_Name = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof EPDU_Identifier) ) return false;

      EPDU_Identifier rhs = (EPDU_Identifier) obj;

      if (ePDU_ID == null) {
         if (rhs.ePDU_ID != null) return false;
      }
      else {
         if (!ePDU_ID.equals(rhs.ePDU_ID)) {
            return false;
         }
      }

      if (ePDU_Name == null) {
         if (rhs.ePDU_Name != null) return false;
      }
      else {
         if (!ePDU_Name.equals(rhs.ePDU_Name)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (ePDU_ID != null) __code = 31*__code + ePDU_ID.hashCode();
      if (ePDU_Name != null) __code = 31*__code + ePDU_Name.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for EPDU_ID
    */
   public EPDU_ID getEPDU_ID () {
      return ePDU_ID;
   }

   public void setEPDU_ID (EPDU_ID value) 
      throws Asn1Exception
   {
      if (!((value.value >= 1 && value.value <= 256))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.ePDU_ID = value;
   }

   public void setEPDU_ID (long value) 
      throws Asn1Exception
   {
      setEPDU_ID (new EPDU_ID(value));
   }

   /**
    * Accessor/mutator methods for EPDU_Name
    */
   public EPDU_Name getEPDU_Name () {
      return ePDU_Name;
   }

   public void setEPDU_Name (EPDU_Name value) 
      throws Asn1Exception
   {
      if (!((value.getLength() >= 1 && value.getLength() <= 32))) {
         throw new Asn1ConsVioException ("value.getLength()", value.getLength());
      }

      this.ePDU_Name = value;
   }

   public void setEPDU_Name (String value) 
      throws Asn1Exception
   {
      setEPDU_Name (new EPDU_Name(value));
   }
   public boolean hasEPDU_Name () {
      return (ePDU_Name != null);
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return ePDU_ID;
         case 1: return ePDU_Name;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "ePDU-ID";
         case 1: return "ePDU-Name";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // optional bits

      boolean ePDU_NamePresent = buffer.decodeBit ("ePDU_NamePresent");

      // decode ePDU_ID

      buffer.getContext().eventDispatcher.startElement("ePDU_ID", -1);

      ePDU_ID = new EPDU_ID();
      ePDU_ID.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("ePDU_ID", -1);

      // decode ePDU_Name

      if (ePDU_NamePresent) {
         buffer.getContext().eventDispatcher.startElement("ePDU_Name", -1);

         ePDU_Name = new EPDU_Name();
         ePDU_Name.decode (buffer);
         buffer.getContext().eventDispatcher.endElement("ePDU_Name", -1);
      }
      else {
         ePDU_Name = null;
      }

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode optional elements bit mask

      buffer.encodeBit ((ePDU_Name != null), null);

      // encode ePDU_ID

      if (ePDU_ID != null) {
         buffer.getContext().eventDispatcher.startElement("ePDU_ID", -1);

         ePDU_ID.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("ePDU_ID", -1);
      }
      else throw new Asn1MissingRequiredException ("ePDU_ID");

      // encode ePDU_Name

      if (ePDU_Name != null) {
         buffer.getContext().eventDispatcher.startElement("ePDU_Name", -1);

         ePDU_Name.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("ePDU_Name", -1);
      }

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
