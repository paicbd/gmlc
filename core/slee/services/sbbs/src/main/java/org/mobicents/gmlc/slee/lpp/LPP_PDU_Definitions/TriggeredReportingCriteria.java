/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.0, Date: 07-May-2021.
 */
package org.mobicents.gmlc.slee.lpp.LPP_PDU_Definitions;

import com.objsys.asn1j.runtime.*;

public class TriggeredReportingCriteria extends Asn1Seq {
   private static final long serialVersionUID = 55;
   static {
      _setKey (_LPP_PDU_DefinitionsRtkey._rtkey);
      Asn1Type._setLicLocation(_LPP_PDU_DefinitionsRtkey._licLocation);
   }

   public String getAsn1TypeName()  {
      return "TriggeredReportingCriteria";
   }

   protected Asn1Boolean cellChange;
   protected ReportingDuration reportingDuration;
   protected Asn1OpenExt extElem1;

   public TriggeredReportingCriteria () {
      super();
      init();
   }

   /**
    * This constructor sets all elements to references to the 
    * given objects
    */
   public TriggeredReportingCriteria (
      Asn1Boolean cellChange_,
      ReportingDuration reportingDuration_
   ) throws Asn1Exception {
      super();
      setCellChange (cellChange_);
      setReportingDuration (reportingDuration_);
   }

   /**
    * This constructor allows primitive data to be passed for all 
    * primitive elements.  It will create new object wrappers for 
    * the primitive data and set other elements to references to 
    * the given objects 
    */
   public TriggeredReportingCriteria (boolean cellChange_,
      long reportingDuration_
   ) throws Asn1Exception {
      super();
      setCellChange (cellChange_);
      setReportingDuration (reportingDuration_);
   }

   public void init () {
      cellChange = null;
      reportingDuration = null;
      extElem1 = null;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof TriggeredReportingCriteria) ) return false;

      TriggeredReportingCriteria rhs = (TriggeredReportingCriteria) obj;

      if (cellChange == null) {
         if (rhs.cellChange != null) return false;
      }
      else {
         if (!cellChange.equals(rhs.cellChange)) {
            return false;
         }
      }

      if (reportingDuration == null) {
         if (rhs.reportingDuration != null) return false;
      }
      else {
         if (!reportingDuration.equals(rhs.reportingDuration)) {
            return false;
         }
      }

      if (extElem1 == null) {
         if (rhs.extElem1 != null) return false;
      }
      else {
         if (!extElem1.equals(rhs.extElem1)) {
            return false;
         }
      }

      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (cellChange != null) __code = 31*__code + cellChange.hashCode();
      if (reportingDuration != null) __code = 31*__code + reportingDuration.hashCode();
      if (extElem1 != null) __code = 31*__code + extElem1.hashCode();

      return __code;
   }

   /**
    * Accessor/mutator methods for CellChange
    */
   public Asn1Boolean getCellChange () {
      return cellChange;
   }

   public void setCellChange (Asn1Boolean value) {
      this.cellChange = value;
   }

   public void setCellChange (boolean value) {
      setCellChange (new Asn1Boolean(value));
   }

   /**
    * Accessor/mutator methods for ReportingDuration
    */
   public ReportingDuration getReportingDuration () {
      return reportingDuration;
   }

   public void setReportingDuration (ReportingDuration value) 
      throws Asn1Exception
   {
      if (!((value.value >= 0 && value.value <= 255))) {
         throw new Asn1ConsVioException ("value.value", value.value);
      }

      this.reportingDuration = value;
   }

   public void setReportingDuration (long value) 
      throws Asn1Exception
   {
      setReportingDuration (new ReportingDuration(value));
   }

   /**
    * Accessor/mutator methods for ExtElem1
    */
   public Asn1OpenExt getExtElem1 () {
      return extElem1;
   }

   public void setExtElem1 (Asn1OpenExt value) {
      this.extElem1 = value;
   }

   public int getElementCount() { return 3; }


   public Object getElementValue(int index){
      switch(index)  {
         case 0: return cellChange;
         case 1: return reportingDuration;
         case 2: return extElem1;
         default: return null;
      }
   }


   public String getElementName(int index){
      switch(index)  {
         case 0: return "cellChange";
         case 1: return "reportingDuration";
         case 2: return null;
         default: return null;
      }
   }


   public void decode (Asn1PerDecodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      init ();

      // extension bit

      boolean extbit = buffer.decodeBit ("extbit");

      // decode cellChange

      buffer.getContext().eventDispatcher.startElement("cellChange", -1);

      cellChange = new Asn1Boolean();
      cellChange.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("cellChange", -1);

      // decode reportingDuration

      buffer.getContext().eventDispatcher.startElement("reportingDuration", -1);

      reportingDuration = new ReportingDuration();
      reportingDuration.decode (buffer);
      buffer.getContext().eventDispatcher.endElement("reportingDuration", -1);

      if (extbit) {

         int bitcnt = buffer.decodeSmallLength(), i = 0;
         long bitidx = buffer.getBitOffset() + bitcnt;
         boolean[] bitmap = new boolean [bitcnt];

         while (i < bitcnt) {
            bitmap[i++] = buffer.decodeBit ("unknown");
         }

         i = 0;

         // decode unknown extension elements

         if (i < bitcnt) {
            Asn1OpenType openType = null;
            extElem1 = new Asn1OpenExt();
            int j = 0;
            while (i < bitcnt) {
               if (bitmap[i]) {
                  buffer.getContext().eventDispatcher.startElement("...", -1);

                  openType = extElem1.decodeOpenType (buffer, true, j++);
               }
               else {
                  extElem1.setOpenType (null, j++);
               }
               i++;
            }
         }
         buffer.byteAlign ();

      }

   }

   public void encode (Asn1PerEncodeBuffer buffer)
      throws Asn1Exception, java.io.IOException
   {
      // extension bit

      boolean extbit = (((extElem1 != null) && (!extElem1.value.isEmpty()))
      );

      buffer.encodeBit (extbit, "extbit");

      // encode cellChange

      if (cellChange != null) {
         buffer.getContext().eventDispatcher.startElement("cellChange", -1);

         cellChange.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("cellChange", -1);
      }
      else throw new Asn1MissingRequiredException ("cellChange");

      // encode reportingDuration

      if (reportingDuration != null) {
         buffer.getContext().eventDispatcher.startElement("reportingDuration", -1);

         reportingDuration.encode (buffer);

         buffer.getContext().eventDispatcher.endElement("reportingDuration", -1);
      }
      else throw new Asn1MissingRequiredException ("reportingDuration");

      if (extbit) {

         // encode extension optional bits length

         int bitcnt = 0;
         if (extElem1 != null) bitcnt += extElem1.value.size();
         buffer.encodeSmallLength (bitcnt);

         // encode optional bits

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encodeExtBits (buffer);
         }

         // encode extension elements

         if (extElem1 != null && extElem1.value.size() > 0) {
            extElem1.encode (buffer);
         }
      }
   }

}
